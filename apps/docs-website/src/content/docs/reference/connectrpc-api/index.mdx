---
title: ConnectRPC API
description: Generated reference for Chatto's public ConnectRPC services.
editUrl: false
---

{/* Generated by protoc-gen-doc from proto/chatto/api/v1/*.proto. Do not edit directly. */}

Chatto exposes these ConnectRPC services below `/api/connect`.

Endpoint paths use the Connect convention:

`/api/connect/<fully-qualified-service>/<method>`

Use this reference when building clients against Chatto's public ConnectRPC API.

<a id="chatto-api-v1-NotificationPreferencesService"></a>

## NotificationPreferencesService

Reads and updates per-room notification preferences for the current user.

<a id="chatto-api-v1-NotificationPreferencesService-GetServerNotificationPreference"></a>

### GetServerNotificationPreference

Returns the current user's explicit and effective server notification level.

```http
POST /api/connect/chatto.api.v1.NotificationPreferencesService/GetServerNotificationPreference
```

<a id="chatto-api-v1-GetServerNotificationPreferenceRequest"></a>

#### Input: GetServerNotificationPreferenceRequest

Request for the current user's server-level notification preference.


<a id="chatto-api-v1-GetServerNotificationPreferenceResponse"></a>

#### Result: GetServerNotificationPreferenceResponse

Current server-level notification preference.

| Field | Type | Description |
| --- | --- | --- |
| `level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Explicit server-level setting. |
| `effective_level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Level after applying system defaults. |


<a id="chatto-api-v1-NotificationPreferencesService-SetServerNotificationLevel"></a>

### SetServerNotificationLevel

Sets the current user's explicit server notification level and returns the
resulting stored and effective levels.

```http
POST /api/connect/chatto.api.v1.NotificationPreferencesService/SetServerNotificationLevel
```

<a id="chatto-api-v1-SetServerNotificationLevelRequest"></a>

#### Input: SetServerNotificationLevelRequest

Request to update the current user's server-level notification level.

| Field | Type | Description |
| --- | --- | --- |
| `level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Required. New explicit notification level. Use NOTIFICATION_LEVEL_DEFAULT to return the server to default behavior. |


<a id="chatto-api-v1-SetServerNotificationLevelResponse"></a>

#### Result: SetServerNotificationLevelResponse

Updated server-level notification preference.

| Field | Type | Description |
| --- | --- | --- |
| `level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Explicit server-level setting. |
| `effective_level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Level after applying system defaults. |


<a id="chatto-api-v1-NotificationPreferencesService-GetRoomNotificationPreference"></a>

### GetRoomNotificationPreference

Returns the current user's explicit and effective notification level for a
room. Use this before rendering room notification controls so the UI can
distinguish inherited defaults from an explicit room override.

```http
POST /api/connect/chatto.api.v1.NotificationPreferencesService/GetRoomNotificationPreference
```

<a id="chatto-api-v1-GetRoomNotificationPreferenceRequest"></a>

#### Input: GetRoomNotificationPreferenceRequest

Request for the current user's notification preference in one room.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room whose notification preference should be loaded for the current user. |


<a id="chatto-api-v1-GetRoomNotificationPreferenceResponse"></a>

#### Result: GetRoomNotificationPreferenceResponse

Current notification preference for a room.

Clients can show both the stored room-level setting and the effective setting
the server will apply after defaults are resolved.

| Field | Type | Description |
| --- | --- | --- |
| `level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Explicit level stored for the current user and room. |
| `effective_level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Level after applying defaults and inheritance. |


<a id="chatto-api-v1-NotificationPreferencesService-SetRoomNotificationLevel"></a>

### SetRoomNotificationLevel

Sets the current user's explicit notification level for a room and returns
the resulting stored and effective levels.

```http
POST /api/connect/chatto.api.v1.NotificationPreferencesService/SetRoomNotificationLevel
```

<a id="chatto-api-v1-SetRoomNotificationLevelRequest"></a>

#### Input: SetRoomNotificationLevelRequest

Request to update the current user's notification level in one room.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room whose notification level should be changed for the current user. |
| `level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Required. New explicit notification level. Use NOTIFICATION_LEVEL_DEFAULT to return the room to inherited/default behavior. |


<a id="chatto-api-v1-SetRoomNotificationLevelResponse"></a>

#### Result: SetRoomNotificationLevelResponse

Updated notification preference for a room.

The response mirrors the read shape so clients can update local state without
issuing a second read request.

| Field | Type | Description |
| --- | --- | --- |
| `level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Explicit level stored for the current user and room. |
| `effective_level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Level after applying defaults and inheritance. |


<a id="chatto-api-v1-PresenceService"></a>

## PresenceService

Lets clients report the authenticated user's current live presence.

<a id="chatto-api-v1-PresenceService-ReportPresence"></a>

### ReportPresence

Reports the current user's live presence status. This state is transient:
clients should refresh it periodically while visible, and should stop
calling this RPC when the user chooses to appear offline.

```http
POST /api/connect/chatto.api.v1.PresenceService/ReportPresence
```

<a id="chatto-api-v1-ReportPresenceRequest"></a>

#### Input: ReportPresenceRequest

Request to report the current user's live presence status.

| Field | Type | Description |
| --- | --- | --- |
| `status` | [`PresenceStatus`](#chatto-api-v1-PresenceStatus) | Live status to report for the authenticated user. Offline is rejected. |
| `user_selected` | `bool` | True when this report comes from a deliberate user selection rather than automatic idle/refresh reporting. Automatic reports do not overwrite an active manually selected Away or Do Not Disturb status from another client. |


<a id="chatto-api-v1-ReportPresenceResponse"></a>

#### Result: ReportPresenceResponse

Result of reporting live presence.

| Field | Type | Description |
| --- | --- | --- |
| `status` | [`PresenceStatus`](#chatto-api-v1-PresenceStatus) | Reportable status accepted and stored by the server. |


<a id="chatto-api-v1-UserStatusService"></a>

## UserStatusService

Manages the current user's custom profile status.

<a id="chatto-api-v1-UserStatusService-SetCustomStatus"></a>

### SetCustomStatus

Sets or replaces the current user's custom status. Emoji and text are
required, and expires_at must be omitted or in the future.

```http
POST /api/connect/chatto.api.v1.UserStatusService/SetCustomStatus
```

<a id="chatto-api-v1-SetCustomStatusRequest"></a>

#### Input: SetCustomStatusRequest

Request to set or replace the current user's custom status.

| Field | Type | Description |
| --- | --- | --- |
| `emoji` | `string` | Short emoji marker displayed with the status. |
| `text` | `string` | User-written status text. |
| `expires_at` | `google.protobuf.Timestamp` | Optional future time after which clients should stop showing the status. |


<a id="chatto-api-v1-SetCustomStatusResponse"></a>

#### Result: SetCustomStatusResponse

Result of setting the current user's custom status.

| Field | Type | Description |
| --- | --- | --- |
| `status` | [`CustomUserStatus`](#chatto-api-v1-CustomUserStatus) | Stored custom status after validation and normalization. |


<a id="chatto-api-v1-UserStatusService-ClearCustomStatus"></a>

### ClearCustomStatus

Clears the current user's custom status. The call is idempotent and returns
the resulting empty status state.

```http
POST /api/connect/chatto.api.v1.UserStatusService/ClearCustomStatus
```

<a id="chatto-api-v1-ClearCustomStatusRequest"></a>

#### Input: ClearCustomStatusRequest

Request to clear the current user's custom status.


<a id="chatto-api-v1-ClearCustomStatusResponse"></a>

#### Result: ClearCustomStatusResponse

Result of clearing the current user's custom status.

| Field | Type | Description |
| --- | --- | --- |
| `status` | [`CustomUserStatus`](#chatto-api-v1-CustomUserStatus) | Current custom status after the operation. Usually absent after a clear. |


<a id="chatto-api-v1-ViewerService"></a>

## ViewerService

Provides authenticated user profile, preferences, and capability state.

<a id="chatto-api-v1-ViewerService-GetViewer"></a>

### GetViewer

Returns the current authenticated viewer. This RPC requires a logged-in
user; unauthenticated callers receive an UNAUTHENTICATED error.

```http
POST /api/connect/chatto.api.v1.ViewerService/GetViewer
```

<a id="chatto-api-v1-GetViewerRequest"></a>

#### Input: GetViewerRequest

Request for the authenticated viewer snapshot.


<a id="chatto-api-v1-GetViewerResponse"></a>

#### Result: GetViewerResponse

Authenticated viewer snapshot needed by application shells.

| Field | Type | Description |
| --- | --- | --- |
| `user` | [`ViewerUser`](#chatto-api-v1-ViewerUser) | Current authenticated user. |
| `capabilities` | [`ViewerCapabilities`](#chatto-api-v1-ViewerCapabilities) | Permission-derived capabilities. |
| `server_notification_preference` | [`ServerNotificationPreference`](#chatto-api-v1-ServerNotificationPreference) | Server-wide notification preference. |
| `room_notification_preferences` | repeated [`RoomNotificationPreference`](#chatto-api-v1-RoomNotificationPreference) | Notification preferences for rooms the user participates in. |


<a id="chatto-api-v1-AccountService"></a>

## AccountService

Self-service account, profile, avatar, and display preference commands.

<a id="chatto-api-v1-AccountService-UpdateProfile"></a>

### UpdateProfile

Updates the authenticated user's login and/or display name.

```http
POST /api/connect/chatto.api.v1.AccountService/UpdateProfile
```

<a id="chatto-api-v1-UpdateProfileRequest"></a>

#### Input: UpdateProfileRequest

Request to update the authenticated user's profile. At least one field must
be present.

| Field | Type | Description |
| --- | --- | --- |
| `display_name` | `optional string` | New display name, when changing it. |
| `login` | `optional string` | New login identifier, when changing it. |


<a id="chatto-api-v1-UpdateProfileResponse"></a>

#### Result: UpdateProfileResponse

Result of a profile update.

| Field | Type | Description |
| --- | --- | --- |
| `user` | [`AccountUser`](#chatto-api-v1-AccountUser) | Updated user profile. |


<a id="chatto-api-v1-AccountService-UploadAvatar"></a>

### UploadAvatar

Uploads and sets the authenticated user's avatar.

```http
POST /api/connect/chatto.api.v1.AccountService/UploadAvatar
```

<a id="chatto-api-v1-UploadAvatarRequest"></a>

#### Input: UploadAvatarRequest

Request to upload and set the authenticated user's avatar.

| Field | Type | Description |
| --- | --- | --- |
| `image` | `bytes` | Raw image bytes. The server validates, resizes, and stores a WebP avatar. |
| `filename` | `string` | Original browser filename, for diagnostics and future compatibility. |
| `content_type` | `string` | Browser-provided content type, for diagnostics and future compatibility. |


<a id="chatto-api-v1-UploadAvatarResponse"></a>

#### Result: UploadAvatarResponse

Result of uploading the authenticated user's avatar.

| Field | Type | Description |
| --- | --- | --- |
| `user` | [`AccountUser`](#chatto-api-v1-AccountUser) | Updated user profile. |


<a id="chatto-api-v1-AccountService-DeleteAvatar"></a>

### DeleteAvatar

Deletes the authenticated user's avatar. The call is idempotent.

```http
POST /api/connect/chatto.api.v1.AccountService/DeleteAvatar
```

<a id="chatto-api-v1-DeleteAvatarRequest"></a>

#### Input: DeleteAvatarRequest

Request to delete the authenticated user's avatar.


<a id="chatto-api-v1-DeleteAvatarResponse"></a>

#### Result: DeleteAvatarResponse

Result of deleting the authenticated user's avatar.

| Field | Type | Description |
| --- | --- | --- |
| `user` | [`AccountUser`](#chatto-api-v1-AccountUser) | Updated user profile. |


<a id="chatto-api-v1-AccountService-UpdateSettings"></a>

### UpdateSettings

Updates the authenticated user's display preferences.

```http
POST /api/connect/chatto.api.v1.AccountService/UpdateSettings
```

<a id="chatto-api-v1-UpdateSettingsRequest"></a>

#### Input: UpdateSettingsRequest

Request to update the authenticated user's display preferences. Omitted
fields are left unchanged. An empty timezone clears the explicit timezone.

| Field | Type | Description |
| --- | --- | --- |
| `timezone` | `optional string` | IANA timezone override. Empty clears the override. |
| `time_format` | optional [`TimeFormat`](#chatto-api-v1-TimeFormat) | Preferred time format. |


<a id="chatto-api-v1-UpdateSettingsResponse"></a>

#### Result: UpdateSettingsResponse

Result of updating display preferences.

| Field | Type | Description |
| --- | --- | --- |
| `settings` | [`UserSettings`](#chatto-api-v1-UserSettings) | Stored settings after merging the request. |


<a id="chatto-api-v1-AccountService-RequestAccountDeletion"></a>

### RequestAccountDeletion

Issues a short-lived token used to confirm self-deletion.

```http
POST /api/connect/chatto.api.v1.AccountService/RequestAccountDeletion
```

<a id="chatto-api-v1-RequestAccountDeletionRequest"></a>

#### Input: RequestAccountDeletionRequest

Request a short-lived confirmation token for deleting the authenticated
account.


<a id="chatto-api-v1-RequestAccountDeletionResponse"></a>

#### Result: RequestAccountDeletionResponse

Result of issuing an account deletion confirmation token.

| Field | Type | Description |
| --- | --- | --- |
| `confirmation_token` | `string` | Confirmation token. Clients must pass this to DeleteMyAccount. |


<a id="chatto-api-v1-AccountService-DeleteMyAccount"></a>

### DeleteMyAccount

Permanently deletes the authenticated account after token validation.

```http
POST /api/connect/chatto.api.v1.AccountService/DeleteMyAccount
```

<a id="chatto-api-v1-DeleteMyAccountRequest"></a>

#### Input: DeleteMyAccountRequest

Request to permanently delete the authenticated account.

| Field | Type | Description |
| --- | --- | --- |
| `confirmation_token` | `string` | Confirmation token obtained from RequestAccountDeletion. |


<a id="chatto-api-v1-DeleteMyAccountResponse"></a>

#### Result: DeleteMyAccountResponse

Result of deleting the authenticated account.

| Field | Type | Description |
| --- | --- | --- |
| `deleted` | `bool` | True when the account was deleted. |


<a id="chatto-api-v1-AdminDiagnosticsService"></a>

## AdminDiagnosticsService

Provides owner-only server diagnostics for the admin system page.

<a id="chatto-api-v1-AdminDiagnosticsService-GetSystemInfo"></a>

### GetSystemInfo

Returns broker, JetStream, and projection diagnostics.

```http
POST /api/connect/chatto.api.v1.AdminDiagnosticsService/GetSystemInfo
```

<a id="chatto-api-v1-GetSystemInfoRequest"></a>

#### Input: GetSystemInfoRequest

Request to read owner-only server diagnostics.


<a id="chatto-api-v1-GetSystemInfoResponse"></a>

#### Result: GetSystemInfoResponse

Owner-only operational diagnostics for the Chatto server.

| Field | Type | Description |
| --- | --- | --- |
| `system_info` | [`AdminSystemInfo`](#chatto-api-v1-AdminSystemInfo) | Broker connection, account, and JetStream state. |
| `projections` | repeated [`AdminProjectionState`](#chatto-api-v1-AdminProjectionState) | Event-sourced projection health and memory estimates. |


<a id="chatto-api-v1-AdminEventLogService"></a>

## AdminEventLogService

Provides server-admin audit/event-log diagnostics.

<a id="chatto-api-v1-AdminEventLogService-ListEvents"></a>

### ListEvents

Lists durable EVT entries newest-first.

```http
POST /api/connect/chatto.api.v1.AdminEventLogService/ListEvents
```

<a id="chatto-api-v1-ListEventsRequest"></a>

#### Input: ListEventsRequest

Request to list durable EVT entries newest-first.

| Field | Type | Description |
| --- | --- | --- |
| `limit` | `int32` | Maximum entries to return. The server clamps this to its diagnostic limit. |
| `before` | `optional string` | Exclusive sequence cursor. Returned entries are older than this sequence. |
| `filter` | [`AdminEventLogFilter`](#chatto-api-v1-AdminEventLogFilter) | Optional filter. Filtered scans inspect a bounded window per request. |


<a id="chatto-api-v1-ListEventsResponse"></a>

#### Result: ListEventsResponse

Newest-first page of durable EVT entries.

| Field | Type | Description |
| --- | --- | --- |
| `entries` | repeated [`AdminEventLogEntry`](#chatto-api-v1-AdminEventLogEntry) | Event-log rows. |
| `has_older` | `bool` | Whether older rows may be fetched with end_cursor. |
| `end_cursor` | `optional string` | Sequence cursor for loading older rows. |
| `total_count` | `int64` | Total EVT message count reported by JetStream. |
| `scanned_count` | `int32` | Number of stream sequences inspected while building this page. |
| `scan_limit` | `int32` | Maximum number of stream sequences inspected for this page. |
| `scan_limited` | `bool` | True when a filtered request reached scan_limit before finding a full page. |


<a id="chatto-api-v1-AdminEventLogService-ListEventTypes"></a>

### ListEventTypes

Lists filterable event type names.

```http
POST /api/connect/chatto.api.v1.AdminEventLogService/ListEventTypes
```

<a id="chatto-api-v1-ListEventTypesRequest"></a>

#### Input: ListEventTypesRequest

Request to list filterable event types.


<a id="chatto-api-v1-ListEventTypesResponse"></a>

#### Result: ListEventTypesResponse

Event types accepted by event-log filters.

| Field | Type | Description |
| --- | --- | --- |
| `event_types` | `repeated string` | Sorted event type names. |


<a id="chatto-api-v1-AdminEventLogService-GetEvent"></a>

### GetEvent

Reads one durable EVT entry by stream sequence.

```http
POST /api/connect/chatto.api.v1.AdminEventLogService/GetEvent
```

<a id="chatto-api-v1-GetEventRequest"></a>

#### Input: GetEventRequest

Request to read one durable EVT entry by stream sequence.

| Field | Type | Description |
| --- | --- | --- |
| `sequence` | `string` | Required EVT stream sequence. |


<a id="chatto-api-v1-GetEventResponse"></a>

#### Result: GetEventResponse

Result of reading one durable EVT entry.

| Field | Type | Description |
| --- | --- | --- |
| `entry` | optional [`AdminEventLogEntry`](#chatto-api-v1-AdminEventLogEntry) | Entry is absent when the requested sequence does not exist. |


<a id="chatto-api-v1-MemberDirectoryService"></a>

## MemberDirectoryService

Reads server and room member directories.

<a id="chatto-api-v1-MemberDirectoryService-ListServerMembers"></a>

### ListServerMembers

Lists authenticated server members. Every authenticated user is a server
member; admin-sensitive fields stay out of this public row shape.

```http
POST /api/connect/chatto.api.v1.MemberDirectoryService/ListServerMembers
```

<a id="chatto-api-v1-ListServerMembersRequest"></a>

#### Input: ListServerMembersRequest

Request for server members visible to the authenticated user.

| Field | Type | Description |
| --- | --- | --- |
| `search` | `string` | Optional case-insensitive search against login and display name. |
| `limit` | `int32` | Maximum results to return. Defaults to 20, maximum 100. |
| `offset` | `int32` | Zero-based result offset. |


<a id="chatto-api-v1-ListServerMembersResponse"></a>

#### Result: ListServerMembersResponse

Server member page.

| Field | Type | Description |
| --- | --- | --- |
| `members` | repeated [`DirectoryMember`](#chatto-api-v1-DirectoryMember) | Members in the requested page. |
| `total_count` | `int32` | Total matching members before pagination. |
| `has_more` | `bool` | Whether more matching members exist after this page. |


<a id="chatto-api-v1-MemberDirectoryService-ListRoomMembers"></a>

### ListRoomMembers

Lists explicit members of a room. The caller must be a member of the room.

```http
POST /api/connect/chatto.api.v1.MemberDirectoryService/ListRoomMembers
```

<a id="chatto-api-v1-ListRoomMembersRequest"></a>

#### Input: ListRoomMembersRequest

Request for members of one room.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room whose explicit members should be listed. |
| `search` | `string` | Optional case-insensitive search against login and display name. |
| `limit` | `int32` | Maximum results to return. Defaults to 20, maximum 100. |
| `offset` | `int32` | Zero-based result offset. |


<a id="chatto-api-v1-ListRoomMembersResponse"></a>

#### Result: ListRoomMembersResponse

Room member page.

| Field | Type | Description |
| --- | --- | --- |
| `members` | repeated [`DirectoryMember`](#chatto-api-v1-DirectoryMember) | Members in the requested page. |
| `total_count` | `int32` | Total matching members before pagination. |
| `has_more` | `bool` | Whether more matching members exist after this page. |


<a id="chatto-api-v1-RoomService"></a>

## RoomService

Manages channel room lifecycle and room membership for the current user.

<a id="chatto-api-v1-RoomService-CreateRoom"></a>

### CreateRoom

Creates a new channel room in a room group. The caller must be allowed to
create rooms in the target group.

```http
POST /api/connect/chatto.api.v1.RoomService/CreateRoom
```

<a id="chatto-api-v1-CreateRoomRequest"></a>

#### Input: CreateRoomRequest

Request to create a channel room.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Required. Name of the new channel room. |
| `description` | `string` | Optional room description. |
| `group_id` | `string` | Required. Room group that should contain the new channel. |
| `universal` | `bool` | Whether the new channel should grant effective membership to eligible server members. |


<a id="chatto-api-v1-CreateRoomResponse"></a>

#### Result: CreateRoomResponse

Result of creating a room.

| Field | Type | Description |
| --- | --- | --- |
| `room` | [`Room`](#chatto-api-v1-Room) | Created room. |


<a id="chatto-api-v1-RoomService-UpdateRoom"></a>

### UpdateRoom

Updates a room's name and description. The caller must be allowed to manage
rooms.

```http
POST /api/connect/chatto.api.v1.RoomService/UpdateRoom
```

<a id="chatto-api-v1-UpdateRoomRequest"></a>

#### Input: UpdateRoomRequest

Request to update a room's editable metadata.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room to update. |
| `name` | `string` | Required. New room name. |
| `description` | `string` | Optional new room description. |


<a id="chatto-api-v1-UpdateRoomResponse"></a>

#### Result: UpdateRoomResponse

Result of updating a room.

| Field | Type | Description |
| --- | --- | --- |
| `room` | [`Room`](#chatto-api-v1-Room) | Updated room. |


<a id="chatto-api-v1-RoomService-ArchiveRoom"></a>

### ArchiveRoom

Archives a room so it is hidden from active room lists. The caller must be
allowed to manage rooms.

```http
POST /api/connect/chatto.api.v1.RoomService/ArchiveRoom
```

<a id="chatto-api-v1-ArchiveRoomRequest"></a>

#### Input: ArchiveRoomRequest

Request to archive a room.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room to archive. |


<a id="chatto-api-v1-ArchiveRoomResponse"></a>

#### Result: ArchiveRoomResponse

Result of archiving a room.

| Field | Type | Description |
| --- | --- | --- |
| `room` | [`Room`](#chatto-api-v1-Room) | Archived room. |


<a id="chatto-api-v1-RoomService-UnarchiveRoom"></a>

### UnarchiveRoom

Restores an archived room to active room lists. The caller must be allowed
to manage rooms.

```http
POST /api/connect/chatto.api.v1.RoomService/UnarchiveRoom
```

<a id="chatto-api-v1-UnarchiveRoomRequest"></a>

#### Input: UnarchiveRoomRequest

Request to unarchive a room.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room to unarchive. |


<a id="chatto-api-v1-UnarchiveRoomResponse"></a>

#### Result: UnarchiveRoomResponse

Result of unarchiving a room.

| Field | Type | Description |
| --- | --- | --- |
| `room` | [`Room`](#chatto-api-v1-Room) | Unarchived room. |


<a id="chatto-api-v1-RoomService-SetRoomUniversal"></a>

### SetRoomUniversal

Changes whether a channel room grants effective membership to eligible
server members. Direct-message rooms cannot be universal.

```http
POST /api/connect/chatto.api.v1.RoomService/SetRoomUniversal
```

<a id="chatto-api-v1-SetRoomUniversalRequest"></a>

#### Input: SetRoomUniversalRequest

Request to change a channel room's universal membership flag.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Channel room to update. |
| `universal` | `bool` | New universal membership state. |


<a id="chatto-api-v1-SetRoomUniversalResponse"></a>

#### Result: SetRoomUniversalResponse

Result of changing a channel room's universal membership flag.

| Field | Type | Description |
| --- | --- | --- |
| `room` | [`Room`](#chatto-api-v1-Room) | Updated room. |


<a id="chatto-api-v1-RoomService-JoinRoom"></a>

### JoinRoom

Joins the room as the current user when room permissions allow it.

```http
POST /api/connect/chatto.api.v1.RoomService/JoinRoom
```

<a id="chatto-api-v1-JoinRoomRequest"></a>

#### Input: JoinRoomRequest

Request to join a room as the current user.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room to join. |


<a id="chatto-api-v1-JoinRoomResponse"></a>

#### Result: JoinRoomResponse

Result of joining a room.

| Field | Type | Description |
| --- | --- | --- |
| `room` | [`Room`](#chatto-api-v1-Room) | Joined room. |


<a id="chatto-api-v1-RoomService-StartDM"></a>

### StartDM

Starts or fetches a direct-message room for the current user and the
requested participant set. The caller must be allowed to start DMs.

```http
POST /api/connect/chatto.api.v1.RoomService/StartDM
```

<a id="chatto-api-v1-StartDMRequest"></a>

#### Input: StartDMRequest

Request to start or fetch a direct-message room.

| Field | Type | Description |
| --- | --- | --- |
| `participant_ids` | `repeated string` | Other participants to include in the direct-message room. The current user is always included by the server. An empty list creates or fetches the caller's self-DM. |


<a id="chatto-api-v1-StartDMResponse"></a>

#### Result: StartDMResponse

Result of starting or fetching a direct-message room.

| Field | Type | Description |
| --- | --- | --- |
| `room` | [`Room`](#chatto-api-v1-Room) | Direct-message room for the participant set. |


<a id="chatto-api-v1-RoomService-LeaveRoom"></a>

### LeaveRoom

Leaves the room as the current user. Direct-message and universal rooms
cannot be left.

```http
POST /api/connect/chatto.api.v1.RoomService/LeaveRoom
```

<a id="chatto-api-v1-LeaveRoomRequest"></a>

#### Input: LeaveRoomRequest

Request to leave a room as the current user.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room to leave. |


<a id="chatto-api-v1-LeaveRoomResponse"></a>

#### Result: LeaveRoomResponse

Result of leaving a room.

| Field | Type | Description |
| --- | --- | --- |
| `left` | `bool` | True when the current user is no longer an explicit member after the call. |


<a id="chatto-api-v1-RoomService-ListRoomBans"></a>

### ListRoomBans

Lists active channel room bans. The caller must be allowed to moderate room
membership bans.

```http
POST /api/connect/chatto.api.v1.RoomService/ListRoomBans
```

<a id="chatto-api-v1-ListRoomBansRequest"></a>

#### Input: ListRoomBansRequest

Request to list active room bans.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Optional channel room filter. Empty lists active bans across all rooms. |


<a id="chatto-api-v1-ListRoomBansResponse"></a>

#### Result: ListRoomBansResponse

Active room bans visible to the current moderator.

| Field | Type | Description |
| --- | --- | --- |
| `bans` | repeated [`RoomBan`](#chatto-api-v1-RoomBan) | Active bans, newest first. |


<a id="chatto-api-v1-RoomService-BanRoomMember"></a>

### BanRoomMember

Bans a member from a channel room. Direct-message rooms cannot be moderated
this way, and the target must currently be a room member.

```http
POST /api/connect/chatto.api.v1.RoomService/BanRoomMember
```

<a id="chatto-api-v1-BanRoomMemberRequest"></a>

#### Input: BanRoomMemberRequest

Request to ban a member from a channel room.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Channel room to ban the user from. |
| `user_id` | `string` | Required. User to ban from the room. |
| `reason` | `string` | Required moderator-entered reason stored for audit. |
| `expires_at` | `google.protobuf.Timestamp` | Optional future time when the ban expires. |


<a id="chatto-api-v1-BanRoomMemberResponse"></a>

#### Result: BanRoomMemberResponse

Result of banning a room member.

| Field | Type | Description |
| --- | --- | --- |
| `banned` | `bool` | True when the ban operation completed. |


<a id="chatto-api-v1-RoomService-UnbanRoomMember"></a>

### UnbanRoomMember

Removes an active channel room ban. Calling this when no active ban exists
is allowed and still returns success.

```http
POST /api/connect/chatto.api.v1.RoomService/UnbanRoomMember
```

<a id="chatto-api-v1-UnbanRoomMemberRequest"></a>

#### Input: UnbanRoomMemberRequest

Request to remove a channel room ban.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Channel room to unban the user from. |
| `user_id` | `string` | Required. User to unban. |
| `reason` | `string` | Required moderator-entered reason stored for audit. |


<a id="chatto-api-v1-UnbanRoomMemberResponse"></a>

#### Result: UnbanRoomMemberResponse

Result of removing a room ban.

| Field | Type | Description |
| --- | --- | --- |
| `unbanned` | `bool` | True when the unban operation completed. |


<a id="chatto-api-v1-RoomDirectoryService"></a>

## RoomDirectoryService

Reads room directory data and performs directory-adjacent commands.

<a id="chatto-api-v1-RoomDirectoryService-ListRooms"></a>

### ListRooms

Lists rooms visible to the current user. Channel rooms are non-archived
rooms visible through membership or room.list. DM rooms are membership-only
and empty DM conversations are omitted.

```http
POST /api/connect/chatto.api.v1.RoomDirectoryService/ListRooms
```

<a id="chatto-api-v1-ListRoomsRequest"></a>

#### Input: ListRoomsRequest

Request for rooms visible to the current user.

| Field | Type | Description |
| --- | --- | --- |
| `scope` | [`RoomDirectoryScope`](#chatto-api-v1-RoomDirectoryScope) | Which room kinds to include. Defaults to all. |


<a id="chatto-api-v1-ListRoomsResponse"></a>

#### Result: ListRoomsResponse

Visible rooms for the current user.

| Field | Type | Description |
| --- | --- | --- |
| `rooms` | repeated [`DirectoryRoom`](#chatto-api-v1-DirectoryRoom) | Rooms matching the requested scope. |


<a id="chatto-api-v1-RoomDirectoryService-ListRoomGroups"></a>

### ListRoomGroups

Lists ordered channel room groups and sidebar items visible to the current
user. Archived and hidden room entries are omitted from group results.

```http
POST /api/connect/chatto.api.v1.RoomDirectoryService/ListRoomGroups
```

<a id="chatto-api-v1-ListRoomGroupsRequest"></a>

#### Input: ListRoomGroupsRequest

Request for ordered room groups visible to the current user.


<a id="chatto-api-v1-ListRoomGroupsResponse"></a>

#### Result: ListRoomGroupsResponse

Ordered room groups visible to the current user.

| Field | Type | Description |
| --- | --- | --- |
| `groups` | repeated [`RoomGroup`](#chatto-api-v1-RoomGroup) | Channel room groups in sidebar order. |


<a id="chatto-api-v1-RoomDirectoryService-GetRoom"></a>

### GetRoom

Returns one visible room by ID. DM rooms require membership.

```http
POST /api/connect/chatto.api.v1.RoomDirectoryService/GetRoom
```

<a id="chatto-api-v1-GetRoomRequest"></a>

#### Input: GetRoomRequest

Request for one room visible to the current user.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room to resolve. |


<a id="chatto-api-v1-GetRoomResponse"></a>

#### Result: GetRoomResponse

Room visible to the current user.

| Field | Type | Description |
| --- | --- | --- |
| `room` | [`DirectoryRoom`](#chatto-api-v1-DirectoryRoom) | Resolved room and viewer state. |


<a id="chatto-api-v1-RoomDirectoryService-JoinGroup"></a>

### JoinGroup

Joins every unarchived room in a group that the current user can join.
Already-joined and non-joinable rooms are skipped.

```http
POST /api/connect/chatto.api.v1.RoomDirectoryService/JoinGroup
```

<a id="chatto-api-v1-JoinGroupRequest"></a>

#### Input: JoinGroupRequest

Request to join all joinable rooms in one room group.

| Field | Type | Description |
| --- | --- | --- |
| `group_id` | `string` | Required. Room group whose rooms should be joined. |


<a id="chatto-api-v1-JoinGroupResponse"></a>

#### Result: JoinGroupResponse

Result of joining all joinable rooms in one room group.

| Field | Type | Description |
| --- | --- | --- |
| `joined_room_ids` | `repeated string` | Room IDs that transitioned from not joined to joined. |


<a id="chatto-api-v1-AdminRoomLayoutService"></a>

## AdminRoomLayoutService

Reads and mutates the server-admin channel room layout.

<a id="chatto-api-v1-AdminRoomLayoutService-ListAdminRoomLayout"></a>

### ListAdminRoomLayout

Lists the current room groups, rooms, and sidebar links for the admin
layout editor.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/ListAdminRoomLayout
```

<a id="chatto-api-v1-ListAdminRoomLayoutRequest"></a>

#### Input: ListAdminRoomLayoutRequest

Request to read the admin room layout.


<a id="chatto-api-v1-ListAdminRoomLayoutResponse"></a>

#### Result: ListAdminRoomLayoutResponse

Current admin room layout.

| Field | Type | Description |
| --- | --- | --- |
| `groups` | repeated [`AdminRoomLayoutGroup`](#chatto-api-v1-AdminRoomLayoutGroup) | Room groups in sidebar order. |


<a id="chatto-api-v1-AdminRoomLayoutService-CreateRoomGroup"></a>

### CreateRoomGroup

Creates a room group. The caller must be allowed to manage roles.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/CreateRoomGroup
```

<a id="chatto-api-v1-CreateRoomGroupRequest"></a>

#### Input: CreateRoomGroupRequest

Request to create a room group.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Required group display name. |
| `description` | `string` | Optional operator-facing description. |


<a id="chatto-api-v1-CreateRoomGroupResponse"></a>

#### Result: CreateRoomGroupResponse

Result of creating a room group.

| Field | Type | Description |
| --- | --- | --- |
| `group` | [`AdminRoomLayoutGroup`](#chatto-api-v1-AdminRoomLayoutGroup) | Created room group. |


<a id="chatto-api-v1-AdminRoomLayoutService-UpdateRoomGroup"></a>

### UpdateRoomGroup

Updates a room group's metadata. The caller must be allowed to manage
roles.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/UpdateRoomGroup
```

<a id="chatto-api-v1-UpdateRoomGroupRequest"></a>

#### Input: UpdateRoomGroupRequest

Request to update a room group.

| Field | Type | Description |
| --- | --- | --- |
| `group_id` | `string` | Required room group ID. |
| `name` | `string` | Required group display name. |
| `description` | `string` | Optional operator-facing description. |


<a id="chatto-api-v1-UpdateRoomGroupResponse"></a>

#### Result: UpdateRoomGroupResponse

Result of updating a room group.

| Field | Type | Description |
| --- | --- | --- |
| `group` | [`AdminRoomLayoutGroup`](#chatto-api-v1-AdminRoomLayoutGroup) | Updated room group. |


<a id="chatto-api-v1-AdminRoomLayoutService-DeleteRoomGroup"></a>

### DeleteRoomGroup

Deletes an empty room group. The caller must be allowed to manage roles.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/DeleteRoomGroup
```

<a id="chatto-api-v1-DeleteRoomGroupRequest"></a>

#### Input: DeleteRoomGroupRequest

Request to delete an empty room group.

| Field | Type | Description |
| --- | --- | --- |
| `group_id` | `string` | Required room group ID. |


<a id="chatto-api-v1-DeleteRoomGroupResponse"></a>

#### Result: DeleteRoomGroupResponse

Result of deleting a room group.

| Field | Type | Description |
| --- | --- | --- |
| `deleted` | `bool` | True when the room group was deleted. |


<a id="chatto-api-v1-AdminRoomLayoutService-ReorderRoomGroups"></a>

### ReorderRoomGroups

Replaces the global room group order. The caller must be allowed to manage
roles.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/ReorderRoomGroups
```

<a id="chatto-api-v1-ReorderRoomGroupsRequest"></a>

#### Input: ReorderRoomGroupsRequest

Request to replace the global room group order.

| Field | Type | Description |
| --- | --- | --- |
| `ordered_group_ids` | `repeated string` | Ordered room group IDs. The list must include every existing group exactly once. |


<a id="chatto-api-v1-ReorderRoomGroupsResponse"></a>

#### Result: ReorderRoomGroupsResponse

Result of replacing the global room group order.

| Field | Type | Description |
| --- | --- | --- |
| `groups` | repeated [`AdminRoomLayoutGroup`](#chatto-api-v1-AdminRoomLayoutGroup) | Room groups in their new order. |


<a id="chatto-api-v1-AdminRoomLayoutService-MoveRoomToGroup"></a>

### MoveRoomToGroup

Moves a channel room to another room group. The caller must be allowed to
manage rooms in both source and destination groups.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/MoveRoomToGroup
```

<a id="chatto-api-v1-MoveRoomToGroupRequest"></a>

#### Input: MoveRoomToGroupRequest

Request to move a channel room to another room group.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required channel room ID. |
| `group_id` | `string` | Required destination room group ID. |


<a id="chatto-api-v1-MoveRoomToGroupResponse"></a>

#### Result: MoveRoomToGroupResponse

Result of moving a channel room.

| Field | Type | Description |
| --- | --- | --- |
| `room` | [`Room`](#chatto-api-v1-Room) | Moved room after the operation. |


<a id="chatto-api-v1-AdminRoomLayoutService-ReorderSidebarItemsInGroup"></a>

### ReorderSidebarItemsInGroup

Replaces the mixed room/link sidebar order in one group. The caller must
be allowed to manage rooms in that group.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/ReorderSidebarItemsInGroup
```

<a id="chatto-api-v1-ReorderSidebarItemsInGroupRequest"></a>

#### Input: ReorderSidebarItemsInGroupRequest

Request to replace the mixed sidebar item order in one group.

| Field | Type | Description |
| --- | --- | --- |
| `group_id` | `string` | Required room group ID. |
| `items` | repeated [`AdminRoomLayoutItemInput`](#chatto-api-v1-AdminRoomLayoutItemInput) | Ordered room/sidebar-link item references. |


<a id="chatto-api-v1-ReorderSidebarItemsInGroupResponse"></a>

#### Result: ReorderSidebarItemsInGroupResponse

Result of replacing the mixed sidebar item order.

| Field | Type | Description |
| --- | --- | --- |
| `group` | [`AdminRoomLayoutGroup`](#chatto-api-v1-AdminRoomLayoutGroup) | Updated room group. |


<a id="chatto-api-v1-AdminRoomLayoutService-CreateSidebarLink"></a>

### CreateSidebarLink

Creates a sidebar link. The caller must be allowed to manage rooms in the
destination group.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/CreateSidebarLink
```

<a id="chatto-api-v1-CreateSidebarLinkRequest"></a>

#### Input: CreateSidebarLinkRequest

Request to create a sidebar link in one room group.

| Field | Type | Description |
| --- | --- | --- |
| `group_id` | `string` | Required destination room group ID. |
| `label` | `string` | Required display label. |
| `url` | `string` | Required absolute http(s) URL or server-local path. |


<a id="chatto-api-v1-CreateSidebarLinkResponse"></a>

#### Result: CreateSidebarLinkResponse

Result of creating a sidebar link.

| Field | Type | Description |
| --- | --- | --- |
| `sidebar_link` | [`SidebarLink`](#chatto-api-v1-SidebarLink) | Created sidebar link. |


<a id="chatto-api-v1-AdminRoomLayoutService-UpdateSidebarLink"></a>

### UpdateSidebarLink

Updates a sidebar link. The caller must be allowed to manage rooms in the
link's current group.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/UpdateSidebarLink
```

<a id="chatto-api-v1-UpdateSidebarLinkRequest"></a>

#### Input: UpdateSidebarLinkRequest

Request to update a sidebar link.

| Field | Type | Description |
| --- | --- | --- |
| `link_id` | `string` | Required sidebar link ID. |
| `label` | `string` | Required display label. |
| `url` | `string` | Required absolute http(s) URL or server-local path. |


<a id="chatto-api-v1-UpdateSidebarLinkResponse"></a>

#### Result: UpdateSidebarLinkResponse

Result of updating a sidebar link.

| Field | Type | Description |
| --- | --- | --- |
| `sidebar_link` | [`SidebarLink`](#chatto-api-v1-SidebarLink) | Updated sidebar link. |


<a id="chatto-api-v1-AdminRoomLayoutService-DeleteSidebarLink"></a>

### DeleteSidebarLink

Deletes a sidebar link. The caller must be allowed to manage rooms in the
link's current group.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/DeleteSidebarLink
```

<a id="chatto-api-v1-DeleteSidebarLinkRequest"></a>

#### Input: DeleteSidebarLinkRequest

Request to delete a sidebar link.

| Field | Type | Description |
| --- | --- | --- |
| `link_id` | `string` | Required sidebar link ID. |


<a id="chatto-api-v1-DeleteSidebarLinkResponse"></a>

#### Result: DeleteSidebarLinkResponse

Result of deleting a sidebar link.

| Field | Type | Description |
| --- | --- | --- |
| `deleted` | `bool` | True when the sidebar link was deleted. |


<a id="chatto-api-v1-AdminRoomLayoutService-MoveSidebarLinkToGroup"></a>

### MoveSidebarLinkToGroup

Moves a sidebar link to another group. The caller must be allowed to manage
rooms in both source and destination groups.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/MoveSidebarLinkToGroup
```

<a id="chatto-api-v1-MoveSidebarLinkToGroupRequest"></a>

#### Input: MoveSidebarLinkToGroupRequest

Request to move a sidebar link to another room group.

| Field | Type | Description |
| --- | --- | --- |
| `link_id` | `string` | Required sidebar link ID. |
| `group_id` | `string` | Required destination room group ID. |


<a id="chatto-api-v1-MoveSidebarLinkToGroupResponse"></a>

#### Result: MoveSidebarLinkToGroupResponse

Result of moving a sidebar link.

| Field | Type | Description |
| --- | --- | --- |
| `sidebar_link` | [`SidebarLink`](#chatto-api-v1-SidebarLink) | Moved sidebar link. |


<a id="chatto-api-v1-AdminUserManagementService"></a>

## AdminUserManagementService

Server-admin user identity and cooldown commands.

<a id="chatto-api-v1-AdminUserManagementService-ListMembers"></a>

### ListMembers

Lists server members for the admin members screen.

```http
POST /api/connect/chatto.api.v1.AdminUserManagementService/ListMembers
```

<a id="chatto-api-v1-ListMembersRequest"></a>

#### Input: ListMembersRequest

Request server-admin member rows.

| Field | Type | Description |
| --- | --- | --- |
| `search` | `optional string` | Optional case-insensitive login/display-name search term. |
| `limit` | `int32` | Maximum rows to return. Defaults to 20 and is capped at 100. |
| `offset` | `int32` | Number of matching rows to skip. |


<a id="chatto-api-v1-ListMembersResponse"></a>

#### Result: ListMembersResponse

Server-admin member rows plus role summaries.

| Field | Type | Description |
| --- | --- | --- |
| `users` | repeated [`AdminMember`](#chatto-api-v1-AdminMember) | Matching users. |
| `roles` | repeated [`AdminMemberRoleSummary`](#chatto-api-v1-AdminMemberRoleSummary) | Server roles for display-name lookup. |
| `total_count` | `int32` | Total matching user count before pagination. |
| `has_more` | `bool` | Whether more matching rows exist after this page. |


<a id="chatto-api-v1-AdminUserManagementService-GetMember"></a>

### GetMember

Gets one server member plus role/permission metadata for admin details.

```http
POST /api/connect/chatto.api.v1.AdminUserManagementService/GetMember
```

<a id="chatto-api-v1-GetMemberRequest"></a>

#### Input: GetMemberRequest

Request one server-admin member detail record.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Target user ID. |


<a id="chatto-api-v1-GetMemberResponse"></a>

#### Result: GetMemberResponse

Server-admin member detail payload.

| Field | Type | Description |
| --- | --- | --- |
| `member` | [`AdminMember`](#chatto-api-v1-AdminMember) | Member record, when found. |
| `roles` | repeated [`AdminMemberRole`](#chatto-api-v1-AdminMemberRole) | Server roles for assignment UI. |
| `available_permissions` | `repeated string` | Server permissions available for per-user overrides. |
| `viewer_can_assign_roles` | `bool` | Whether the caller may assign/revoke roles. |
| `viewer_can_manage_roles` | `bool` | Whether the caller may edit role definitions. |
| `viewer_can_manage_user_permissions` | `bool` | Whether the caller may edit direct user permissions. |


<a id="chatto-api-v1-AdminUserManagementService-AssignRole"></a>

### AssignRole

Assigns a server role to a user.

```http
POST /api/connect/chatto.api.v1.AdminUserManagementService/AssignRole
```

<a id="chatto-api-v1-AssignRoleRequest"></a>

#### Input: AssignRoleRequest

Request to assign a server role to a user.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Target user ID. |
| `role_name` | `string` | Role name to assign. |


<a id="chatto-api-v1-AssignRoleResponse"></a>

#### Result: AssignRoleResponse

Result of assigning a server role.

| Field | Type | Description |
| --- | --- | --- |
| `assigned` | `bool` | True when the request completed. |


<a id="chatto-api-v1-AdminUserManagementService-RevokeRole"></a>

### RevokeRole

Revokes a server role from a user.

```http
POST /api/connect/chatto.api.v1.AdminUserManagementService/RevokeRole
```

<a id="chatto-api-v1-RevokeRoleRequest"></a>

#### Input: RevokeRoleRequest

Request to revoke a server role from a user.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Target user ID. |
| `role_name` | `string` | Role name to revoke. |


<a id="chatto-api-v1-RevokeRoleResponse"></a>

#### Result: RevokeRoleResponse

Result of revoking a server role.

| Field | Type | Description |
| --- | --- | --- |
| `revoked` | `bool` | True when the request completed. |


<a id="chatto-api-v1-AdminUserManagementService-UpdateUser"></a>

### UpdateUser

Updates a user's login and/or display name as an admin action.

```http
POST /api/connect/chatto.api.v1.AdminUserManagementService/UpdateUser
```

<a id="chatto-api-v1-UpdateUserRequest"></a>

#### Input: UpdateUserRequest

Request to update a user's identity as a server-admin action.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Target user ID. |
| `display_name` | `optional string` | New display name, when changing it. |
| `login` | `optional string` | New login identifier, when changing it. |


<a id="chatto-api-v1-UpdateUserResponse"></a>

#### Result: UpdateUserResponse

Result of an admin user update.

| Field | Type | Description |
| --- | --- | --- |
| `user` | [`AccountUser`](#chatto-api-v1-AccountUser) | Updated user profile. |


<a id="chatto-api-v1-AdminUserManagementService-ClearUsernameCooldown"></a>

### ClearUsernameCooldown

Clears the target user's self-service username-change cooldown.

```http
POST /api/connect/chatto.api.v1.AdminUserManagementService/ClearUsernameCooldown
```

<a id="chatto-api-v1-ClearUsernameCooldownRequest"></a>

#### Input: ClearUsernameCooldownRequest

Request to clear a user's self-service username-change cooldown.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Target user ID. |


<a id="chatto-api-v1-ClearUsernameCooldownResponse"></a>

#### Result: ClearUsernameCooldownResponse

Result of clearing a user's username-change cooldown.

| Field | Type | Description |
| --- | --- | --- |
| `cleared` | `bool` | True when the request completed. |


<a id="chatto-api-v1-RoomTimelineService"></a>

## RoomTimelineService

Reads room and thread timeline pages.

<a id="chatto-api-v1-RoomTimelineService-GetRoomEvents"></a>

### GetRoomEvents

Returns one page of room timeline events, including related user data needed
to render the page.

```http
POST /api/connect/chatto.api.v1.RoomTimelineService/GetRoomEvents
```

<a id="chatto-api-v1-GetRoomEventsRequest"></a>

#### Input: GetRoomEventsRequest

Request for a page of room timeline events.

Omit the cursor to load the initial page. Set before to a previously returned
start_cursor to page toward older events, or after to a previously returned
end_cursor to page toward newer events.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room whose timeline should be loaded. |
| `limit` | `int32` | Maximum number of events to return. The server may clamp very large limits. |
| `cursor.before` | `string` | Return events older than this opaque cursor. |
| `cursor.after` | `string` | Return events newer than this opaque cursor. |


<a id="chatto-api-v1-GetRoomEventsResponse"></a>

#### Result: GetRoomEventsResponse

Response containing one room timeline page.

| Field | Type | Description |
| --- | --- | --- |
| `page` | [`RoomTimelinePage`](#chatto-api-v1-RoomTimelinePage) | Loaded timeline page. |


<a id="chatto-api-v1-RoomTimelineService-GetRoomEventsAround"></a>

### GetRoomEventsAround

Returns a room timeline window centered around a specific event. Use this to
open a permalink, search result, or notification target in context.

```http
POST /api/connect/chatto.api.v1.RoomTimelineService/GetRoomEventsAround
```

<a id="chatto-api-v1-GetRoomEventsAroundRequest"></a>

#### Input: GetRoomEventsAroundRequest

Request for room timeline events around a specific event.

Use this when a client needs to jump to a known event and render enough
context around it.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room whose timeline should be loaded. |
| `event_id` | `string` | Required. Anchor event ID that should appear in the returned page. |
| `limit` | `int32` | Maximum number of events to return around the anchor. |


<a id="chatto-api-v1-GetRoomEventsAroundResponse"></a>

#### Result: GetRoomEventsAroundResponse

Response containing a room timeline window around a specific event.

| Field | Type | Description |
| --- | --- | --- |
| `page` | [`RoomTimelinePage`](#chatto-api-v1-RoomTimelinePage) | Loaded timeline page. |
| `target_index` | `int32` | Zero-based index of the anchor event within page.events. |


<a id="chatto-api-v1-RoomTimelineService-ResolveMessageLinkTarget"></a>

### ResolveMessageLinkTarget

Resolves a permalink target to either the room timeline or a message
thread, including thread-only replies that are not room timeline rows.

```http
POST /api/connect/chatto.api.v1.RoomTimelineService/ResolveMessageLinkTarget
```

<a id="chatto-api-v1-ResolveMessageLinkTargetRequest"></a>

#### Input: ResolveMessageLinkTargetRequest

Request for resolving a message permalink target.

Unlike GetRoomEventsAround, this can resolve thread-only replies that do not
appear as rows in the room timeline.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room containing the linked event. |
| `event_id` | `string` | Required. Linked event ID. |


<a id="chatto-api-v1-ResolveMessageLinkTargetResponse"></a>

#### Result: ResolveMessageLinkTargetResponse

Response describing where a message permalink should land.

| Field | Type | Description |
| --- | --- | --- |
| `event` | [`RoomTimelineEvent`](#chatto-api-v1-RoomTimelineEvent) | Linked event, hydrated with the same shape used by timeline pages. |
| `thread_root_event_id` | `string` | Event ID of the thread root when event is a thread-only reply. Empty when the event should open in the room timeline. |
| `includes` | [`RoomTimelineIncludes`](#chatto-api-v1-RoomTimelineIncludes) | Related entities needed to render the event. |


<a id="chatto-api-v1-RoomTimelineService-GetThreadEvents"></a>

### GetThreadEvents

Returns one page of events in a message thread. Initial pages include the
thread root message; cursor pages return replies in the requested direction.

```http
POST /api/connect/chatto.api.v1.RoomTimelineService/GetThreadEvents
```

<a id="chatto-api-v1-GetThreadEventsRequest"></a>

#### Input: GetThreadEventsRequest

Request for a page of events in one thread.

Omit the cursor to load the latest visible part of the thread, including the
root message. Set before to a previously returned start_cursor to page toward
older replies, or after to a previously returned end_cursor to page toward
newer replies without repeating the root message.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room containing the thread. |
| `thread_root_event_id` | `string` | Required. Event ID of the root message for the thread. |
| `limit` | `int32` | Maximum number of events to return. The server may clamp very large limits. |
| `cursor.before` | `string` | Return thread events older than this opaque cursor. |
| `cursor.after` | `string` | Return thread events newer than this opaque cursor. |


<a id="chatto-api-v1-GetThreadEventsResponse"></a>

#### Result: GetThreadEventsResponse

Response containing one thread timeline page.

| Field | Type | Description |
| --- | --- | --- |
| `page` | [`RoomTimelinePage`](#chatto-api-v1-RoomTimelinePage) | Loaded timeline page. |


<a id="chatto-api-v1-RoomTimelineService-GetThreadEventsAround"></a>

### GetThreadEventsAround

Returns a thread timeline window centered around a specific event. Use this
to open a reply from a notification or search result in context.

```http
POST /api/connect/chatto.api.v1.RoomTimelineService/GetThreadEventsAround
```

<a id="chatto-api-v1-GetThreadEventsAroundRequest"></a>

#### Input: GetThreadEventsAroundRequest

Request for thread events around a specific event.

Use this when a client needs to jump to a known reply and render surrounding
thread context.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room containing the thread. |
| `thread_root_event_id` | `string` | Required. Event ID of the root message for the thread. |
| `event_id` | `string` | Required. Anchor event ID inside the thread. The event should belong to the requested thread. |
| `limit` | `int32` | Maximum number of events to return around the anchor. |


<a id="chatto-api-v1-GetThreadEventsAroundResponse"></a>

#### Result: GetThreadEventsAroundResponse

Response containing a thread timeline window around a specific event.

| Field | Type | Description |
| --- | --- | --- |
| `page` | [`RoomTimelinePage`](#chatto-api-v1-RoomTimelinePage) | Loaded timeline page. |
| `target_index` | `int32` | Zero-based index of the anchor event within page.events. |


<a id="chatto-api-v1-AttachmentService"></a>

## AttachmentService

Reads room attachments and refreshes expiring signed attachment URLs.

<a id="chatto-api-v1-AttachmentService-ListRoomAttachments"></a>

### ListRoomAttachments

Lists current message-owned room attachments. Authentication and room
membership are required.

```http
POST /api/connect/chatto.api.v1.AttachmentService/ListRoomAttachments
```

<a id="chatto-api-v1-ListRoomAttachmentsRequest"></a>

#### Input: ListRoomAttachmentsRequest

Request for room-scoped attachment list pages.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required room ID. |
| `limit` | `int32` | Maximum number of attachments to return. |
| `offset` | `int32` | Zero-based offset into the newest-first attachment list. |
| `thumbnail` | [`AttachmentThumbnailOptions`](#chatto-api-v1-AttachmentThumbnailOptions) | Thumbnail URL options. Defaults are applied when absent. |


<a id="chatto-api-v1-ListRoomAttachmentsResponse"></a>

#### Result: ListRoomAttachmentsResponse

Room-scoped attachment list response.

| Field | Type | Description |
| --- | --- | --- |
| `items` | repeated [`RoomAttachmentListItem`](#chatto-api-v1-RoomAttachmentListItem) | Current attachments in newest message order. |
| `total_count` | `int32` | Total current attachment count for the room. |
| `has_more` | `bool` | True when a later page exists. |


<a id="chatto-api-v1-AttachmentService-RefreshMessageAttachmentUrls"></a>

### RefreshMessageAttachmentUrls

Refreshes signed URLs for the current attachments on one message.
Authentication and room membership are required.

```http
POST /api/connect/chatto.api.v1.AttachmentService/RefreshMessageAttachmentUrls
```

<a id="chatto-api-v1-RefreshMessageAttachmentUrlsRequest"></a>

#### Input: RefreshMessageAttachmentUrlsRequest

Request to refresh signed attachment URLs for one message.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required room ID. |
| `event_id` | `string` | Required message event ID. |
| `thumbnail` | [`AttachmentThumbnailOptions`](#chatto-api-v1-AttachmentThumbnailOptions) | Thumbnail URL options. Defaults are applied when absent. |


<a id="chatto-api-v1-RefreshMessageAttachmentUrlsResponse"></a>

#### Result: RefreshMessageAttachmentUrlsResponse

Response containing fresh signed URLs for the current message attachments.

| Field | Type | Description |
| --- | --- | --- |
| `attachments` | repeated [`RefreshedAttachmentUrls`](#chatto-api-v1-RefreshedAttachmentUrls) | Fresh URL bundle for each current attachment on the message. |


<a id="chatto-api-v1-LinkPreviewService"></a>

## LinkPreviewService

Fetches link preview metadata for message composers.

<a id="chatto-api-v1-LinkPreviewService-FetchLinkPreview"></a>

### FetchLinkPreview

Fetches and caches metadata for a URL. Authentication is required to avoid
exposing the preview fetcher as an unauthenticated network proxy.

```http
POST /api/connect/chatto.api.v1.LinkPreviewService/FetchLinkPreview
```

<a id="chatto-api-v1-FetchLinkPreviewRequest"></a>

#### Input: FetchLinkPreviewRequest

Request to fetch server-side link preview metadata for a URL.

| Field | Type | Description |
| --- | --- | --- |
| `url` | `string` | URL to preview. |


<a id="chatto-api-v1-FetchLinkPreviewResponse"></a>

#### Result: FetchLinkPreviewResponse

Result of fetching link preview metadata.

| Field | Type | Description |
| --- | --- | --- |
| `preview` | [`FetchedLinkPreview`](#chatto-api-v1-FetchedLinkPreview) | Preview metadata, or absent when the URL cannot be previewed. |


<a id="chatto-api-v1-MessageService"></a>

## MessageService

Posts messages into room and thread timelines.

<a id="chatto-api-v1-MessageService-PostMessage"></a>

### PostMessage

Posts a message for the current user. The user must be a room member and
must have message.post for room messages or message.post-in-thread for
thread replies. Echoing a thread reply also requires message.echo and
message.post.

```http
POST /api/connect/chatto.api.v1.MessageService/PostMessage
```

<a id="chatto-api-v1-PostMessageRequest"></a>

#### Input: PostMessageRequest

Request to post a message in a room or thread.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room where the message should be posted. |
| `body` | `string` | Message body text. Required unless attachments or attachment_asset_ids is non-empty. |
| `attachment_asset_ids` | `repeated string` | Existing room-scoped attachment asset IDs to include with the message. |
| `thread_root_event_id` | `string` | Event ID of the thread root message when posting a thread reply. |
| `in_reply_to` | `string` | Event ID this message replies to for attribution. |
| `also_send_to_channel` | `bool` | True to also echo a thread reply into the main room timeline. |
| `mention_confirmation_token` | `string` | Short-lived token returned by a prior mention confirmation challenge. |
| `link_preview` | [`MessageLinkPreviewInput`](#chatto-api-v1-MessageLinkPreviewInput) | Link preview selected by the client. |
| `attachments` | repeated [`MessageAttachmentUpload`](#chatto-api-v1-MessageAttachmentUpload) | Browser-uploaded attachments to include with the message. |


<a id="chatto-api-v1-PostMessageResponse"></a>

#### Result: PostMessageResponse

Result of posting a message.

| Field | Type | Description |
| --- | --- | --- |
| `result.event` | [`RoomTimelineEvent`](#chatto-api-v1-RoomTimelineEvent) | Renderable timeline event for the posted message. |
| `result.mention_confirmation` | [`MentionConfirmationChallenge`](#chatto-api-v1-MentionConfirmationChallenge) | Challenge shown when a message would notify many people. |
| `includes` | [`RoomTimelineIncludes`](#chatto-api-v1-RoomTimelineIncludes) | Related entities needed to render event when a message was posted. |


<a id="chatto-api-v1-MessageService-UpdateMessage"></a>

### UpdateMessage

Edits a message body. Authors can edit their own messages within the edit
window. Non-authors need message.manage and cannot change channel echo
state.

```http
POST /api/connect/chatto.api.v1.MessageService/UpdateMessage
```

<a id="chatto-api-v1-UpdateMessageRequest"></a>

#### Input: UpdateMessageRequest

Request to edit a message body.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room containing the message. |
| `event_id` | `string` | Required. Event ID of the message to edit. |
| `body` | `string` | New message body text. |
| `also_send_to_channel` | `optional bool` | For thread replies, whether a channel echo should exist after saving. Omit to preserve the current echo state. |


<a id="chatto-api-v1-UpdateMessageResponse"></a>

#### Result: UpdateMessageResponse

Result of editing a message.

| Field | Type | Description |
| --- | --- | --- |
| `updated` | `bool` | True when the edit was accepted. |


<a id="chatto-api-v1-MessageService-DeleteMessage"></a>

### DeleteMessage

Retracts a message. Authors can delete their own messages. Non-authors need
message.manage.

```http
POST /api/connect/chatto.api.v1.MessageService/DeleteMessage
```

<a id="chatto-api-v1-DeleteMessageRequest"></a>

#### Input: DeleteMessageRequest

Request to retract a message.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room containing the message. |
| `event_id` | `string` | Required. Event ID of the message to retract. |


<a id="chatto-api-v1-DeleteMessageResponse"></a>

#### Result: DeleteMessageResponse

Result of retracting a message.

| Field | Type | Description |
| --- | --- | --- |
| `deleted` | `bool` | True when the delete/retract request was accepted. |


<a id="chatto-api-v1-MessageService-DeleteAttachment"></a>

### DeleteAttachment

Removes one attachment from the author's own message.

```http
POST /api/connect/chatto.api.v1.MessageService/DeleteAttachment
```

<a id="chatto-api-v1-DeleteAttachmentRequest"></a>

#### Input: DeleteAttachmentRequest

Request to remove one attachment from a message.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room containing the message. |
| `event_id` | `string` | Required. Event ID of the message containing the attachment. |
| `attachment_id` | `string` | Required. Attachment ID to remove from the message. |


<a id="chatto-api-v1-DeleteAttachmentResponse"></a>

#### Result: DeleteAttachmentResponse

Result of removing one attachment from a message.

| Field | Type | Description |
| --- | --- | --- |
| `deleted` | `bool` | True when the attachment removal was accepted. |


<a id="chatto-api-v1-MessageService-DeleteLinkPreview"></a>

### DeleteLinkPreview

Removes the accepted link preview from the author's own message.

```http
POST /api/connect/chatto.api.v1.MessageService/DeleteLinkPreview
```

<a id="chatto-api-v1-DeleteLinkPreviewRequest"></a>

#### Input: DeleteLinkPreviewRequest

Request to remove the accepted link preview from a message.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room containing the message. |
| `event_id` | `string` | Required. Event ID of the message containing the link preview. |
| `url` | `string` | Required. URL of the link preview to remove. |


<a id="chatto-api-v1-DeleteLinkPreviewResponse"></a>

#### Result: DeleteLinkPreviewResponse

Result of removing a link preview from a message.

| Field | Type | Description |
| --- | --- | --- |
| `deleted` | `bool` | True when the link preview removal was accepted. |


<a id="chatto-api-v1-MessageService-SendTypingIndicator"></a>

### SendTypingIndicator

Publishes a live-only typing indicator. Room membership is required; message
posting permission is not.

```http
POST /api/connect/chatto.api.v1.MessageService/SendTypingIndicator
```

<a id="chatto-api-v1-SendTypingIndicatorRequest"></a>

#### Input: SendTypingIndicatorRequest

Request to publish a live-only typing indicator.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room where the current user is typing. |
| `thread_root_event_id` | `string` | Event ID of the thread root when typing inside a thread. |


<a id="chatto-api-v1-SendTypingIndicatorResponse"></a>

#### Result: SendTypingIndicatorResponse

Result of publishing a typing indicator.

| Field | Type | Description |
| --- | --- | --- |
| `sent` | `bool` | True when the typing indicator was accepted for publish. |


<a id="chatto-api-v1-NotificationService"></a>

## NotificationService

Reads and dismisses pending notifications for the authenticated viewer.

<a id="chatto-api-v1-NotificationService-ListNotifications"></a>

### ListNotifications

Lists the authenticated viewer's pending notifications.

```http
POST /api/connect/chatto.api.v1.NotificationService/ListNotifications
```

<a id="chatto-api-v1-ListNotificationsRequest"></a>

#### Input: ListNotificationsRequest

Request for the authenticated viewer's pending notifications.

| Field | Type | Description |
| --- | --- | --- |
| `limit` | `int32` | Maximum number of notifications to return. |
| `offset` | `int32` | Number of notifications to skip. |


<a id="chatto-api-v1-ListNotificationsResponse"></a>

#### Result: ListNotificationsResponse

Pending notification page.

| Field | Type | Description |
| --- | --- | --- |
| `items` | repeated [`NotificationItem`](#chatto-api-v1-NotificationItem) | Page items, newest first. |
| `total_count` | `int32` | Total count before pagination. |
| `has_more` | `bool` | True when more notifications exist beyond this page. |
| `server_name` | `string` | Current server display name for non-DM location labels. |


<a id="chatto-api-v1-NotificationService-ListRoomNotifications"></a>

### ListRoomNotifications

Lists pending notifications for one room. Non-members receive an empty page.

```http
POST /api/connect/chatto.api.v1.NotificationService/ListRoomNotifications
```

<a id="chatto-api-v1-ListRoomNotificationsRequest"></a>

#### Input: ListRoomNotificationsRequest

Request for pending notifications scoped to one room.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room whose notifications should be listed. |
| `limit` | `int32` | Maximum number of notifications to return. |
| `offset` | `int32` | Number of notifications to skip. |


<a id="chatto-api-v1-ListRoomNotificationsResponse"></a>

#### Result: ListRoomNotificationsResponse

Pending notification page scoped to one room.

| Field | Type | Description |
| --- | --- | --- |
| `items` | repeated [`NotificationItem`](#chatto-api-v1-NotificationItem) | Page items, newest first. |
| `total_count` | `int32` | Total count before pagination. |
| `has_more` | `bool` | True when more notifications exist beyond this page. |
| `server_name` | `string` | Current server display name for non-DM location labels. |


<a id="chatto-api-v1-NotificationService-ListNotificationCounts"></a>

### ListNotificationCounts

Lists pending notification counts grouped by room.

```http
POST /api/connect/chatto.api.v1.NotificationService/ListNotificationCounts
```

<a id="chatto-api-v1-ListNotificationCountsRequest"></a>

#### Input: ListNotificationCountsRequest

Request for pending notification counts grouped by room.


<a id="chatto-api-v1-ListNotificationCountsResponse"></a>

#### Result: ListNotificationCountsResponse

Pending notification counts grouped by room.

| Field | Type | Description |
| --- | --- | --- |
| `room_counts` | repeated [`RoomNotificationCount`](#chatto-api-v1-RoomNotificationCount) | Counts for rooms with at least one pending notification. |


<a id="chatto-api-v1-NotificationService-HasNotifications"></a>

### HasNotifications

Checks whether the authenticated viewer has any pending notifications.

```http
POST /api/connect/chatto.api.v1.NotificationService/HasNotifications
```

<a id="chatto-api-v1-HasNotificationsRequest"></a>

#### Input: HasNotificationsRequest

Request for a lightweight pending notification check.


<a id="chatto-api-v1-HasNotificationsResponse"></a>

#### Result: HasNotificationsResponse

Lightweight pending notification check.

| Field | Type | Description |
| --- | --- | --- |
| `has_notifications` | `bool` | True when the authenticated viewer has at least one pending notification. |


<a id="chatto-api-v1-NotificationService-DismissNotification"></a>

### DismissNotification

Dismisses one pending notification.

```http
POST /api/connect/chatto.api.v1.NotificationService/DismissNotification
```

<a id="chatto-api-v1-DismissNotificationRequest"></a>

#### Input: DismissNotificationRequest

Request to dismiss one pending notification.

| Field | Type | Description |
| --- | --- | --- |
| `notification_id` | `string` | Required. Notification to dismiss. |


<a id="chatto-api-v1-DismissNotificationResponse"></a>

#### Result: DismissNotificationResponse

Result of dismissing one pending notification.

| Field | Type | Description |
| --- | --- | --- |
| `dismissed` | `bool` | True when the notification existed and was dismissed. |


<a id="chatto-api-v1-NotificationService-DismissAllNotifications"></a>

### DismissAllNotifications

Dismisses all pending notifications.

```http
POST /api/connect/chatto.api.v1.NotificationService/DismissAllNotifications
```

<a id="chatto-api-v1-DismissAllNotificationsRequest"></a>

#### Input: DismissAllNotificationsRequest

Request to dismiss all pending notifications.


<a id="chatto-api-v1-DismissAllNotificationsResponse"></a>

#### Result: DismissAllNotificationsResponse

Result of dismissing all pending notifications.

| Field | Type | Description |
| --- | --- | --- |
| `dismissed_count` | `int32` | Number of notifications dismissed. |


<a id="chatto-api-v1-PermissionService"></a>

## PermissionService

Provides permission matrix reads and admin permission writes.

<a id="chatto-api-v1-PermissionService-GetRolePermissionTierMatrix"></a>

### GetRolePermissionTierMatrix

Gets the role-permission matrix for a single tier.

```http
POST /api/connect/chatto.api.v1.PermissionService/GetRolePermissionTierMatrix
```

<a id="chatto-api-v1-GetRolePermissionTierMatrixRequest"></a>

#### Input: GetRolePermissionTierMatrixRequest

Request a role-permission tier matrix.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Optional room scope. Mutually exclusive with group_id. |
| `group_id` | `string` | Optional room-group scope. Mutually exclusive with room_id. |


<a id="chatto-api-v1-GetRolePermissionTierMatrixResponse"></a>

#### Result: GetRolePermissionTierMatrixResponse

Response containing a role-permission tier matrix.

| Field | Type | Description |
| --- | --- | --- |
| `matrix` | [`TierRoles`](#chatto-api-v1-TierRoles) | Requested matrix. |


<a id="chatto-api-v1-PermissionService-GetRolePermissionMatrix"></a>

### GetRolePermissionMatrix

Gets one role's full permission matrix. Requires role.manage.

```http
POST /api/connect/chatto.api.v1.PermissionService/GetRolePermissionMatrix
```

<a id="chatto-api-v1-GetRolePermissionMatrixRequest"></a>

#### Input: GetRolePermissionMatrixRequest

Request one role's permission matrix.

| Field | Type | Description |
| --- | --- | --- |
| `role_name` | `string` | Stable role name. |


<a id="chatto-api-v1-GetRolePermissionMatrixResponse"></a>

#### Result: GetRolePermissionMatrixResponse

Response containing one role's permission matrix.

| Field | Type | Description |
| --- | --- | --- |
| `matrix` | [`RolePermissionMatrix`](#chatto-api-v1-RolePermissionMatrix) | Requested matrix. Nil when the role does not exist. |


<a id="chatto-api-v1-PermissionService-GetUserPermissionMatrix"></a>

### GetUserPermissionMatrix

Gets one user's full permission matrix. Requires user.manage-permissions.

```http
POST /api/connect/chatto.api.v1.PermissionService/GetUserPermissionMatrix
```

<a id="chatto-api-v1-GetUserPermissionMatrixRequest"></a>

#### Input: GetUserPermissionMatrixRequest

Request one user's permission matrix.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | User ID. |


<a id="chatto-api-v1-GetUserPermissionMatrixResponse"></a>

#### Result: GetUserPermissionMatrixResponse

Response containing one user's permission matrix.

| Field | Type | Description |
| --- | --- | --- |
| `matrix` | [`UserPermissionMatrix`](#chatto-api-v1-UserPermissionMatrix) | Requested matrix. |


<a id="chatto-api-v1-PermissionService-ExplainPermissions"></a>

### ExplainPermissions

Explains permission resolution for another user. Requires role.manage.

```http
POST /api/connect/chatto.api.v1.PermissionService/ExplainPermissions
```

<a id="chatto-api-v1-ExplainPermissionsRequest"></a>

#### Input: ExplainPermissionsRequest

Request permission explanations for a user at server or room scope.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | User whose permissions should be explained. |
| `room_id` | `string` | Optional room scope. Omit for server-scoped permissions. |


<a id="chatto-api-v1-ExplainPermissionsResponse"></a>

#### Result: ExplainPermissionsResponse

Permission explanations for all permissions applicable at the requested scope.

| Field | Type | Description |
| --- | --- | --- |
| `explanations` | repeated [`PermissionExplanation`](#chatto-api-v1-PermissionExplanation) | Explanation rows sorted by permission metadata order. |


<a id="chatto-api-v1-PermissionService-SetRolePermission"></a>

### SetRolePermission

Sets one role permission decision. Requires role.manage or scoped room.manage for room scope.

```http
POST /api/connect/chatto.api.v1.PermissionService/SetRolePermission
```

<a id="chatto-api-v1-SetRolePermissionRequest"></a>

#### Input: SetRolePermissionRequest

Request to set one role permission state.

| Field | Type | Description |
| --- | --- | --- |
| `role_name` | `string` | Role name. |
| `permission` | `string` | Permission identifier. |
| `decision` | [`PermissionDecision`](#chatto-api-v1-PermissionDecision) | New decision. NONE clears the explicit state. |
| `scope` | [`PermissionScope`](#chatto-api-v1-PermissionScope) | Target scope. Omit or use SERVER for server scope. |


<a id="chatto-api-v1-SetRolePermissionResponse"></a>

#### Result: SetRolePermissionResponse

Result of setting one role permission state.

| Field | Type | Description |
| --- | --- | --- |
| `ok` | `bool` | True when the request completed. |


<a id="chatto-api-v1-PermissionService-RevokeRolePermissionGrant"></a>

### RevokeRolePermissionGrant

Removes only a server-scope allow grant from a role. Requires role.manage.

```http
POST /api/connect/chatto.api.v1.PermissionService/RevokeRolePermissionGrant
```

<a id="chatto-api-v1-RevokeRolePermissionGrantRequest"></a>

#### Input: RevokeRolePermissionGrantRequest

Request to remove only one server-scope role permission grant.

| Field | Type | Description |
| --- | --- | --- |
| `role_name` | `string` | Role name. |
| `permission` | `string` | Permission identifier. |


<a id="chatto-api-v1-RevokeRolePermissionGrantResponse"></a>

#### Result: RevokeRolePermissionGrantResponse

Result of removing one server-scope role permission grant.

| Field | Type | Description |
| --- | --- | --- |
| `ok` | `bool` | True when the request completed. |


<a id="chatto-api-v1-PermissionService-SetUserPermission"></a>

### SetUserPermission

Sets one user permission decision. Requires user.manage-permissions.

```http
POST /api/connect/chatto.api.v1.PermissionService/SetUserPermission
```

<a id="chatto-api-v1-SetUserPermissionRequest"></a>

#### Input: SetUserPermissionRequest

Request to set one user permission state.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Target user ID. |
| `permission` | `string` | Permission identifier. |
| `decision` | [`PermissionDecision`](#chatto-api-v1-PermissionDecision) | New decision. NONE clears the explicit state. |
| `scope` | [`PermissionScope`](#chatto-api-v1-PermissionScope) | Target scope. Omit or use SERVER for server scope. |


<a id="chatto-api-v1-SetUserPermissionResponse"></a>

#### Result: SetUserPermissionResponse

Result of setting one user permission state.

| Field | Type | Description |
| --- | --- | --- |
| `ok` | `bool` | True when the request completed. |


<a id="chatto-api-v1-PushNotificationService"></a>

## PushNotificationService

Browser Web Push subscription commands for the current user.

<a id="chatto-api-v1-PushNotificationService-Subscribe"></a>

### Subscribe

Stores or updates the caller's browser push subscription.

The server must have Web Push configured. Clients normally call this after
the browser grants notification permission and returns a PushSubscription.

```http
POST /api/connect/chatto.api.v1.PushNotificationService/Subscribe
```

<a id="chatto-api-v1-SubscribeRequest"></a>

#### Input: SubscribeRequest

Request to store a PushSubscription returned by the browser Push API.

| Field | Type | Description |
| --- | --- | --- |
| `endpoint` | `string` | Push service endpoint URL. |
| `p256dh` | `string` | Client P-256 ECDH public key from PushSubscription.keys.p256dh. |
| `auth` | `string` | Authentication secret from PushSubscription.keys.auth. |
| `user_agent` | `optional string` | Optional browser user-agent string for device identification. |


<a id="chatto-api-v1-SubscribeResponse"></a>

#### Result: SubscribeResponse

Response from storing a browser push subscription.

| Field | Type | Description |
| --- | --- | --- |
| `subscribed` | `bool` | True when the subscription was stored. |


<a id="chatto-api-v1-PushNotificationService-Unsubscribe"></a>

### Unsubscribe

Removes the caller's browser push subscription by endpoint.

The call is idempotent: removing an unknown endpoint still succeeds.

```http
POST /api/connect/chatto.api.v1.PushNotificationService/Unsubscribe
```

<a id="chatto-api-v1-UnsubscribeRequest"></a>

#### Input: UnsubscribeRequest

Request to remove a browser push subscription.

| Field | Type | Description |
| --- | --- | --- |
| `endpoint` | `string` | Push service endpoint URL to remove. |


<a id="chatto-api-v1-UnsubscribeResponse"></a>

#### Result: UnsubscribeResponse

Response from removing a browser push subscription.

| Field | Type | Description |
| --- | --- | --- |
| `unsubscribed` | `bool` | True when the request completed. |


<a id="chatto-api-v1-ReactionService"></a>

## ReactionService

Manages message reactions for the current user.

<a id="chatto-api-v1-ReactionService-AddReaction"></a>

### AddReaction

Adds a reaction to a message. The user must be a room member and have
message.react in the target room.

```http
POST /api/connect/chatto.api.v1.ReactionService/AddReaction
```

<a id="chatto-api-v1-AddReactionRequest"></a>

#### Input: AddReactionRequest

Request to add the current user's reaction to a message.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room containing the message event. |
| `message_event_id` | `string` | Required. Event ID of the message being reacted to. |
| `emoji` | `string` | Required. Emoji shortcode name, such as "thumbsup" or "heart". |


<a id="chatto-api-v1-AddReactionResponse"></a>

#### Result: AddReactionResponse

Result of adding a reaction.

| Field | Type | Description |
| --- | --- | --- |
| `added` | `bool` | True when the reaction was newly added, false when it already existed. |


<a id="chatto-api-v1-ReactionService-RemoveReaction"></a>

### RemoveReaction

Removes a reaction from a message. The user must be a room member and have
message.react in the target room.

```http
POST /api/connect/chatto.api.v1.ReactionService/RemoveReaction
```

<a id="chatto-api-v1-RemoveReactionRequest"></a>

#### Input: RemoveReactionRequest

Request to remove the current user's reaction from a message.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room containing the message event. |
| `message_event_id` | `string` | Required. Event ID of the message whose reaction should be removed. |
| `emoji` | `string` | Required. Emoji shortcode name, such as "thumbsup" or "heart". |


<a id="chatto-api-v1-RemoveReactionResponse"></a>

#### Result: RemoveReactionResponse

Result of removing a reaction.

| Field | Type | Description |
| --- | --- | --- |
| `removed` | `bool` | True when the reaction was removed, false when it did not exist. |


<a id="chatto-api-v1-ReadStateService"></a>

## ReadStateService

Updates room and thread read state for the current user.

<a id="chatto-api-v1-ReadStateService-MarkRoomAsRead"></a>

### MarkRoomAsRead

Marks a room timeline as read through the supplied event. If no event is
supplied, the server marks through the room's latest root event. Clients
usually call this after the user has viewed the latest visible event in the
room.

```http
POST /api/connect/chatto.api.v1.ReadStateService/MarkRoomAsRead
```

<a id="chatto-api-v1-MarkRoomAsReadRequest"></a>

#### Input: MarkRoomAsReadRequest

Request to mark a room timeline as read.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room whose timeline should be marked read. |
| `up_to_event_id` | `string` | Highest room event ID the current user has read. When set, the event must exist as a root event in the room timeline. Leave empty to mark through the room's current latest root event. |


<a id="chatto-api-v1-MarkRoomAsReadResponse"></a>

#### Result: MarkRoomAsReadResponse

Result of marking a room timeline as read.

Clients can use the previous timestamp to decide whether unread badges or
local notification state need to be reconciled.

| Field | Type | Description |
| --- | --- | --- |
| `last_read_at` | `google.protobuf.Timestamp` | New room read timestamp stored for the current user. |
| `previous_last_read_at` | `google.protobuf.Timestamp` | Previous room read timestamp, when one existed. |


<a id="chatto-api-v1-ReadStateService-MarkThreadAsRead"></a>

### MarkThreadAsRead

Marks a thread timeline as read through the supplied event without changing
the room-level read marker.

```http
POST /api/connect/chatto.api.v1.ReadStateService/MarkThreadAsRead
```

<a id="chatto-api-v1-MarkThreadAsReadRequest"></a>

#### Input: MarkThreadAsReadRequest

Request to mark a message thread as read.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room containing the thread. |
| `thread_root_event_id` | `string` | Required. Event ID of the root message for the thread. |
| `up_to_event_id` | `string` | Highest thread event ID the current user has read. The event should belong to the thread identified by thread_root_event_id. |


<a id="chatto-api-v1-MarkThreadAsReadResponse"></a>

#### Result: MarkThreadAsReadResponse

Result of marking a message thread as read.

| Field | Type | Description |
| --- | --- | --- |
| `previous_read_at` | `google.protobuf.Timestamp` | Previous thread read timestamp, when one existed. |


<a id="chatto-api-v1-RoleService"></a>

## RoleService

Provides authenticated role catalog reads and admin role CRUD.

<a id="chatto-api-v1-RoleService-ListRoles"></a>

### ListRoles

Lists the server role catalog. Requires an authenticated user.

```http
POST /api/connect/chatto.api.v1.RoleService/ListRoles
```

<a id="chatto-api-v1-ListRolesRequest"></a>

#### Input: ListRolesRequest

Request the server role catalog.


<a id="chatto-api-v1-ListRolesResponse"></a>

#### Result: ListRolesResponse

Server role catalog plus viewer role-management capabilities.

| Field | Type | Description |
| --- | --- | --- |
| `roles` | repeated [`Role`](#chatto-api-v1-Role) | Roles sorted by position. |
| `viewer_can_manage_roles` | `bool` | Whether the caller may create/update/delete role definitions. |
| `viewer_can_assign_roles` | `bool` | Whether the caller may assign/revoke roles and view role rosters. |


<a id="chatto-api-v1-RoleService-GetRole"></a>

### GetRole

Gets one server role plus admin detail metadata. Requires an authenticated user.

```http
POST /api/connect/chatto.api.v1.RoleService/GetRole
```

<a id="chatto-api-v1-GetRoleRequest"></a>

#### Input: GetRoleRequest

Request one server role.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Stable role name. |


<a id="chatto-api-v1-GetRoleResponse"></a>

#### Result: GetRoleResponse

One server role plus role-management metadata.

| Field | Type | Description |
| --- | --- | --- |
| `role` | [`Role`](#chatto-api-v1-Role) | Requested role, when found. |
| `users` | repeated [`RoleUser`](#chatto-api-v1-RoleUser) | Explicit users with this role. Empty unless the caller may assign roles. |
| `viewer_can_manage_roles` | `bool` | Whether the caller may create/update/delete role definitions. |
| `viewer_can_assign_roles` | `bool` | Whether the caller may assign/revoke roles and view role rosters. |


<a id="chatto-api-v1-RoleService-CreateRole"></a>

### CreateRole

Creates a custom server role. Requires role.manage.

```http
POST /api/connect/chatto.api.v1.RoleService/CreateRole
```

<a id="chatto-api-v1-CreateRoleRequest"></a>

#### Input: CreateRoleRequest

Request to create a server role.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Stable role name. |
| `display_name` | `string` | Display name shown in admin and mention UIs. |
| `description` | `string` | Optional role description. |
| `pingable` | `bool` | Whether messages may notify users assigned to this role. |


<a id="chatto-api-v1-CreateRoleResponse"></a>

#### Result: CreateRoleResponse

Result of creating a server role.

| Field | Type | Description |
| --- | --- | --- |
| `role` | [`Role`](#chatto-api-v1-Role) | Created role. |


<a id="chatto-api-v1-RoleService-UpdateRole"></a>

### UpdateRole

Updates role metadata. Requires role.manage.

```http
POST /api/connect/chatto.api.v1.RoleService/UpdateRole
```

<a id="chatto-api-v1-UpdateRoleRequest"></a>

#### Input: UpdateRoleRequest

Request to update server role metadata.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Stable role name. |
| `display_name` | `string` | Display name shown in admin and mention UIs. |
| `description` | `string` | Optional role description. |
| `pingable` | `optional bool` | Whether messages may notify users assigned to this role, when changing it. |


<a id="chatto-api-v1-UpdateRoleResponse"></a>

#### Result: UpdateRoleResponse

Result of updating a server role.

| Field | Type | Description |
| --- | --- | --- |
| `role` | [`Role`](#chatto-api-v1-Role) | Updated role. |


<a id="chatto-api-v1-RoleService-DeleteRole"></a>

### DeleteRole

Deletes a custom server role. Requires role.manage.

```http
POST /api/connect/chatto.api.v1.RoleService/DeleteRole
```

<a id="chatto-api-v1-DeleteRoleRequest"></a>

#### Input: DeleteRoleRequest

Request to delete a server role.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Stable role name. |


<a id="chatto-api-v1-DeleteRoleResponse"></a>

#### Result: DeleteRoleResponse

Result of deleting a server role.

| Field | Type | Description |
| --- | --- | --- |
| `deleted` | `bool` | True when the request completed. |


<a id="chatto-api-v1-RoleService-ReorderRoles"></a>

### ReorderRoles

Replaces the custom role order. Requires role.manage.

```http
POST /api/connect/chatto.api.v1.RoleService/ReorderRoles
```

<a id="chatto-api-v1-ReorderRolesRequest"></a>

#### Input: ReorderRolesRequest

Request to replace custom server role order.

| Field | Type | Description |
| --- | --- | --- |
| `role_names` | `repeated string` | Ordered custom role names. The list must include every custom role exactly once; system roles keep fixed positions and must not be included. |


<a id="chatto-api-v1-ReorderRolesResponse"></a>

#### Result: ReorderRolesResponse

Result of replacing custom server role order.

| Field | Type | Description |
| --- | --- | --- |
| `roles` | repeated [`Role`](#chatto-api-v1-Role) | Full role catalog after reordering. |


<a id="chatto-api-v1-ServerService"></a>

## ServerService

Provides public server discovery metadata.

<a id="chatto-api-v1-ServerService-GetServer"></a>

### GetServer

Returns public server metadata, branding, registration status, and login
provider information. This RPC is available before login so clients can
render the first server screen and authentication choices.

```http
POST /api/connect/chatto.api.v1.ServerService/GetServer
```

<a id="chatto-api-v1-GetServerRequest"></a>

#### Input: GetServerRequest

Request for public server metadata.


<a id="chatto-api-v1-GetServerResponse"></a>

#### Result: GetServerResponse

Public metadata clients need before they can authenticate or render the
initial server view.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Display name of the Chatto server. |
| `version` | `string` | Server software version. |
| `auth_methods` | `repeated string` | Enabled legacy authentication method identifiers. |
| `auth_providers` | repeated [`AuthProvider`](#chatto-api-v1-AuthProvider) | Configured login providers. |
| `registration_open` | `bool` | Whether users can create accounts through the public UI. |
| `authorize_url` | `string` | URL for the legacy authorization flow, when enabled. |
| `welcome_message` | `string` | Optional welcome message configured by the server administrator. |
| `description` | `string` | Optional server description configured by the server administrator. |
| `icon_url` | `string` | Optional server icon URL suitable for branding the server in client UI. |
| `banner_url` | `string` | Optional server banner URL suitable for larger server branding surfaces. |


<a id="chatto-api-v1-ServerStateService"></a>

## ServerStateService

Provides authenticated server state and current user's server capabilities.

<a id="chatto-api-v1-ServerStateService-GetServerState"></a>

### GetServerState

Returns authenticated server state. This RPC requires a logged-in user;
public discovery remains available through ServerService.GetServer.

```http
POST /api/connect/chatto.api.v1.ServerStateService/GetServerState
```

<a id="chatto-api-v1-GetServerStateRequest"></a>

#### Input: GetServerStateRequest

Request for authenticated server state.


<a id="chatto-api-v1-GetServerStateResponse"></a>

#### Result: GetServerStateResponse

Authenticated server state used by the web client and public API clients.

| Field | Type | Description |
| --- | --- | --- |
| `profile` | [`ServerProfile`](#chatto-api-v1-ServerProfile) | Server profile and branding. |
| `push_notifications_enabled` | `bool` | Whether Web Push notifications are fully configured. |
| `vapid_public_key` | `optional string` | Optional VAPID public key for Web Push registration. |
| `livekit_url` | `optional string` | Optional LiveKit URL for voice calls. |
| `direct_registration_enabled` | `bool` | Whether direct email/password registration is enabled. |
| `video_processing_enabled` | `bool` | Whether video processing is enabled. |
| `max_upload_size` | `int64` | Maximum general upload size in bytes. |
| `max_video_upload_size` | `int64` | Maximum video upload size in bytes. |
| `message_edit_window_seconds` | `int32` | Message edit window in seconds. |
| `viewer_capabilities` | [`ServerViewerCapabilities`](#chatto-api-v1-ServerViewerCapabilities) | Permission-derived state for the authenticated user. |


<a id="chatto-api-v1-ServerStateService-UpdateServerConfig"></a>

### UpdateServerConfig

Updates runtime-editable server profile settings. This RPC requires
server.manage.

```http
POST /api/connect/chatto.api.v1.ServerStateService/UpdateServerConfig
```

<a id="chatto-api-v1-UpdateServerConfigRequest"></a>

#### Input: UpdateServerConfigRequest

Request to update runtime-editable server profile settings.

| Field | Type | Description |
| --- | --- | --- |
| `server_name` | `optional string` | Optional server name for page titles. Set to empty string to use default. |
| `description` | `optional string` | Optional short server description for OG link-preview metadata. Set to empty string to clear. |
| `motd` | `optional string` | Optional message of the day for the header. Set to empty string to clear. |
| `welcome_message` | `optional string` | Optional welcome message shown on the login page. Set to empty string to clear. |


<a id="chatto-api-v1-UpdateServerConfigResponse"></a>

#### Result: UpdateServerConfigResponse

Result of updating runtime-editable server profile settings.

| Field | Type | Description |
| --- | --- | --- |
| `profile` | [`ServerProfile`](#chatto-api-v1-ServerProfile) | Updated server profile and branding. |


<a id="chatto-api-v1-ServerStateService-UploadServerLogo"></a>

### UploadServerLogo

Uploads and sets the public server logo. This RPC requires server.manage.

```http
POST /api/connect/chatto.api.v1.ServerStateService/UploadServerLogo
```

<a id="chatto-api-v1-UploadServerLogoRequest"></a>

#### Input: UploadServerLogoRequest

Request to upload and set the public server logo.

| Field | Type | Description |
| --- | --- | --- |
| `image` | `bytes` | Raw image bytes. The server validates, resizes, and stores a WebP logo. |
| `filename` | `string` | Original browser filename, for diagnostics and future compatibility. |
| `content_type` | `string` | Browser-provided content type, for diagnostics and future compatibility. |


<a id="chatto-api-v1-UploadServerLogoResponse"></a>

#### Result: UploadServerLogoResponse

Result of uploading the public server logo.

| Field | Type | Description |
| --- | --- | --- |
| `profile` | [`ServerProfile`](#chatto-api-v1-ServerProfile) | Updated server profile and branding. |


<a id="chatto-api-v1-ServerStateService-DeleteServerLogo"></a>

### DeleteServerLogo

Clears the public server logo. This RPC requires server.manage.

```http
POST /api/connect/chatto.api.v1.ServerStateService/DeleteServerLogo
```

<a id="chatto-api-v1-DeleteServerLogoRequest"></a>

#### Input: DeleteServerLogoRequest

Request to clear the public server logo.


<a id="chatto-api-v1-DeleteServerLogoResponse"></a>

#### Result: DeleteServerLogoResponse

Result of clearing the public server logo.

| Field | Type | Description |
| --- | --- | --- |
| `profile` | [`ServerProfile`](#chatto-api-v1-ServerProfile) | Updated server profile and branding. |


<a id="chatto-api-v1-ServerStateService-UploadServerBanner"></a>

### UploadServerBanner

Uploads and sets the public server banner. This RPC requires server.manage.

```http
POST /api/connect/chatto.api.v1.ServerStateService/UploadServerBanner
```

<a id="chatto-api-v1-UploadServerBannerRequest"></a>

#### Input: UploadServerBannerRequest

Request to upload and set the public server banner.

| Field | Type | Description |
| --- | --- | --- |
| `image` | `bytes` | Raw image bytes. The server validates, resizes, and stores a WebP banner. |
| `filename` | `string` | Original browser filename, for diagnostics and future compatibility. |
| `content_type` | `string` | Browser-provided content type, for diagnostics and future compatibility. |


<a id="chatto-api-v1-UploadServerBannerResponse"></a>

#### Result: UploadServerBannerResponse

Result of uploading the public server banner.

| Field | Type | Description |
| --- | --- | --- |
| `profile` | [`ServerProfile`](#chatto-api-v1-ServerProfile) | Updated server profile and branding. |


<a id="chatto-api-v1-ServerStateService-DeleteServerBanner"></a>

### DeleteServerBanner

Clears the public server banner. This RPC requires server.manage.

```http
POST /api/connect/chatto.api.v1.ServerStateService/DeleteServerBanner
```

<a id="chatto-api-v1-DeleteServerBannerRequest"></a>

#### Input: DeleteServerBannerRequest

Request to clear the public server banner.


<a id="chatto-api-v1-DeleteServerBannerResponse"></a>

#### Result: DeleteServerBannerResponse

Result of clearing the public server banner.

| Field | Type | Description |
| --- | --- | --- |
| `profile` | [`ServerProfile`](#chatto-api-v1-ServerProfile) | Updated server profile and branding. |


<a id="chatto-api-v1-ServerStateService-GetServerSecurityConfig"></a>

### GetServerSecurityConfig

Returns security-sensitive server configuration. This RPC requires
server.manage.

```http
POST /api/connect/chatto.api.v1.ServerStateService/GetServerSecurityConfig
```

<a id="chatto-api-v1-GetServerSecurityConfigRequest"></a>

#### Input: GetServerSecurityConfigRequest

Request for server security configuration.


<a id="chatto-api-v1-GetServerSecurityConfigResponse"></a>

#### Result: GetServerSecurityConfigResponse

Security-sensitive server configuration. The caller must be allowed to
manage server configuration.

| Field | Type | Description |
| --- | --- | --- |
| `blocked_usernames` | `string` | Newline-separated blocked usernames. |


<a id="chatto-api-v1-ServerStateService-UpdateBlockedUsernames"></a>

### UpdateBlockedUsernames

Updates the blocked-username list. This RPC requires server.manage.

```http
POST /api/connect/chatto.api.v1.ServerStateService/UpdateBlockedUsernames
```

<a id="chatto-api-v1-UpdateBlockedUsernamesRequest"></a>

#### Input: UpdateBlockedUsernamesRequest

Request to update the server blocked-username list.

| Field | Type | Description |
| --- | --- | --- |
| `blocked_usernames` | `string` | Newline-separated blocked usernames. |


<a id="chatto-api-v1-UpdateBlockedUsernamesResponse"></a>

#### Result: UpdateBlockedUsernamesResponse

Result of updating the server blocked-username list.

| Field | Type | Description |
| --- | --- | --- |
| `blocked_usernames` | `string` | Effective newline-separated blocked usernames after the update. |


<a id="chatto-api-v1-ThreadService"></a>

## ThreadService

Manages thread follow state for the current user.

<a id="chatto-api-v1-ThreadService-ListFollowedThreads"></a>

### ListFollowedThreads

Returns followed threads for the current user, including enough root-message
data for clients to render the list without extra per-field fetches.

```http
POST /api/connect/chatto.api.v1.ThreadService/ListFollowedThreads
```

<a id="chatto-api-v1-ListFollowedThreadsRequest"></a>

#### Input: ListFollowedThreadsRequest

Request for a page of followed threads for the current user.

| Field | Type | Description |
| --- | --- | --- |
| `limit` | `int32` | Maximum number of followed threads to return. |
| `offset` | `int32` | Zero-based offset into the followed-thread list. |


<a id="chatto-api-v1-ListFollowedThreadsResponse"></a>

#### Result: ListFollowedThreadsResponse

Response containing one followed-thread page.

| Field | Type | Description |
| --- | --- | --- |
| `threads` | repeated [`FollowedThread`](#chatto-api-v1-FollowedThread) | Followed threads in newest-activity-first order. |
| `total_count` | `int32` | Total followed-thread count before pagination. |
| `has_more` | `bool` | True when another page exists after this response. |
| `includes` | [`RoomTimelineIncludes`](#chatto-api-v1-RoomTimelineIncludes) | Related entities needed to render root messages. |


<a id="chatto-api-v1-ThreadService-FollowThread"></a>

### FollowThread

Follows a thread for the current user. Followed threads can be surfaced in
clients and can participate in thread notification behavior.

```http
POST /api/connect/chatto.api.v1.ThreadService/FollowThread
```

<a id="chatto-api-v1-FollowThreadRequest"></a>

#### Input: FollowThreadRequest

Request to follow one message thread.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room containing the thread. |
| `thread_root_event_id` | `string` | Required. Event ID of the root message for the thread. |


<a id="chatto-api-v1-FollowThreadResponse"></a>

#### Result: FollowThreadResponse

Result of following a thread.

| Field | Type | Description |
| --- | --- | --- |
| `following` | `bool` | True when the current user follows the thread after the operation. |


<a id="chatto-api-v1-ThreadService-UnfollowThread"></a>

### UnfollowThread

Stops following a thread for the current user. The response reports the
resulting follow state so clients can update local UI immediately.

```http
POST /api/connect/chatto.api.v1.ThreadService/UnfollowThread
```

<a id="chatto-api-v1-UnfollowThreadRequest"></a>

#### Input: UnfollowThreadRequest

Request to stop following one message thread.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room containing the thread. |
| `thread_root_event_id` | `string` | Required. Event ID of the root message for the thread. |


<a id="chatto-api-v1-UnfollowThreadResponse"></a>

#### Result: UnfollowThreadResponse

Result of unfollowing a thread.

| Field | Type | Description |
| --- | --- | --- |
| `following` | `bool` | True when the current user follows the thread after the operation. |


<a id="chatto-api-v1-UserService"></a>

## UserService

Provides authenticated public user profile lookups.

<a id="chatto-api-v1-UserService-GetUser"></a>

### GetUser

Gets a user by stable ID. Returns NOT_FOUND when the ID is unknown.

```http
POST /api/connect/chatto.api.v1.UserService/GetUser
```

<a id="chatto-api-v1-GetUserRequest"></a>

#### Input: GetUserRequest

Request one user by stable user ID.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Required user ID. |
| `avatar` | [`UserAvatarOptions`](#chatto-api-v1-UserAvatarOptions) | Optional avatar URL transform parameters. Omit for the original avatar URL. |


<a id="chatto-api-v1-GetUserResponse"></a>

#### Result: GetUserResponse

Public authenticated user profile response.

| Field | Type | Description |
| --- | --- | --- |
| `user` | [`DirectoryMember`](#chatto-api-v1-DirectoryMember) | Requested user. |


<a id="chatto-api-v1-UserService-GetUserByLogin"></a>

### GetUserByLogin

Gets a user by login. Returns an empty response when the login is unknown.

```http
POST /api/connect/chatto.api.v1.UserService/GetUserByLogin
```

<a id="chatto-api-v1-GetUserByLoginRequest"></a>

#### Input: GetUserByLoginRequest

Request one user by login identifier.

| Field | Type | Description |
| --- | --- | --- |
| `login` | `string` | Required login identifier. |
| `avatar` | [`UserAvatarOptions`](#chatto-api-v1-UserAvatarOptions) | Optional avatar URL transform parameters. Omit for the original avatar URL. |


<a id="chatto-api-v1-GetUserByLoginResponse"></a>

#### Result: GetUserByLoginResponse

Public authenticated user profile response for login lookup.

| Field | Type | Description |
| --- | --- | --- |
| `user` | [`DirectoryMember`](#chatto-api-v1-DirectoryMember) | Requested user. Absent when the login is unknown. |


<a id="chatto-api-v1-UserService-BatchGetUsers"></a>

### BatchGetUsers

Gets lightweight summaries for multiple users, suitable for frontend cache
misses when rendering event-focused payloads.

```http
POST /api/connect/chatto.api.v1.UserService/BatchGetUsers
```

<a id="chatto-api-v1-BatchGetUsersRequest"></a>

#### Input: BatchGetUsersRequest

Request lightweight summaries for a set of stable user IDs.

| Field | Type | Description |
| --- | --- | --- |
| `user_ids` | `repeated string` | Required user IDs. Unknown IDs are omitted from the response. |
| `avatar` | [`UserAvatarOptions`](#chatto-api-v1-UserAvatarOptions) | Optional avatar URL transform parameters. Omit for the original avatar URL. |


<a id="chatto-api-v1-BatchGetUsersResponse"></a>

#### Result: BatchGetUsersResponse

Lightweight batch user summary response.

| Field | Type | Description |
| --- | --- | --- |
| `users` | repeated [`UserSummary`](#chatto-api-v1-UserSummary) | Found users. The server preserves first-seen request order and de-duplicates repeated IDs. |


<a id="chatto-api-v1-VoiceCallService"></a>

## VoiceCallService

Voice-call request/response APIs backed by Chatto call-state projections.

<a id="chatto-api-v1-VoiceCallService-ListActiveCallRooms"></a>

### ListActiveCallRooms

Lists channel room IDs that currently have active calls.

Returns an empty list when LiveKit is not configured.

```http
POST /api/connect/chatto.api.v1.VoiceCallService/ListActiveCallRooms
```

<a id="chatto-api-v1-ListActiveCallRoomsRequest"></a>

#### Input: ListActiveCallRoomsRequest

Request for active channel call room IDs.


<a id="chatto-api-v1-ListActiveCallRoomsResponse"></a>

#### Result: ListActiveCallRoomsResponse

Active channel call room IDs.

| Field | Type | Description |
| --- | --- | --- |
| `room_ids` | `repeated string` | Room IDs with active calls. |


<a id="chatto-api-v1-VoiceCallService-ListCallParticipants"></a>

### ListCallParticipants

Lists participants currently observed in a room's active call.

The caller must be a member of the room.

```http
POST /api/connect/chatto.api.v1.VoiceCallService/ListCallParticipants
```

<a id="chatto-api-v1-ListCallParticipantsRequest"></a>

#### Input: ListCallParticipantsRequest

Request for participants in a room call.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room whose active call should be inspected. |


<a id="chatto-api-v1-ListCallParticipantsResponse"></a>

#### Result: ListCallParticipantsResponse

Participants in one room call.

| Field | Type | Description |
| --- | --- | --- |
| `participants` | repeated [`CallParticipant`](#chatto-api-v1-CallParticipant) | Participants currently projected for the room call. |


<a id="chatto-api-v1-VoiceCallService-JoinCall"></a>

### JoinCall

Records the caller's intent to join a room call.

Returns joined=false when LiveKit is not configured.

```http
POST /api/connect/chatto.api.v1.VoiceCallService/JoinCall
```

<a id="chatto-api-v1-JoinCallRequest"></a>

#### Input: JoinCallRequest

Request to record joining a room call.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room whose call is being joined. |


<a id="chatto-api-v1-JoinCallResponse"></a>

#### Result: JoinCallResponse

Response from recording a join intent.

| Field | Type | Description |
| --- | --- | --- |
| `joined` | `bool` | True when a join fact was recorded. |


<a id="chatto-api-v1-VoiceCallService-GetCallToken"></a>

### GetCallToken

Issues a LiveKit token for joining the room's active call.

The caller must be a member of the room and a call must already be active.

```http
POST /api/connect/chatto.api.v1.VoiceCallService/GetCallToken
```

<a id="chatto-api-v1-GetCallTokenRequest"></a>

#### Input: GetCallTokenRequest

Request for a LiveKit token for a room call.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room whose active call should be joined. |


<a id="chatto-api-v1-GetCallTokenResponse"></a>

#### Result: GetCallTokenResponse

LiveKit token details for joining a room call.

| Field | Type | Description |
| --- | --- | --- |
| `token` | `string` | LiveKit JWT token. |
| `e2ee_key` | `string` | Shared E2EE key for this active call. |
| `call_id` | `string` | Active call session ID. |


<a id="chatto-api-v1-VoiceCallService-LeaveCall"></a>

### LeaveCall

Records the caller's intent to leave a room call.

Returns left=false when LiveKit is not configured.

```http
POST /api/connect/chatto.api.v1.VoiceCallService/LeaveCall
```

<a id="chatto-api-v1-LeaveCallRequest"></a>

#### Input: LeaveCallRequest

Request to record leaving a room call.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room whose call is being left. |


<a id="chatto-api-v1-LeaveCallResponse"></a>

#### Result: LeaveCallResponse

Response from recording a leave intent.

| Field | Type | Description |
| --- | --- | --- |
| `left` | `bool` | True when a leave fact was recorded. |



## Supporting Types

<a id="chatto-api-v1-CustomUserStatus"></a>

### CustomUserStatus

Custom status shown on a user profile, separate from presence.

| Field | Type | Description |
| --- | --- | --- |
| `emoji` | `string` | Short emoji marker displayed with the status. |
| `text` | `string` | User-written status text. |
| `expires_at` | `google.protobuf.Timestamp` | Optional time after which clients should stop showing the status. |



<a id="chatto-api-v1-RoomNotificationPreference"></a>

### RoomNotificationPreference

Room notification preference for one joined room.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room whose preference is represented. |
| `level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Explicit room-level setting. |
| `effective_level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Level after applying room/server/system defaults. |



<a id="chatto-api-v1-ServerNotificationPreference"></a>

### ServerNotificationPreference

Server-wide notification preference for the authenticated user.

| Field | Type | Description |
| --- | --- | --- |
| `level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Explicit server-level setting. |
| `effective_level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Level after applying system defaults. |



<a id="chatto-api-v1-UserSettings"></a>

### UserSettings

Server-level display settings for the authenticated user.

| Field | Type | Description |
| --- | --- | --- |
| `timezone` | `optional string` | Optional IANA timezone override. Absent means the client should use the browser's local timezone. |
| `time_format` | [`TimeFormat`](#chatto-api-v1-TimeFormat) | Preferred time format. |



<a id="chatto-api-v1-ViewerCapabilities"></a>

### ViewerCapabilities

Permission-derived capabilities for the authenticated user.

| Field | Type | Description |
| --- | --- | --- |
| `can_view_admin` | `bool` | Whether the user has at least one server-admin permission. |
| `can_start_dms` | `bool` | Whether the user may start direct-message conversations. |
| `can_admin_view_users` | `bool` | Whether the user may view the admin users screen. |
| `can_admin_manage_users` | `bool` | Whether the user may manage users. |
| `can_admin_view_roles` | `bool` | Whether the user may view role administration. |
| `can_admin_manage_roles` | `bool` | Whether the user may manage roles. |
| `can_admin_view_system` | `bool` | Whether the user may view system administration. |
| `can_admin_view_audit` | `bool` | Whether the user may view the audit log. |
| `has_unread_followed_threads` | `bool` | Whether the user has unread followed threads. |
| `can_manage_user_permissions` | `bool` | Whether the user may edit direct per-user permission overrides. |



<a id="chatto-api-v1-ViewerUser"></a>

### ViewerUser

Current authenticated user's public profile and self-only settings.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable user ID. |
| `login` | `string` | Login identifier. |
| `display_name` | `string` | Display name shown in Chatto. |
| `avatar_url` | `optional string` | Optional avatar URL. |
| `custom_status` | [`CustomUserStatus`](#chatto-api-v1-CustomUserStatus) | Custom profile status, when set. |
| `presence_status` | [`PresenceStatus`](#chatto-api-v1-PresenceStatus) | Current live presence status. |
| `has_verified_email` | `bool` | Whether the account has at least one verified email address. |
| `settings` | [`UserSettings`](#chatto-api-v1-UserSettings) | Current user's display settings. |
| `viewer_can_delete_account` | `bool` | Whether the authenticated user may delete this account. |
| `last_login_change` | `google.protobuf.Timestamp` | Last time a login credential changed, when known. |



<a id="chatto-api-v1-AccountUser"></a>

### AccountUser

Profile fields returned after a self-service profile or avatar command.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable user ID. |
| `login` | `string` | Login identifier. |
| `display_name` | `string` | Display name shown in Chatto. |
| `avatar_url` | `optional string` | Optional avatar URL. |



<a id="chatto-api-v1-AdminAccountInfo"></a>

### AdminAccountInfo

JetStream account limits and usage.

| Field | Type | Description |
| --- | --- | --- |
| `memory` | `int64` | Maximum memory storage in bytes. Values less than or equal to zero mean unlimited. |
| `memory_used` | `int64` | Used memory storage in bytes. |
| `storage` | `int64` | Maximum file storage in bytes. Values less than or equal to zero mean unlimited. |
| `storage_used` | `int64` | Used file storage in bytes. |
| `streams` | `int32` | Maximum number of streams. Values less than or equal to zero mean unlimited. |
| `streams_used` | `int32` | Current number of streams. |
| `consumers` | `int32` | Maximum number of consumers. Values less than or equal to zero mean unlimited. |
| `consumers_used` | `int32` | Current number of consumers. |



<a id="chatto-api-v1-AdminConnectionInfo"></a>

### AdminConnectionInfo

Current NATS server connection metadata.

| Field | Type | Description |
| --- | --- | --- |
| `connected` | `bool` | Whether Chatto is currently connected to NATS. |
| `server_id` | `string` | Connected NATS server ID. |
| `server_name` | `string` | Connected NATS server name. |
| `version` | `string` | Connected NATS server version. |
| `max_payload` | `int64` | Maximum payload accepted by the connected NATS server. |
| `rtt` | `string` | Round-trip latency to the connected NATS server. |



<a id="chatto-api-v1-AdminNatsConsumerInfo"></a>

### AdminNatsConsumerInfo

JetStream consumer state.

| Field | Type | Description |
| --- | --- | --- |
| `stream` | `string` | Stream that owns the consumer. |
| `name` | `string` | Consumer name. |
| `durable` | `string` | Durable name, when the consumer is durable. |
| `filter_subject` | `string` | Single filter subject, when configured. |
| `filter_subjects` | `repeated string` | Multiple filter subjects, when configured. |
| `ack_policy` | `string` | NATS acknowledgement policy. |
| `pull_based` | `bool` | Whether this is a pull consumer. |
| `push_bound` | `bool` | Whether this push consumer has an active subscription. |
| `pending` | `int64` | Pending messages for this consumer. |
| `ack_pending` | `int32` | Messages waiting for acknowledgement. |
| `redelivered` | `int32` | Redelivered message count. |
| `waiting` | `int32` | Pull requests waiting for messages. |
| `delivered_consumer_sequence` | `string` | Delivered consumer sequence. |
| `delivered_stream_sequence` | `string` | Delivered stream sequence. |
| `ack_floor_consumer_sequence` | `string` | Acknowledgement floor consumer sequence. |
| `ack_floor_stream_sequence` | `string` | Acknowledgement floor stream sequence. |



<a id="chatto-api-v1-AdminNatsStats"></a>

### AdminNatsStats

JetStream stream and consumer diagnostics.

| Field | Type | Description |
| --- | --- | --- |
| `total_messages` | `int64` | Total messages stored across all streams. |
| `total_bytes` | `int64` | Total bytes stored across all streams. |
| `total_consumer_pending` | `int64` | Total pending messages across all consumers. |
| `total_ack_pending` | `int32` | Total messages currently waiting for acknowledgement. |
| `streams` | repeated [`AdminNatsStreamInfo`](#chatto-api-v1-AdminNatsStreamInfo) | Streams sorted by name. |
| `consumers` | repeated [`AdminNatsConsumerInfo`](#chatto-api-v1-AdminNatsConsumerInfo) | Consumers sorted by stream and name. |



<a id="chatto-api-v1-AdminNatsStreamInfo"></a>

### AdminNatsStreamInfo

JetStream stream state.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Stream name. |
| `description` | `string` | Stream description. |
| `subjects` | `repeated string` | Subjects stored in the stream. |
| `storage` | `string` | Storage backend name. |
| `messages` | `int64` | Number of messages in the stream. |
| `bytes` | `int64` | Number of bytes in the stream. |
| `first_sequence` | `string` | First stream sequence. |
| `last_sequence` | `string` | Last stream sequence. |
| `consumer_count` | `int32` | Number of stream consumers. |
| `replicas` | `int32` | Configured stream replica count. |
| `cluster_leader` | `string` | Current cluster leader, when reported by NATS. |



<a id="chatto-api-v1-AdminProjectionMetric"></a>

### AdminProjectionMetric

Projection-specific diagnostic metric.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Metric name. |
| `value` | `int64` | Metric value. |
| `bytes` | `int64` | Estimated memory represented by this metric in bytes. |



<a id="chatto-api-v1-AdminProjectionState"></a>

### AdminProjectionState

Event-sourced projection health and memory estimates.

| Field | Type | Description |
| --- | --- | --- |
| `key` | `string` | Stable projection key. |
| `name` | `string` | Display name. |
| `subjects` | `repeated string` | EVT subjects consumed by this projection. |
| `started` | `bool` | Whether the projection projector is running. |
| `startup_duration_seconds` | `optional double` | Startup duration in seconds, set after startup completes. |
| `last_applied_sequence` | `string` | Last event sequence applied by the projection. |
| `matching_stream_sequence` | `string` | Target sequence among the projection's matching subjects. |
| `stream_last_sequence` | `string` | Current last sequence of the EVT stream. |
| `lag` | `int64` | Matching-subject lag between target and applied sequence. |
| `failed` | `bool` | Whether the projection failed. |
| `failed_sequence` | `string` | Sequence that failed, when failed is true. |
| `failure` | `string` | Failure message, when failed is true. |
| `entry_count` | `int64` | Estimated number of entries held by the projection. |
| `estimated_bytes` | `int64` | Estimated memory used by the projection in bytes. |
| `average_entry_bytes` | `int64` | Estimated average memory per entry in bytes. |
| `metrics` | repeated [`AdminProjectionMetric`](#chatto-api-v1-AdminProjectionMetric) | Projection-specific diagnostic metrics. |



<a id="chatto-api-v1-AdminServerStats"></a>

### AdminServerStats

Aggregate Chatto object counts.

| Field | Type | Description |
| --- | --- | --- |
| `user_count` | `int32` | Registered user count. |
| `channel_room_count` | `int32` | Channel room count. |
| `dm_room_count` | `int32` | Direct-message room count. |



<a id="chatto-api-v1-AdminSystemInfo"></a>

### AdminSystemInfo

Broker and application diagnostics shown on the server admin system page.

| Field | Type | Description |
| --- | --- | --- |
| `connection` | [`AdminConnectionInfo`](#chatto-api-v1-AdminConnectionInfo) | Current NATS server connection metadata. |
| `account` | [`AdminAccountInfo`](#chatto-api-v1-AdminAccountInfo) | JetStream account limits and usage. |
| `nats` | [`AdminNatsStats`](#chatto-api-v1-AdminNatsStats) | JetStream stream and consumer diagnostics. |
| `stats` | [`AdminServerStats`](#chatto-api-v1-AdminServerStats) | Aggregate Chatto object counts. |



<a id="chatto-api-v1-AdminEventLogFilter"></a>

### AdminEventLogFilter

Filter for owner/auditor event-log inspection.

| Field | Type | Description |
| --- | --- | --- |
| `event_type` | `string` | Event oneof payload type, such as MessagePostedEvent. |
| `actor_id` | `string` | Actor user ID. |
| `created_at_from` | `google.protobuf.Timestamp` | Inclusive lower creation timestamp bound. |
| `created_at_to` | `google.protobuf.Timestamp` | Inclusive upper creation timestamp bound. |



<a id="chatto-api-v1-DirectoryMember"></a>

### DirectoryMember

Public user/member profile used by directory and mention surfaces.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable user ID. |
| `login` | `string` | Login identifier. |
| `display_name` | `string` | Display name shown in Chatto. |
| `deleted` | `bool` | Whether this row represents a deleted account tombstone. |
| `avatar_url` | `optional string` | Optional avatar URL. |
| `presence_status` | [`PresenceStatus`](#chatto-api-v1-PresenceStatus) | Current live presence status. |
| `custom_status` | [`CustomUserStatus`](#chatto-api-v1-CustomUserStatus) | Custom profile status, when set. |
| `roles` | `repeated string` | Explicit server roles assigned to the user. Server member listings include the virtual `everyone` role for parity with Chatto's permission model. |
| `created_at` | `google.protobuf.Timestamp` | Account creation time when known. |



<a id="chatto-api-v1-Room"></a>

### Room

Public room metadata returned by room commands.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable room ID. |
| `kind` | [`RoomKind`](#chatto-api-v1-RoomKind) | Room kind. |
| `name` | `string` | Room name. Direct-message rooms may have an empty name because clients derive their display label from participants. |
| `description` | `string` | Optional room description. |
| `archived` | `bool` | True when the room is archived and hidden from active room lists. |
| `group_id` | `string` | Room group ID for channel rooms. Empty for direct-message rooms. |
| `universal` | `bool` | True when a channel grants effective membership to eligible server members. |



<a id="chatto-api-v1-RoomBan"></a>

### RoomBan

Active channel room ban with optional hydrated room and user references.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable ban event ID. |
| `room_id` | `string` | Channel room ID. |
| `room` | [`Room`](#chatto-api-v1-Room) | Channel room metadata, when the referenced room still exists. |
| `user_id` | `string` | Banned user ID. |
| `user` | [`DirectoryMember`](#chatto-api-v1-DirectoryMember) | Banned user profile, when the referenced user still exists. |
| `moderator_id` | `string` | Moderator user ID that created the ban. |
| `moderator` | [`DirectoryMember`](#chatto-api-v1-DirectoryMember) | Moderator profile, when the referenced user still exists. |
| `reason` | `string` | Moderator-entered ban reason. |
| `created_at` | `google.protobuf.Timestamp` | Time the ban was created. |
| `expires_at` | `google.protobuf.Timestamp` | Optional future time when the ban expires. |



<a id="chatto-api-v1-DirectoryRoom"></a>

### DirectoryRoom

Room metadata plus viewer-specific state.

| Field | Type | Description |
| --- | --- | --- |
| `room` | [`Room`](#chatto-api-v1-Room) | Public room metadata. |
| `viewer_state` | [`RoomViewerState`](#chatto-api-v1-RoomViewerState) | State and permissions resolved for the current user. |



<a id="chatto-api-v1-RoomGroup"></a>

### RoomGroup

Ordered group of channel rooms and sidebar links.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable room group ID. |
| `name` | `string` | Display name. |
| `description` | `string` | Operator-facing description, when set. |
| `rooms` | repeated [`DirectoryRoom`](#chatto-api-v1-DirectoryRoom) | Visible channel rooms in this group order. |
| `items` | repeated [`RoomGroupItem`](#chatto-api-v1-RoomGroupItem) | Mixed room/sidebar-link entries in sidebar order. |



<a id="chatto-api-v1-RoomGroupItem"></a>

### RoomGroupItem

One ordered item in a room group.

| Field | Type | Description |
| --- | --- | --- |
| `item.room` | [`DirectoryRoom`](#chatto-api-v1-DirectoryRoom) | Visible room entry. |
| `item.sidebar_link` | [`SidebarLink`](#chatto-api-v1-SidebarLink) | Sidebar link entry. |



<a id="chatto-api-v1-RoomViewerState"></a>

### RoomViewerState

Viewer-specific state for one room.

These fields let clients render navigation rows and room action menus without
issuing per-room permission checks.

| Field | Type | Description |
| --- | --- | --- |
| `is_member` | `bool` | True when the current user is an effective room member. |
| `has_unread` | `bool` | True when the room has unread root messages for the current user. |
| `can_list_room` | `bool` | True when the room can appear in directory surfaces for the current user. |
| `can_join_room` | `bool` | True when the current user can join this room. |
| `can_post_message` | `bool` | True when the current user can post new root messages. |
| `can_post_in_thread` | `bool` | True when the current user can post thread replies. |
| `can_attach` | `bool` | True when the current user can attach files to messages. |
| `can_react` | `bool` | True when the current user can add and remove reactions. |
| `can_echo_message` | `bool` | True when the current user can echo thread replies to the main room. |
| `can_manage_others_message` | `bool` | True when the current user can edit or delete other users' messages. |
| `can_manage_room` | `bool` | True when the current user can configure this room. |
| `can_ban_room_members` | `bool` | True when the current user can ban members from this room. |



<a id="chatto-api-v1-SidebarLink"></a>

### SidebarLink

Sidebar link metadata for room group navigation.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable sidebar link ID. |
| `label` | `string` | Display label. |
| `url` | `string` | Absolute http(s) URL or server-local path. |



<a id="chatto-api-v1-AdminRoomLayoutGroup"></a>

### AdminRoomLayoutGroup

Ordered room group used by the admin room layout editor.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable room group ID. |
| `name` | `string` | Display name. |
| `description` | `string` | Operator-facing description, when set. |
| `rooms` | repeated [`AdminRoomLayoutRoom`](#chatto-api-v1-AdminRoomLayoutRoom) | Channel rooms in this group order. |
| `items` | repeated [`AdminRoomLayoutItem`](#chatto-api-v1-AdminRoomLayoutItem) | Mixed room/sidebar-link entries in sidebar order. |



<a id="chatto-api-v1-AdminRoomLayoutItem"></a>

### AdminRoomLayoutItem

One ordered sidebar item in an admin room group layout.

| Field | Type | Description |
| --- | --- | --- |
| `item.room` | [`AdminRoomLayoutRoom`](#chatto-api-v1-AdminRoomLayoutRoom) | Channel room entry. |
| `item.sidebar_link` | [`SidebarLink`](#chatto-api-v1-SidebarLink) | Sidebar link entry. |



<a id="chatto-api-v1-AdminRoomLayoutItemInput"></a>

### AdminRoomLayoutItemInput

One item reference in a sidebar order replacement.

| Field | Type | Description |
| --- | --- | --- |
| `kind` | [`AdminRoomLayoutItemKind`](#chatto-api-v1-AdminRoomLayoutItemKind) | Item kind. |
| `id` | `string` | Room ID when kind is ROOM, sidebar link ID when kind is SIDEBAR_LINK. |



<a id="chatto-api-v1-AdminRoomLayoutRoom"></a>

### AdminRoomLayoutRoom

Room metadata used by server administrators when editing channel layout.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable room ID. |
| `name` | `string` | Display name. |
| `description` | `string` | Optional room description. |
| `archived` | `bool` | True when the room is archived. |
| `universal` | `bool` | True when the channel grants effective membership to eligible members. |



<a id="chatto-api-v1-AdminMember"></a>

### AdminMember

User row returned by server-admin member management reads.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable user ID. |
| `login` | `string` | Login identifier. |
| `display_name` | `string` | Display name shown in Chatto. |
| `avatar_url` | `optional string` | Optional avatar URL. |
| `roles` | `repeated string` | Explicit server role assignments. The implicit everyone role is omitted. |
| `created_at` | `google.protobuf.Timestamp` | Account creation time, when known. |
| `deleted` | `bool` | Whether the account has been deleted. |
| `has_verified_email` | `bool` | Whether the caller may see that the user has a verified email and one exists. |
| `verified_emails` | `repeated string` | Verified email addresses visible to the caller. |
| `viewer_can_delete_account` | `bool` | Whether the caller may delete this account. |
| `last_login_change` | `google.protobuf.Timestamp` | Last self-service username change, when visible and known. |



<a id="chatto-api-v1-AdminMemberRole"></a>

### AdminMemberRole

Server role details used by the member detail role-assignment UI.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Stable role name. |
| `display_name` | `string` | Display name shown in admin UI. |
| `position` | `int32` | Display/order position. |
| `permissions` | `repeated string` | Server permissions granted by this role. |
| `permission_denials` | `repeated string` | Server permissions denied by this role. |



<a id="chatto-api-v1-AdminMemberRoleSummary"></a>

### AdminMemberRoleSummary

Role summary used by member list filters and badges.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Stable role name. |
| `display_name` | `string` | Display name shown in admin UI. |



<a id="chatto-api-v1-RoomTimelineAssetUrl"></a>

### RoomTimelineAssetUrl

Time-limited URL for a room timeline asset.

Clients should expect these URLs to expire and refresh the timeline data when
an asset URL is no longer usable.

| Field | Type | Description |
| --- | --- | --- |
| `url` | `string` | Signed asset URL. |
| `expires_at` | `google.protobuf.Timestamp` | Time when the signed URL expires. |



<a id="chatto-api-v1-RoomTimelineAttachment"></a>

### RoomTimelineAttachment

Attachment metadata included with a posted message.

Image and video dimensions are best-effort metadata for layout. Asset URLs can
be absent while processing is pending or when the source is unavailable.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable attachment ID. |
| `filename` | `string` | Original filename. |
| `content_type` | `string` | MIME content type. |
| `width` | `int32` | Image or video width in pixels, when known. |
| `height` | `int32` | Image or video height in pixels, when known. |
| `asset_url` | [`RoomTimelineAssetUrl`](#chatto-api-v1-RoomTimelineAssetUrl) | Signed URL for the original asset, when available. |
| `thumbnail_asset_url` | [`RoomTimelineAssetUrl`](#chatto-api-v1-RoomTimelineAssetUrl) | Signed URL for a thumbnail image, when available. |
| `video_processing` | [`RoomTimelineVideoProcessing`](#chatto-api-v1-RoomTimelineVideoProcessing) | Video-specific processing metadata, when this attachment is a video. |



<a id="chatto-api-v1-RoomTimelineEvent"></a>

### RoomTimelineEvent

One event in a room or thread timeline.

Clients should inspect the event oneof to choose the renderer for the event.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable event ID. |
| `created_at` | `google.protobuf.Timestamp` | Time when the event was created. |
| `actor_id` | `string` | User ID of the event actor. |
| `event.message_posted` | [`RoomTimelineMessagePosted`](#chatto-api-v1-RoomTimelineMessagePosted) | A message was posted. |
| `event.room_created` | [`RoomTimelineRoomEvent`](#chatto-api-v1-RoomTimelineRoomEvent) | The room was created. |
| `event.room_updated` | [`RoomTimelineRoomEvent`](#chatto-api-v1-RoomTimelineRoomEvent) | The room metadata was updated. |
| `event.room_deleted` | [`RoomTimelineRoomEvent`](#chatto-api-v1-RoomTimelineRoomEvent) | The room was deleted. |
| `event.room_archived` | [`RoomTimelineRoomEvent`](#chatto-api-v1-RoomTimelineRoomEvent) | The room was archived. |
| `event.room_unarchived` | [`RoomTimelineRoomEvent`](#chatto-api-v1-RoomTimelineRoomEvent) | The room was unarchived. |
| `event.user_joined_room` | [`RoomTimelineRoomEvent`](#chatto-api-v1-RoomTimelineRoomEvent) | A user joined the room. |
| `event.user_left_room` | [`RoomTimelineRoomEvent`](#chatto-api-v1-RoomTimelineRoomEvent) | A user left the room. |



<a id="chatto-api-v1-RoomTimelineIncludes"></a>

### RoomTimelineIncludes

Related entities included beside timeline events.

Includes avoid repeating the same user data on every event in a page.

| Field | Type | Description |
| --- | --- | --- |
| `users` | map&lt;`string`, [`RoomTimelineUser`](#chatto-api-v1-RoomTimelineUser)&gt; | Users keyed by user ID. |



<a id="chatto-api-v1-RoomTimelineLinkPreview"></a>

### RoomTimelineLinkPreview

Link preview metadata extracted from a message body.

Clients should treat all fields as optional because preview extraction depends
on the linked site and the embed provider.

| Field | Type | Description |
| --- | --- | --- |
| `url` | `string` | Previewed URL. |
| `title` | `string` | Page or embed title. |
| `description` | `string` | Page or embed description. |
| `site_name` | `string` | Site name, when known. |
| `image_url` | `string` | Preview image URL, when available. |
| `embed_type` | `string` | Embed provider or type, when recognized. |
| `embed_id` | `string` | Provider-specific embed ID, when recognized. |



<a id="chatto-api-v1-RoomTimelineMessagePosted"></a>

### RoomTimelineMessagePosted

Payload for a message-posted timeline event.

The same shape is used for top-level room messages, thread replies, and thread
echo entries. Thread-related fields let clients render reply counts, thread
participants, and follow state without additional per-message requests.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room containing the message. |
| `body` | `optional string` | Message body text, when available. A present empty string is distinct from an absent body. |
| `attachments` | repeated [`RoomTimelineAttachment`](#chatto-api-v1-RoomTimelineAttachment) | Attachments sent with the message. |
| `link_preview` | [`RoomTimelineLinkPreview`](#chatto-api-v1-RoomTimelineLinkPreview) | Link preview extracted for the message, when available. |
| `updated_at` | `google.protobuf.Timestamp` | Time when the message was last edited. |
| `in_reply_to` | `string` | Event ID this message directly replies to, when this is a reply. |
| `thread_root_event_id` | `string` | Event ID of the root message for the thread this message belongs to. |
| `echo_of_event_id` | `string` | Event ID this event echoes into the current view, when applicable. Echoes allow thread activity to appear in another timeline context. |
| `echo_from_thread_root_event_id` | `string` | Thread root ID of the echoed event, when applicable. |
| `channel_echo_event_id` | `string` | Channel timeline event ID for a thread echo, when applicable. |
| `reply_count` | `int32` | Number of replies in this message's thread. |
| `last_reply_at` | `google.protobuf.Timestamp` | Creation time of the most recent reply in this message's thread. |
| `thread_participant_user_ids` | `repeated string` | User IDs that have participated in this message's thread. |
| `viewer_is_following_thread` | `optional bool` | Whether the current user follows this message's thread, when known. |
| `reactions` | repeated [`RoomTimelineReactionSummary`](#chatto-api-v1-RoomTimelineReactionSummary) | Reaction summaries for this message. |



<a id="chatto-api-v1-RoomTimelinePage"></a>

### RoomTimelinePage

Cursor page of room or thread timeline events.

Use opaque start_cursor and end_cursor values with before/after requests to
continue paging in either direction. Clients must treat cursor values as
server-owned tokens and must not parse or construct them. The has_older and
has_newer flags tell clients whether another request can extend the current
window.

| Field | Type | Description |
| --- | --- | --- |
| `events` | repeated [`RoomTimelineEvent`](#chatto-api-v1-RoomTimelineEvent) | Events in display order. |
| `start_cursor` | `string` | Opaque cursor for the first event in the page. |
| `end_cursor` | `string` | Opaque cursor for the last event in the page. |
| `has_older` | `bool` | True when older events are available before start_cursor. |
| `has_newer` | `bool` | True when newer events are available after end_cursor. |
| `includes` | [`RoomTimelineIncludes`](#chatto-api-v1-RoomTimelineIncludes) | Related entities needed to render the page. |



<a id="chatto-api-v1-RoomTimelineReactionSummary"></a>

### RoomTimelineReactionSummary

Aggregated reaction state for one emoji on one event.

The summary is scoped to the current event and includes whether the current
user has reacted with the same emoji.

| Field | Type | Description |
| --- | --- | --- |
| `emoji` | `string` | Emoji or reaction key. |
| `count` | `int32` | Number of users who reacted with this emoji. |
| `has_reacted` | `bool` | True when the current user reacted with this emoji. |
| `user_ids` | `repeated string` | User IDs that reacted with this emoji. |



<a id="chatto-api-v1-RoomTimelineRoomEvent"></a>

### RoomTimelineRoomEvent

Payload for room lifecycle and membership timeline events.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room affected by the event. |



<a id="chatto-api-v1-RoomTimelineUser"></a>

### RoomTimelineUser

User data included with room timeline events.

Timeline events reference users by ID; clients can resolve those IDs through
the includes block returned with each page.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable user ID. |
| `login` | `string` | Login name. |
| `display_name` | `string` | Display name, when set. |
| `deleted` | `bool` | True when the user account has been deleted. |
| `avatar_url` | `string` | Avatar image URL, when available. |



<a id="chatto-api-v1-RoomTimelineVideoProcessing"></a>

### RoomTimelineVideoProcessing

Processing metadata for a video attachment.

Clients can use this object to show upload/transcoding progress and decide
whether to play a processed variant, show a thumbnail, or display a failure
state.

| Field | Type | Description |
| --- | --- | --- |
| `status` | [`RoomTimelineVideoProcessingStatus`](#chatto-api-v1-RoomTimelineVideoProcessingStatus) | Current processing status. |
| `duration_ms` | `int64` | Video duration in milliseconds. |
| `width` | `int32` | Source video width in pixels. |
| `height` | `int32` | Source video height in pixels. |
| `source_available` | `bool` | True when the original source asset is currently available. |
| `reason_code` | `string` | Stable reason code for a failed or incomplete processing state. |
| `thumbnail_asset_url` | [`RoomTimelineAssetUrl`](#chatto-api-v1-RoomTimelineAssetUrl) | Signed URL for the generated thumbnail, when available. |
| `variants` | repeated [`RoomTimelineVideoVariant`](#chatto-api-v1-RoomTimelineVideoVariant) | Available transcoded renditions. |



<a id="chatto-api-v1-RoomTimelineVideoVariant"></a>

### RoomTimelineVideoVariant

One transcoded video rendition.

| Field | Type | Description |
| --- | --- | --- |
| `quality` | `string` | Quality label for the rendition. |
| `width` | `int32` | Video width in pixels. |
| `height` | `int32` | Video height in pixels. |
| `size` | `int64` | Rendition size in bytes. |
| `asset_url` | [`RoomTimelineAssetUrl`](#chatto-api-v1-RoomTimelineAssetUrl) | Signed URL for the rendition. |



<a id="chatto-api-v1-AttachmentThumbnailOptions"></a>

### AttachmentThumbnailOptions

Thumbnail transform parameters for attachment URL reads.

| Field | Type | Description |
| --- | --- | --- |
| `width` | `int32` | Thumbnail width in pixels. |
| `height` | `int32` | Thumbnail height in pixels. |
| `fit` | [`AttachmentFitMode`](#chatto-api-v1-AttachmentFitMode) | Thumbnail fit mode. |



<a id="chatto-api-v1-RefreshedAttachmentUrls"></a>

### RefreshedAttachmentUrls

Fresh signed URLs for one message attachment.

| Field | Type | Description |
| --- | --- | --- |
| `attachment_id` | `string` | Attachment ID these URLs belong to. |
| `asset_url` | [`RoomTimelineAssetUrl`](#chatto-api-v1-RoomTimelineAssetUrl) | Fresh signed URL for the original attachment. |
| `thumbnail_asset_url` | [`RoomTimelineAssetUrl`](#chatto-api-v1-RoomTimelineAssetUrl) | Fresh signed transformed thumbnail URL for the attachment. |
| `video_thumbnail_asset_url` | [`RoomTimelineAssetUrl`](#chatto-api-v1-RoomTimelineAssetUrl) | Fresh signed URL for the processed video thumbnail, when present. |
| `variants` | repeated [`RoomTimelineVideoVariant`](#chatto-api-v1-RoomTimelineVideoVariant) | Fresh signed URLs for processed video variants, when present. |



<a id="chatto-api-v1-RoomAttachmentListItem"></a>

### RoomAttachmentListItem

One current room attachment and its message anchor.

| Field | Type | Description |
| --- | --- | --- |
| `attachment` | [`RoomTimelineAttachment`](#chatto-api-v1-RoomTimelineAttachment) | Attachment metadata and signed URLs. |
| `message_event_id` | `string` | Message event containing the attachment. |
| `thread_root_event_id` | `string` | Thread root event ID when the containing message is a thread reply. |
| `created_at` | `google.protobuf.Timestamp` | Message creation timestamp. |



<a id="chatto-api-v1-FetchedLinkPreview"></a>

### FetchedLinkPreview

Link preview metadata returned to clients before posting a message.

| Field | Type | Description |
| --- | --- | --- |
| `url` | `string` | Previewed URL. |
| `title` | `string` | Page or embed title. |
| `description` | `string` | Page or embed description. |
| `image_url` | `string` | Preview image URL, when available. |
| `image_asset_id` | `string` | Existing server asset ID for the preview image, when available. |
| `site_name` | `string` | Site name, when known. |
| `embed_type` | `string` | Embed provider or type, when recognized. |
| `embed_id` | `string` | Provider-specific embed ID, when recognized. |



<a id="chatto-api-v1-MentionConfirmationChallenge"></a>

### MentionConfirmationChallenge

Large mention confirmation details for a message that was not posted.

| Field | Type | Description |
| --- | --- | --- |
| `recipient_count` | `int32` | Number of people the message would notify. |
| `token` | `string` | Short-lived token to send back with the same message after user approval. |



<a id="chatto-api-v1-MessageAttachmentUpload"></a>

### MessageAttachmentUpload

Attachment bytes submitted with a message post.

Each attachment is uploaded as a room-scoped asset before the message is
committed. The message service validates per-file limits and permissions.

| Field | Type | Description |
| --- | --- | --- |
| `content` | `bytes` | Raw file bytes. |
| `filename` | `string` | Original filename supplied by the client. |
| `content_type` | `string` | MIME content type supplied by the client. Empty values are treated as application/octet-stream. |



<a id="chatto-api-v1-MessageLinkPreviewInput"></a>

### MessageLinkPreviewInput

Link preview metadata submitted with a posted message.

The server validates and stores this data with the message body. Clients
should omit this field when no preview was selected.

| Field | Type | Description |
| --- | --- | --- |
| `url` | `string` | Previewed URL. |
| `title` | `string` | Page or embed title. |
| `description` | `string` | Page or embed description. |
| `site_name` | `string` | Site name, when known. |
| `image_asset_id` | `string` | Existing server asset ID for the preview image, when available. |
| `embed_type` | `string` | Embed provider or type, when recognized. |
| `embed_id` | `string` | Provider-specific embed ID, when recognized. |



<a id="chatto-api-v1-DirectMessageNotification"></a>

### DirectMessageNotification

Direct-message notification payload.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | DM room where the message was posted. |
| `event_id` | `string` | Message event ID. |



<a id="chatto-api-v1-MentionNotification"></a>

### MentionNotification

Mention notification payload.

| Field | Type | Description |
| --- | --- | --- |
| `room` | [`NotificationRoom`](#chatto-api-v1-NotificationRoom) | Room where the mention occurred. |
| `event_id` | `string` | Message event ID. |
| `thread_root_event_id` | `optional string` | Thread root event ID when the mention happened inside a thread. |



<a id="chatto-api-v1-NotificationActor"></a>

### NotificationActor

User summary embedded in notification responses.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable user ID. |
| `login` | `string` | Login identifier. |
| `display_name` | `string` | Display name shown in Chatto. |
| `deleted` | `bool` | True when this is a tombstone/deleted user representation. |
| `avatar_url` | `optional string` | Optional avatar URL. |
| `presence_status` | [`PresenceStatus`](#chatto-api-v1-PresenceStatus) | Current live presence status. |
| `custom_status` | [`CustomUserStatus`](#chatto-api-v1-CustomUserStatus) | Custom profile status, when set. |



<a id="chatto-api-v1-NotificationItem"></a>

### NotificationItem

One pending notification for the authenticated viewer.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable notification ID. |
| `created_at` | `google.protobuf.Timestamp` | Creation time. |
| `actor` | [`NotificationActor`](#chatto-api-v1-NotificationActor) | User who triggered the notification, when still resolvable. |
| `summary` | `string` | Short human-readable notification summary. |
| `kind.direct_message` | [`DirectMessageNotification`](#chatto-api-v1-DirectMessageNotification) | Direct-message notification. |
| `kind.mention` | [`MentionNotification`](#chatto-api-v1-MentionNotification) | Mention notification. |
| `kind.reply` | [`ReplyNotification`](#chatto-api-v1-ReplyNotification) | Reply notification. |
| `kind.room_message` | [`RoomMessageNotification`](#chatto-api-v1-RoomMessageNotification) | All-messages room notification. |



<a id="chatto-api-v1-NotificationRoom"></a>

### NotificationRoom

Room summary embedded in notification responses.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable room ID. |
| `name` | `string` | Room display name. Empty for DM rooms. |



<a id="chatto-api-v1-ReplyNotification"></a>

### ReplyNotification

Reply notification payload.

| Field | Type | Description |
| --- | --- | --- |
| `room` | [`NotificationRoom`](#chatto-api-v1-NotificationRoom) | Room where the reply occurred. |
| `event_id` | `string` | Reply event ID. |
| `in_reply_to_id` | `string` | Event ID of the message being replied to. |
| `thread_root_event_id` | `optional string` | Thread root event ID when the reply happened inside a thread. |



<a id="chatto-api-v1-RoomMessageNotification"></a>

### RoomMessageNotification

All-messages room notification payload.

| Field | Type | Description |
| --- | --- | --- |
| `room` | [`NotificationRoom`](#chatto-api-v1-NotificationRoom) | Room where the message was posted. |
| `event_id` | `string` | Message event ID. |



<a id="chatto-api-v1-RoomNotificationCount"></a>

### RoomNotificationCount

Pending notification count for one room.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room ID. |
| `total_count` | `int32` | Pending notification count for the authenticated viewer in this room. |



<a id="chatto-api-v1-PermissionExplanation"></a>

### PermissionExplanation

Full decision trace for one permission and target user.

| Field | Type | Description |
| --- | --- | --- |
| `permission` | `string` | Permission identifier. |
| `state` | [`PermissionDecision`](#chatto-api-v1-PermissionDecision) | Overall decision after applying deny-wins resolution. |
| `decided_at` | [`PermissionDecisionLevel`](#chatto-api-v1-PermissionDecisionLevel) | Level of the winning decision, when state is not NONE. |
| `decided_by_role` | `string` | Role name or policy marker that produced the winning decision. |
| `trace` | repeated [`PermissionTraceEntry`](#chatto-api-v1-PermissionTraceEntry) | Ordered decision trace. The first entry is the winning decision. |



<a id="chatto-api-v1-PermissionMatrixCell"></a>

### PermissionMatrixCell

Matrix cell for one permission and scope.

| Field | Type | Description |
| --- | --- | --- |
| `permission` | `string` | Permission identifier. |
| `scope_id` | `string` | Scope ID matching PermissionMatrixScope.id. |
| `override` | [`PermissionDecision`](#chatto-api-v1-PermissionDecision) | Explicit decision at this scope. |
| `effective` | [`PermissionDecision`](#chatto-api-v1-PermissionDecision) | Effective decision at this scope. |



<a id="chatto-api-v1-PermissionMatrixScope"></a>

### PermissionMatrixScope

Matrix column scope.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable scope ID for the server, a group, or a room. |
| `label` | `string` | Human-readable label. |
| `kind` | [`PermissionScopeKind`](#chatto-api-v1-PermissionScopeKind) | Scope tier. |
| `parent_group_id` | `string` | Parent group ID for room scopes. |



<a id="chatto-api-v1-PermissionScope"></a>

### PermissionScope

Permission scope reference.

| Field | Type | Description |
| --- | --- | --- |
| `kind` | [`PermissionScopeKind`](#chatto-api-v1-PermissionScopeKind) | Scope tier. |
| `id` | `string` | Scope ID for group or room scope. Empty for server scope. |



<a id="chatto-api-v1-RolePermissionMatrix"></a>

### RolePermissionMatrix

Permission matrix for one role across all scopes.

| Field | Type | Description |
| --- | --- | --- |
| `role_name` | `string` | Role name. |
| `applicable_permissions` | `repeated string` | Permissions rendered as rows. |
| `scopes` | repeated [`PermissionMatrixScope`](#chatto-api-v1-PermissionMatrixScope) | Scopes rendered as columns. |
| `cells` | repeated [`PermissionMatrixCell`](#chatto-api-v1-PermissionMatrixCell) | Sparse matrix cells. |



<a id="chatto-api-v1-TierPermissions"></a>

### TierPermissions

Permission grants and denials at a single tier.

| Field | Type | Description |
| --- | --- | --- |
| `permissions` | `repeated string` | Explicitly granted permissions. |
| `permission_denials` | `repeated string` | Explicitly denied permissions. |



<a id="chatto-api-v1-TierRole"></a>

### TierRole

One role's permission state at a single tier.

| Field | Type | Description |
| --- | --- | --- |
| `role_name` | `string` | Stable role name. |
| `display_name` | `string` | Display name. |
| `description` | `string` | Optional role description. |
| `is_system` | `bool` | Whether this is a built-in role. |
| `position` | `int32` | Display/order position. |
| `override` | [`TierPermissions`](#chatto-api-v1-TierPermissions) | Explicit state at this tier. |
| `inherited_allows` | `repeated string` | Allows inherited from broader tiers. |
| `inherited_denials` | `repeated string` | Denials inherited from broader tiers. |



<a id="chatto-api-v1-TierRoles"></a>

### TierRoles

Role permission matrix for one tier.

| Field | Type | Description |
| --- | --- | --- |
| `applicable_permissions` | `repeated string` | Permissions configurable at this tier. |
| `roles` | repeated [`TierRole`](#chatto-api-v1-TierRole) | Roles sorted by position. |



<a id="chatto-api-v1-UserPermissionMatrix"></a>

### UserPermissionMatrix

Permission matrix for one user across all scopes.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | User ID. |
| `applicable_permissions` | `repeated string` | Permissions rendered as rows. |
| `scopes` | repeated [`PermissionMatrixScope`](#chatto-api-v1-PermissionMatrixScope) | Scopes rendered as columns. |
| `cells` | repeated [`PermissionMatrixCell`](#chatto-api-v1-PermissionMatrixCell) | Sparse matrix cells. |



<a id="chatto-api-v1-RealtimeAssetDeletedEvent"></a>

### RealtimeAssetDeletedEvent

Asset-deleted signal.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `optional string` | Room containing the message that owned the asset, when known. |
| `asset_id` | `string` | Asset ID. |



<a id="chatto-api-v1-RealtimeAssetProcessingEvent"></a>

### RealtimeAssetProcessingEvent

Asset-processing signal.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `optional string` | Room containing the message that owns the asset, when known. |
| `asset_id` | `string` | Asset ID. |
| `message_event_id` | `optional string` | Event ID of the message that owns the asset, when known. |



<a id="chatto-api-v1-RealtimeCallEvent"></a>

### RealtimeCallEvent

Voice-call state signal.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room containing the call. |
| `call_id` | `string` | Call ID. |
| `source` | [`RealtimeCallEventSource`](#chatto-api-v1-RealtimeCallEventSource) | Source that produced the call transition. |



<a id="chatto-api-v1-RealtimeClientFrame"></a>

### RealtimeClientFrame

Client-to-server frame for Chatto's protobuf WebSocket realtime protocol.

Clients send binary protobuf frames to `/api/realtime`. The first frame must
be `hello`; after the server replies with `hello`, clients send
`subscribe_events` to start the authenticated live event stream.

| Field | Type | Description |
| --- | --- | --- |
| `frame.hello` | [`RealtimeClientHello`](#chatto-api-v1-RealtimeClientHello) | Opens the protocol session and optionally carries a bearer token. |
| `frame.subscribe_events` | [`RealtimeSubscribeEvents`](#chatto-api-v1-RealtimeSubscribeEvents) | Starts the caller's authorized live-event stream. |
| `frame.ping` | [`RealtimePing`](#chatto-api-v1-RealtimePing) | Application-level ping. The server replies with `pong`. |



<a id="chatto-api-v1-RealtimeClientHello"></a>

### RealtimeClientHello

Initial client hello.

| Field | Type | Description |
| --- | --- | --- |
| `protocol_version` | `uint32` | Protocol version requested by the client. The current version is 1. |
| `bearer_token` | `optional string` | Optional bearer token. When present, it takes precedence over cookie auth. |



<a id="chatto-api-v1-RealtimeClose"></a>

### RealtimeClose

Close instruction sent before the socket is closed when possible.

| Field | Type | Description |
| --- | --- | --- |
| `code` | `string` | Stable machine-readable close code. |
| `message` | `string` | Human-readable diagnostic message. |
| `reconnect` | `bool` | True when the client should reconnect after a delay. |
| `retry_after_ms` | `uint32` | Suggested reconnect delay in milliseconds. |



<a id="chatto-api-v1-RealtimeError"></a>

### RealtimeError

Protocol error.

| Field | Type | Description |
| --- | --- | --- |
| `code` | `string` | Stable machine-readable error code. |
| `message` | `string` | Human-readable diagnostic message. |
| `fatal` | `bool` | True when the server will close the socket after sending this error. |



<a id="chatto-api-v1-RealtimeEventEnvelope"></a>

### RealtimeEventEnvelope

One authorized live event delivered over the realtime WebSocket.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable event ID. |
| `created_at` | `google.protobuf.Timestamp` | Time the event was created. |
| `actor_id` | `optional string` | User or system actor that caused the event, when known. |
| `event.message_posted` | [`RealtimeMessagePostedEvent`](#chatto-api-v1-RealtimeMessagePostedEvent) | A message was posted in a room or thread. |
| `event.message_edited` | [`RealtimeMessageEditedEvent`](#chatto-api-v1-RealtimeMessageEditedEvent) | A message body or renderable metadata changed. |
| `event.message_retracted` | [`RealtimeMessageRetractedEvent`](#chatto-api-v1-RealtimeMessageRetractedEvent) | A message was retracted. |
| `event.reaction_added` | [`RealtimeReactionEvent`](#chatto-api-v1-RealtimeReactionEvent) | A user added a reaction to a message. |
| `event.reaction_removed` | [`RealtimeReactionEvent`](#chatto-api-v1-RealtimeReactionEvent) | A user removed a reaction from a message. |
| `event.user_typing` | [`RealtimeTypingEvent`](#chatto-api-v1-RealtimeTypingEvent) | A user is typing in a room or thread. |
| `event.presence_changed` | [`RealtimePresenceChangedEvent`](#chatto-api-v1-RealtimePresenceChangedEvent) | A user's live presence changed. |
| `event.room_created` | [`RealtimeRoomEvent`](#chatto-api-v1-RealtimeRoomEvent) | A room was created. |
| `event.room_updated` | [`RealtimeRoomEvent`](#chatto-api-v1-RealtimeRoomEvent) | A room's profile or settings changed. |
| `event.room_deleted` | [`RealtimeRoomEvent`](#chatto-api-v1-RealtimeRoomEvent) | A room was deleted. |
| `event.room_archived` | [`RealtimeRoomEvent`](#chatto-api-v1-RealtimeRoomEvent) | A room was archived. |
| `event.room_unarchived` | [`RealtimeRoomEvent`](#chatto-api-v1-RealtimeRoomEvent) | A room was restored from the archive. |
| `event.user_joined_room` | [`RealtimeRoomEvent`](#chatto-api-v1-RealtimeRoomEvent) | A user joined a room visible to the current user. |
| `event.user_left_room` | [`RealtimeRoomEvent`](#chatto-api-v1-RealtimeRoomEvent) | A user left a room visible to the current user. |
| `event.room_universal_changed` | [`RealtimeRoomUniversalChangedEvent`](#chatto-api-v1-RealtimeRoomUniversalChangedEvent) | A room's universal visibility flag changed. |
| `event.notification_created` | [`RealtimeNotificationCreatedEvent`](#chatto-api-v1-RealtimeNotificationCreatedEvent) | A notification was created for the current user. |
| `event.notification_dismissed` | [`RealtimeNotificationDismissedEvent`](#chatto-api-v1-RealtimeNotificationDismissedEvent) | A notification was dismissed for the current user. |
| `event.notification_level_changed` | [`RealtimeNotificationLevelChangedEvent`](#chatto-api-v1-RealtimeNotificationLevelChangedEvent) | The current user's notification level changed. |
| `event.thread_follow_changed` | [`RealtimeThreadFollowChangedEvent`](#chatto-api-v1-RealtimeThreadFollowChangedEvent) | The current user's thread follow state changed. |
| `event.room_marked_as_read` | [`RealtimeRoomMarkedAsReadEvent`](#chatto-api-v1-RealtimeRoomMarkedAsReadEvent) | The current user's room read state changed. |
| `event.thread_created` | [`RealtimeThreadCreatedEvent`](#chatto-api-v1-RealtimeThreadCreatedEvent) | A thread was created in a room visible to the current user. |
| `event.server_updated` | [`RealtimeServerUpdatedEvent`](#chatto-api-v1-RealtimeServerUpdatedEvent) | The server profile/config changed. |
| `event.user_profile_updated` | [`RealtimeUserProfileUpdatedEvent`](#chatto-api-v1-RealtimeUserProfileUpdatedEvent) | A user profile changed. |
| `event.user_custom_status_set` | [`RealtimeUserCustomStatusSetEvent`](#chatto-api-v1-RealtimeUserCustomStatusSetEvent) | A user's custom status was set or changed. |
| `event.user_custom_status_cleared` | [`RealtimeUserCustomStatusClearedEvent`](#chatto-api-v1-RealtimeUserCustomStatusClearedEvent) | A user's custom status was cleared. |
| `event.server_user_preferences_updated` | [`RealtimeServerUserPreferencesUpdatedEvent`](#chatto-api-v1-RealtimeServerUserPreferencesUpdatedEvent) | The current user's display preferences changed. |
| `event.room_groups_updated` | [`RealtimeRoomGroupsUpdatedEvent`](#chatto-api-v1-RealtimeRoomGroupsUpdatedEvent) | Room-group layout changed. |
| `event.server_member_deleted` | [`RealtimeServerMemberDeletedEvent`](#chatto-api-v1-RealtimeServerMemberDeletedEvent) | A server member was deleted. |
| `event.asset_processing_started` | [`RealtimeAssetProcessingEvent`](#chatto-api-v1-RealtimeAssetProcessingEvent) | Asset processing started for a message attachment. |
| `event.asset_processing_succeeded` | [`RealtimeAssetProcessingEvent`](#chatto-api-v1-RealtimeAssetProcessingEvent) | Asset processing completed successfully for a message attachment. |
| `event.asset_processing_failed` | [`RealtimeAssetProcessingEvent`](#chatto-api-v1-RealtimeAssetProcessingEvent) | Asset processing failed for a message attachment. |
| `event.asset_deleted` | [`RealtimeAssetDeletedEvent`](#chatto-api-v1-RealtimeAssetDeletedEvent) | An asset attached to a message was deleted. |
| `event.call_started` | [`RealtimeCallEvent`](#chatto-api-v1-RealtimeCallEvent) | A voice call started in a room. |
| `event.call_participant_joined` | [`RealtimeCallEvent`](#chatto-api-v1-RealtimeCallEvent) | A participant joined a voice call. |
| `event.call_participant_left` | [`RealtimeCallEvent`](#chatto-api-v1-RealtimeCallEvent) | A participant left a voice call. |
| `event.call_ended` | [`RealtimeCallEvent`](#chatto-api-v1-RealtimeCallEvent) | A voice call ended. |
| `event.mention_notification` | [`RealtimeMentionNotificationEvent`](#chatto-api-v1-RealtimeMentionNotificationEvent) | The current user was mentioned in a room. |
| `event.new_direct_message_notification` | [`RealtimeNewDirectMessageNotificationEvent`](#chatto-api-v1-RealtimeNewDirectMessageNotificationEvent) | The current user received a new direct message. |
| `event.session_terminated` | [`RealtimeSessionTerminatedEvent`](#chatto-api-v1-RealtimeSessionTerminatedEvent) | The current user's session was terminated. |



<a id="chatto-api-v1-RealtimeHeartbeat"></a>

### RealtimeHeartbeat

Application-level heartbeat.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable event ID for this heartbeat. |
| `created_at` | `google.protobuf.Timestamp` | Time the heartbeat was emitted. |



<a id="chatto-api-v1-RealtimeMentionNotificationEvent"></a>

### RealtimeMentionNotificationEvent

Mention attention signal for the connected user.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room where the mention occurred. |
| `actor_user_id` | `string` | Author user ID. |
| `room_name` | `optional string` | Display name of the room where the mention occurred, when hydrated for the caller. |
| `actor_display_name` | `optional string` | Display name of the author who mentioned the connected user, when hydrated. |



<a id="chatto-api-v1-RealtimeMessageEditedEvent"></a>

### RealtimeMessageEditedEvent

Message-edited signal.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room containing the message. |
| `message_event_id` | `string` | Event ID of the edited message. |



<a id="chatto-api-v1-RealtimeMessagePostedEvent"></a>

### RealtimeMessagePostedEvent

Message-posted signal.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room containing the message. |
| `message_event_id` | `string` | Event ID of the posted message. |
| `thread_root_event_id` | `optional string` | Root message event ID when the message was posted in a thread. |



<a id="chatto-api-v1-RealtimeMessageRetractedEvent"></a>

### RealtimeMessageRetractedEvent

Message-retracted signal.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room containing the message. |
| `message_event_id` | `string` | Event ID of the retracted message. |
| `reason` | `optional string` | Retraction reason, when one was supplied. |



<a id="chatto-api-v1-RealtimeNewDirectMessageNotificationEvent"></a>

### RealtimeNewDirectMessageNotificationEvent

New-DM attention signal for the connected user.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | DM room ID. |
| `sender_id` | `string` | Sender user ID. |
| `sender_display_name` | `optional string` | Display name of the sender, when hydrated. |
| `sender_avatar_url` | `optional string` | Avatar URL of the sender, when one is available. |
| `conversation_name` | `optional string` | Display name for the DM conversation from the connected user's perspective, when hydrated. |



<a id="chatto-api-v1-RealtimeNotificationCreatedEvent"></a>

### RealtimeNotificationCreatedEvent

Notification-created signal for the connected user.

| Field | Type | Description |
| --- | --- | --- |
| `notification_id` | `string` | Notification ID. |
| `room_id` | `optional string` | Room associated with the notification, when the notification navigates to a room. |
| `event_id` | `optional string` | Event ID associated with the notification, when the notification navigates to an event. |
| `in_reply_to_id` | `optional string` | Parent event ID for reply notifications, when known. |
| `silent` | `bool` | True when the notification should not alert. |



<a id="chatto-api-v1-RealtimeNotificationDismissedEvent"></a>

### RealtimeNotificationDismissedEvent

Notification-dismissed signal for the connected user.

| Field | Type | Description |
| --- | --- | --- |
| `notification_id` | `string` | Notification ID. |



<a id="chatto-api-v1-RealtimeNotificationLevelChangedEvent"></a>

### RealtimeNotificationLevelChangedEvent

Notification-level signal for the connected user.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room whose notification level changed. |
| `level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Explicit room notification level. |
| `effective_level` | [`NotificationLevel`](#chatto-api-v1-NotificationLevel) | Effective notification level after defaults are applied. |



<a id="chatto-api-v1-RealtimePing"></a>

### RealtimePing

Application-level ping.

| Field | Type | Description |
| --- | --- | --- |
| `nonce` | `string` | Client-chosen opaque value echoed in the pong. |



<a id="chatto-api-v1-RealtimePong"></a>

### RealtimePong

Application-level pong.

| Field | Type | Description |
| --- | --- | --- |
| `nonce` | `string` | Echo of the ping nonce. |



<a id="chatto-api-v1-RealtimePresenceChangedEvent"></a>

### RealtimePresenceChangedEvent

Presence-changed signal.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | User whose presence changed. |
| `status` | [`RealtimePresenceStatus`](#chatto-api-v1-RealtimePresenceStatus) | Latest presence status. |



<a id="chatto-api-v1-RealtimeReactionEvent"></a>

### RealtimeReactionEvent

Reaction signal.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room containing the reacted-to message. |
| `message_event_id` | `string` | Event ID of the reacted-to message. |
| `emoji` | `string` | Reaction emoji. |



<a id="chatto-api-v1-RealtimeRoomEvent"></a>

### RealtimeRoomEvent

Room lifecycle or membership signal.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room affected by the event. |



<a id="chatto-api-v1-RealtimeRoomGroupsUpdatedEvent"></a>

### RealtimeRoomGroupsUpdatedEvent

Room-group layout signal.

| Field | Type | Description |
| --- | --- | --- |
| `changed` | `bool` | Always true; present for event-bus compatibility. |



<a id="chatto-api-v1-RealtimeRoomMarkedAsReadEvent"></a>

### RealtimeRoomMarkedAsReadEvent

Room-read signal for the connected user.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room marked read. |



<a id="chatto-api-v1-RealtimeRoomUniversalChangedEvent"></a>

### RealtimeRoomUniversalChangedEvent

Room universal-visibility signal.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room whose universal setting changed. |
| `universal` | `bool` | Latest universal setting. |



<a id="chatto-api-v1-RealtimeServerFrame"></a>

### RealtimeServerFrame

Server-to-client frame for Chatto's protobuf WebSocket realtime protocol.

| Field | Type | Description |
| --- | --- | --- |
| `frame.hello` | [`RealtimeServerHello`](#chatto-api-v1-RealtimeServerHello) | Confirms protocol version, server version, and capabilities. |
| `frame.subscribed` | [`RealtimeSubscribed`](#chatto-api-v1-RealtimeSubscribed) | Confirms the live-event stream has started. |
| `frame.event` | [`RealtimeEventEnvelope`](#chatto-api-v1-RealtimeEventEnvelope) | A live event authorized for the connected user. |
| `frame.heartbeat` | [`RealtimeHeartbeat`](#chatto-api-v1-RealtimeHeartbeat) | Application-level heartbeat used for liveness checks. |
| `frame.error` | [`RealtimeError`](#chatto-api-v1-RealtimeError) | Protocol or authorization error. |
| `frame.close` | [`RealtimeClose`](#chatto-api-v1-RealtimeClose) | Server-requested close with reconnect guidance. |
| `frame.pong` | [`RealtimePong`](#chatto-api-v1-RealtimePong) | Reply to a client ping. |



<a id="chatto-api-v1-RealtimeServerHello"></a>

### RealtimeServerHello

Initial server hello.

| Field | Type | Description |
| --- | --- | --- |
| `protocol_version` | `uint32` | Protocol version accepted by the server. |
| `server_version` | `string` | Chatto server software version. |
| `heartbeat_interval_seconds` | `uint32` | Approximate heartbeat interval clients should expect. |



<a id="chatto-api-v1-RealtimeServerMemberDeletedEvent"></a>

### RealtimeServerMemberDeletedEvent

Server-member deleted signal.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Deleted user ID. |



<a id="chatto-api-v1-RealtimeServerUpdatedEvent"></a>

### RealtimeServerUpdatedEvent

Server-profile signal.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Server display name. |
| `description` | `string` | Server description. |
| `logo_url` | `optional string` | Server logo URL, when configured. |
| `banner_url` | `optional string` | Server banner URL, when configured. |



<a id="chatto-api-v1-RealtimeServerUserPreferencesUpdatedEvent"></a>

### RealtimeServerUserPreferencesUpdatedEvent

User display-preferences signal for the connected user.

| Field | Type | Description |
| --- | --- | --- |
| `timezone` | `optional string` | IANA timezone name, when the user has selected a fixed timezone. |
| `time_format` | [`RealtimeTimeFormat`](#chatto-api-v1-RealtimeTimeFormat) | Preferred time display format. |



<a id="chatto-api-v1-RealtimeSessionTerminatedEvent"></a>

### RealtimeSessionTerminatedEvent

Session-terminated signal for the connected user's current session.

| Field | Type | Description |
| --- | --- | --- |
| `reason` | `string` | Termination reason. |



<a id="chatto-api-v1-RealtimeSubscribeEvents"></a>

### RealtimeSubscribeEvents

Request to start the caller's authorized live event stream.



<a id="chatto-api-v1-RealtimeSubscribed"></a>

### RealtimeSubscribed

Confirms event streaming has started.



<a id="chatto-api-v1-RealtimeThreadCreatedEvent"></a>

### RealtimeThreadCreatedEvent

Thread-created signal.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room containing the thread. |
| `thread_root_event_id` | `string` | Root message event ID that identifies the thread. |



<a id="chatto-api-v1-RealtimeThreadFollowChangedEvent"></a>

### RealtimeThreadFollowChangedEvent

Thread-follow signal for the connected user.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room containing the thread. |
| `thread_root_event_id` | `string` | Root message event ID of the thread. |
| `following` | `bool` | True when the user now follows the thread. |



<a id="chatto-api-v1-RealtimeTypingEvent"></a>

### RealtimeTypingEvent

Typing signal.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room where the actor is typing. |
| `thread_root_event_id` | `optional string` | Thread root event ID when the actor is typing in a thread. |



<a id="chatto-api-v1-RealtimeUserCustomStatusClearedEvent"></a>

### RealtimeUserCustomStatusClearedEvent

User-custom-status cleared signal.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | User whose status was cleared. |



<a id="chatto-api-v1-RealtimeUserCustomStatusSetEvent"></a>

### RealtimeUserCustomStatusSetEvent

User-custom-status set signal.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | User whose status changed. |
| `emoji` | `string` | Emoji part of the status. |
| `text` | `string` | Text part of the status. |
| `expires_at` | `google.protobuf.Timestamp` | Expiration time, when the status expires automatically. |



<a id="chatto-api-v1-RealtimeUserProfileUpdatedEvent"></a>

### RealtimeUserProfileUpdatedEvent

User-profile signal.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | User whose profile changed. |
| `login` | `string` | Login name. |
| `display_name` | `string` | Display name. |
| `avatar_url` | `optional string` | Avatar URL, when the user has an avatar. |



<a id="chatto-api-v1-Role"></a>

### Role

Server role metadata and server-scope permission state.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Stable role name used in permission and assignment records. |
| `display_name` | `string` | Display name shown in admin and mention UIs. |
| `description` | `string` | Optional role description. |
| `permissions` | `repeated string` | Server permissions granted by this role. |
| `permission_denials` | `repeated string` | Server permissions denied by this role. |
| `is_system` | `bool` | Whether this is a built-in role. |
| `position` | `int32` | Display/order position. |
| `pingable` | `bool` | Whether messages may notify users assigned to this role. |



<a id="chatto-api-v1-RoleUser"></a>

### RoleUser

User summary for role roster displays.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable user ID. |
| `login` | `string` | Login identifier. |
| `display_name` | `string` | Display name shown in Chatto. |



<a id="chatto-api-v1-AuthProvider"></a>

### AuthProvider

Describes one login provider exposed by the server discovery endpoint.

Clients can render these providers before a user has authenticated.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable provider identifier used by clients when starting login. |
| `type` | `string` | Provider category, such as "password" or "oidc". Clients should treat unknown provider types as unsupported. |
| `label` | `string` | Human-readable provider label for login UI. |
| `login_url` | `string` | URL that starts login for this provider. |



<a id="chatto-api-v1-ServerProfile"></a>

### ServerProfile

Authenticated server profile and branding fields.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Display name of the Chatto server. |
| `logo_url` | `optional string` | Optional server logo URL. |
| `banner_url` | `optional string` | Optional server banner URL. |
| `welcome_message` | `optional string` | Optional welcome message. |
| `motd` | `optional string` | Optional message of the day. |
| `description` | `optional string` | Optional server description. |



<a id="chatto-api-v1-ServerViewerCapabilities"></a>

### ServerViewerCapabilities

Permission-derived server capabilities for the authenticated user.

| Field | Type | Description |
| --- | --- | --- |
| `has_any_admin_permission` | `bool` | Whether the user has at least one server-admin permission. |
| `can_manage_server` | `bool` | Whether the user may manage server configuration. |
| `can_create_room` | `bool` | Whether the user may create a channel room at server scope. |
| `can_manage_rooms` | `bool` | Whether the user may manage at least one room. |
| `has_unread_rooms` | `bool` | Whether any joined channel room has unread messages. |



<a id="chatto-api-v1-FollowedThread"></a>

### FollowedThread

One followed thread for the current user.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room containing the thread. |
| `room_name` | `string` | Current display name of the room. |
| `thread_root_event_id` | `string` | Event ID of the root message for the thread. |
| `root_message` | [`RoomTimelineEvent`](#chatto-api-v1-RoomTimelineEvent) | Renderable root message event, when the root is still visible. |
| `reply_count` | `int32` | Number of replies in the thread. |
| `last_reply_at` | `google.protobuf.Timestamp` | Creation time of the latest reply, when the thread has replies. |
| `has_unread` | `bool` | True when the current user has unread replies in the thread. |



<a id="chatto-api-v1-UserAvatarOptions"></a>

### UserAvatarOptions

Avatar transform parameters for user profile reads.

| Field | Type | Description |
| --- | --- | --- |
| `width` | `int32` | Avatar width in pixels. |
| `height` | `int32` | Avatar height in pixels. |
| `fit` | [`UserAvatarFitMode`](#chatto-api-v1-UserAvatarFitMode) | Avatar crop/fit behavior. |



<a id="chatto-api-v1-UserSummary"></a>

### UserSummary

Lightweight public user data for cache-backed render surfaces.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable user ID. |
| `login` | `string` | Login name. |
| `display_name` | `string` | Display name, when set. |
| `deleted` | `bool` | True when the user account has been deleted. |
| `avatar_url` | `string` | Avatar image URL, when available. |



<a id="chatto-api-v1-CallParticipant"></a>

### CallParticipant

User currently participating in a room call.

| Field | Type | Description |
| --- | --- | --- |
| `user` | [`CallParticipantUser`](#chatto-api-v1-CallParticipantUser) | Participant user. |
| `joined_at` | `google.protobuf.Timestamp` | When the user joined this call. |
| `call_id` | `string` | Active call session ID. |



<a id="chatto-api-v1-CallParticipantUser"></a>

### CallParticipantUser

Avatar-display data for a call participant.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable user ID. |
| `login` | `string` | Login identifier. |
| `display_name` | `string` | Display name shown in Chatto. |
| `deleted` | `bool` | Whether the user account was deleted. |
| `avatar_url` | `optional string` | Optional avatar URL. |
| `presence_status` | [`PresenceStatus`](#chatto-api-v1-PresenceStatus) | Current live presence status. |
| `custom_status` | [`CustomUserStatus`](#chatto-api-v1-CustomUserStatus) | Custom profile status, when set. |




## Enums

<a id="chatto-api-v1-NotificationLevel"></a>

### NotificationLevel

Notification delivery level for a room.

| Name | Number | Description |
| --- | --- | --- |
| `NOTIFICATION_LEVEL_UNSPECIFIED` | `0` | The level was not specified. |
| `NOTIFICATION_LEVEL_DEFAULT` | `1` | Use the inherited default for the room. |
| `NOTIFICATION_LEVEL_MUTED` | `2` | Do not notify for this room. |
| `NOTIFICATION_LEVEL_NORMAL` | `3` | Notify according to the normal room rules. |
| `NOTIFICATION_LEVEL_ALL_MESSAGES` | `4` | Notify for every message in the room. |


<a id="chatto-api-v1-PresenceStatus"></a>

### PresenceStatus

Live presence status returned by public read APIs.

Offline is a read-side state only. Clients cannot report Offline through
PresenceService.ReportPresence; they should stop reporting presence and let
the server's live presence record expire.

| Name | Number | Description |
| --- | --- | --- |
| `PRESENCE_STATUS_UNSPECIFIED` | `0` | No presence status was specified. |
| `PRESENCE_STATUS_ONLINE` | `1` | The user is actively available. |
| `PRESENCE_STATUS_AWAY` | `2` | The user is connected but away or idle. |
| `PRESENCE_STATUS_DO_NOT_DISTURB` | `3` | The user does not want notifications while this live status is active. |
| `PRESENCE_STATUS_OFFLINE` | `4` | The user has no active live presence record. |


<a id="chatto-api-v1-TimeFormat"></a>

### TimeFormat

User preference for rendering times in clients.

| Name | Number | Description |
| --- | --- | --- |
| `TIME_FORMAT_UNSPECIFIED` | `0` | No explicit preference was stored. |
| `TIME_FORMAT_AUTO` | `1` | Let the client choose based on locale and browser settings. |
| `TIME_FORMAT_12_HOUR` | `2` | Render times using a 12-hour clock. |
| `TIME_FORMAT_24_HOUR` | `3` | Render times using a 24-hour clock. |


<a id="chatto-api-v1-RoomKind"></a>

### RoomKind

Kind of room represented by the public API.

| Name | Number | Description |
| --- | --- | --- |
| `ROOM_KIND_UNSPECIFIED` | `0` | The room kind was not specified. |
| `ROOM_KIND_CHANNEL` | `1` | A regular channel governed by server and room permissions. |
| `ROOM_KIND_DM` | `2` | A direct-message conversation between members. |


<a id="chatto-api-v1-RoomDirectoryScope"></a>

### RoomDirectoryScope

Room kinds to include in directory responses.

| Name | Number | Description |
| --- | --- | --- |
| `ROOM_DIRECTORY_SCOPE_UNSPECIFIED` | `0` | Include both visible channel rooms and the caller's active DM rooms. |
| `ROOM_DIRECTORY_SCOPE_ALL` | `1` | Include both visible channel rooms and the caller's active DM rooms. |
| `ROOM_DIRECTORY_SCOPE_CHANNELS` | `2` | Include visible channel rooms only. |
| `ROOM_DIRECTORY_SCOPE_DMS` | `3` | Include the caller's active DM rooms only. |


<a id="chatto-api-v1-AdminRoomLayoutItemKind"></a>

### AdminRoomLayoutItemKind

Sidebar item kind used in reorder requests.

| Name | Number | Description |
| --- | --- | --- |
| `ADMIN_ROOM_LAYOUT_ITEM_KIND_UNSPECIFIED` | `0` | The item kind was not specified. |
| `ADMIN_ROOM_LAYOUT_ITEM_KIND_ROOM` | `1` | Channel room entry. |
| `ADMIN_ROOM_LAYOUT_ITEM_KIND_SIDEBAR_LINK` | `2` | Sidebar link entry. |


<a id="chatto-api-v1-RoomTimelineVideoProcessingStatus"></a>

### RoomTimelineVideoProcessingStatus

Processing state for a timeline video attachment.

| Name | Number | Description |
| --- | --- | --- |
| `ROOM_TIMELINE_VIDEO_PROCESSING_STATUS_UNSPECIFIED` | `0` | The processing status was not specified. |
| `ROOM_TIMELINE_VIDEO_PROCESSING_STATUS_PROCESSING` | `1` | Video processing is still running. |
| `ROOM_TIMELINE_VIDEO_PROCESSING_STATUS_COMPLETED` | `2` | Video processing completed successfully. |
| `ROOM_TIMELINE_VIDEO_PROCESSING_STATUS_FAILED` | `3` | Video processing failed. |


<a id="chatto-api-v1-AttachmentFitMode"></a>

### AttachmentFitMode

Fit mode used when generating transformed attachment thumbnails.

| Name | Number | Description |
| --- | --- | --- |
| `ATTACHMENT_FIT_MODE_UNSPECIFIED` | `0` | The fit mode was not specified. |
| `ATTACHMENT_FIT_MODE_CONTAIN` | `1` | Preserve the whole source image within the requested bounds. |
| `ATTACHMENT_FIT_MODE_COVER` | `2` | Fill the requested bounds, cropping when needed. |


<a id="chatto-api-v1-PermissionDecision"></a>

### PermissionDecision

Trinary permission decision used by permission matrices and write requests.

| Name | Number | Description |
| --- | --- | --- |
| `PERMISSION_DECISION_UNSPECIFIED` | `0` | Unspecified decision; rejected for writes. |
| `PERMISSION_DECISION_ALLOW` | `1` | Explicit allow. |
| `PERMISSION_DECISION_DENY` | `2` | Explicit deny. |
| `PERMISSION_DECISION_NONE` | `3` | No explicit decision; clears the permission state on writes. |


<a id="chatto-api-v1-PermissionDecisionLevel"></a>

### PermissionDecisionLevel

Level at which a permission explanation found a decision.

| Name | Number | Description |
| --- | --- | --- |
| `PERMISSION_DECISION_LEVEL_UNSPECIFIED` | `0` | No decision level was reached. |
| `PERMISSION_DECISION_LEVEL_SERVER` | `1` | Server-level role or user decision. |
| `PERMISSION_DECISION_LEVEL_GROUP` | `2` | Room-group-level role or user decision. |
| `PERMISSION_DECISION_LEVEL_ROOM` | `3` | Room-level role or user decision. |


<a id="chatto-api-v1-PermissionScopeKind"></a>

### PermissionScopeKind

Scope tier for permission matrices and write requests.

| Name | Number | Description |
| --- | --- | --- |
| `PERMISSION_SCOPE_KIND_UNSPECIFIED` | `0` | Unspecified scope; treated as server scope where older clients omit it. |
| `PERMISSION_SCOPE_KIND_SERVER` | `1` | Server tier. |
| `PERMISSION_SCOPE_KIND_GROUP` | `2` | Room-group tier. |
| `PERMISSION_SCOPE_KIND_ROOM` | `3` | Room tier. |


<a id="chatto-api-v1-RealtimeCallEventSource"></a>

### RealtimeCallEventSource

Source that produced a realtime voice-call transition.

| Name | Number | Description |
| --- | --- | --- |
| `REALTIME_CALL_EVENT_SOURCE_UNSPECIFIED` | `0` | No call event source was specified. |
| `REALTIME_CALL_EVENT_SOURCE_USER` | `1` | The user explicitly requested the transition through Chatto. |
| `REALTIME_CALL_EVENT_SOURCE_LIVEKIT` | `2` | LiveKit reported the transition through a webhook. |
| `REALTIME_CALL_EVENT_SOURCE_RECONCILIATION` | `3` | Chatto reconciled its local state against LiveKit's current room state. |


<a id="chatto-api-v1-RealtimePresenceStatus"></a>

### RealtimePresenceStatus

Presence status delivered by the realtime stream.

| Name | Number | Description |
| --- | --- | --- |
| `REALTIME_PRESENCE_STATUS_UNSPECIFIED` | `0` | No presence status was specified. |
| `REALTIME_PRESENCE_STATUS_OFFLINE` | `1` | The user is offline. |
| `REALTIME_PRESENCE_STATUS_ONLINE` | `2` | The user is actively available. |
| `REALTIME_PRESENCE_STATUS_AWAY` | `3` | The user is connected but away or idle. |
| `REALTIME_PRESENCE_STATUS_DO_NOT_DISTURB` | `4` | The user does not want notifications while this live status is active. |


<a id="chatto-api-v1-RealtimeTimeFormat"></a>

### RealtimeTimeFormat

Time-display format used by realtime preference events.

| Name | Number | Description |
| --- | --- | --- |
| `REALTIME_TIME_FORMAT_UNSPECIFIED` | `0` | Use browser or locale default. |
| `REALTIME_TIME_FORMAT_12H` | `1` | 12-hour clock. |
| `REALTIME_TIME_FORMAT_24H` | `2` | 24-hour clock. |


<a id="chatto-api-v1-UserAvatarFitMode"></a>

### UserAvatarFitMode

Fit mode used when generating transformed user avatar URLs.

| Name | Number | Description |
| --- | --- | --- |
| `USER_AVATAR_FIT_MODE_UNSPECIFIED` | `0` | The fit mode was not specified. |
| `USER_AVATAR_FIT_MODE_CONTAIN` | `1` | Preserve the whole source image within the requested bounds. |
| `USER_AVATAR_FIT_MODE_COVER` | `2` | Fill the requested bounds, cropping when needed. |


