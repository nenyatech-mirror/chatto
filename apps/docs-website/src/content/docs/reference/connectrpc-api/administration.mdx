---
title: Administration
description: Server administration, room layout, users, roles, permissions, diagnostics, and audit RPCs.
editUrl: false
---

{/* Generated from proto/chatto/api/v1/*.proto. Do not edit directly. */}

Chatto exposes these administration services below `/api/connect`.



Shared message and enum definitions are documented in [Shared Types And Enums](/reference/connectrpc-api/types/).



<a id="chatto-api-v1-ServerService"></a>

## ServerService

Provides public server discovery metadata.

<a id="chatto-api-v1-ServerService-GetServer"></a>

### GetServer

Returns public server metadata, branding, registration status, and login
provider information. This RPC is available before login so clients can
render the first server screen and authentication choices.

```http
POST /api/connect/chatto.api.v1.ServerService/GetServer
```

<a id="chatto-api-v1-GetServerRequest"></a>

#### Input: GetServerRequest

Request for public server metadata.


<a id="chatto-api-v1-GetServerResponse"></a>

#### Result: GetServerResponse

Public metadata clients need before they can authenticate or render the
initial server view.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Display name of the Chatto server. |
| `version` | `string` | Server software version. |
| `auth_methods` | `repeated string` | Enabled legacy authentication method identifiers. |
| `auth_providers` | repeated [`AuthProvider`](/reference/connectrpc-api/types/#chatto-api-v1-AuthProvider) | Configured login providers. |
| `registration_open` | `bool` | Whether users can create accounts through the public UI. |
| `authorize_url` | `string` | URL for the legacy authorization flow, when enabled. |
| `welcome_message` | `optional string` | Optional welcome message configured by the server administrator. |
| `description` | `optional string` | Optional server description configured by the server administrator. |
| `logo_url` | `optional string` | Optional server logo URL suitable for branding the server in client UI. |
| `banner_url` | `optional string` | Optional server banner URL suitable for larger server branding surfaces. |

<a id="chatto-api-v1-ServerStateService"></a>

## ServerStateService

Provides authenticated server state and current user's server capabilities.

<a id="chatto-api-v1-ServerStateService-GetServerState"></a>

### GetServerState

Returns authenticated server state. This RPC requires a logged-in user;
public discovery remains available through ServerService.GetServer.

```http
POST /api/connect/chatto.api.v1.ServerStateService/GetServerState
```

<a id="chatto-api-v1-GetServerStateRequest"></a>

#### Input: GetServerStateRequest

Request for authenticated server state.


<a id="chatto-api-v1-GetServerStateResponse"></a>

#### Result: GetServerStateResponse

Authenticated server state used by the web client and public API clients.

| Field | Type | Description |
| --- | --- | --- |
| `profile` | [`ServerProfile`](/reference/connectrpc-api/types/#chatto-api-v1-ServerProfile) | Server profile and branding. |
| `push_notifications_enabled` | `bool` | Whether Web Push notifications are fully configured. |
| `vapid_public_key` | `optional string` | Optional VAPID public key for Web Push registration. |
| `livekit_url` | `optional string` | Optional LiveKit URL for voice calls. |
| `direct_registration_enabled` | `bool` | Whether direct email/password registration is enabled. |
| `video_processing_enabled` | `bool` | Whether video processing is enabled. |
| `max_upload_size` | `int64` | Maximum general upload size in bytes. |
| `max_video_upload_size` | `int64` | Maximum video upload size in bytes. |
| `message_edit_window_seconds` | `int32` | Message edit window in seconds. |
| `viewer_capabilities` | [`ServerViewerCapabilities`](/reference/connectrpc-api/types/#chatto-api-v1-ServerViewerCapabilities) | Permission-derived state for the authenticated user. |


<a id="chatto-api-v1-ServerStateService-UpdateServerConfig"></a>

### UpdateServerConfig

Updates runtime-editable server profile settings. This RPC requires
server.manage.

```http
POST /api/connect/chatto.api.v1.ServerStateService/UpdateServerConfig
```

<a id="chatto-api-v1-UpdateServerConfigRequest"></a>

#### Input: UpdateServerConfigRequest

Request to update runtime-editable server profile settings.

| Field | Type | Description |
| --- | --- | --- |
| `server_name` | `optional string` | Optional server name for page titles. Set to empty string to use default. |
| `description` | `optional string` | Optional short server description for OG link-preview metadata. Set to empty string to clear. |
| `motd` | `optional string` | Optional message of the day for the header. Set to empty string to clear. |
| `welcome_message` | `optional string` | Optional welcome message shown on the login page. Set to empty string to clear. |


<a id="chatto-api-v1-UpdateServerConfigResponse"></a>

#### Result: UpdateServerConfigResponse

Result of updating runtime-editable server profile settings.

| Field | Type | Description |
| --- | --- | --- |
| `profile` | [`ServerProfile`](/reference/connectrpc-api/types/#chatto-api-v1-ServerProfile) | Updated server profile and branding. |


<a id="chatto-api-v1-ServerStateService-UploadServerLogo"></a>

### UploadServerLogo

Uploads and sets the public server logo. This RPC requires server.manage.

```http
POST /api/connect/chatto.api.v1.ServerStateService/UploadServerLogo
```

<a id="chatto-api-v1-UploadServerLogoRequest"></a>

#### Input: UploadServerLogoRequest

Request to upload and set the public server logo.

| Field | Type | Description |
| --- | --- | --- |
| `image` | `bytes` | Raw image bytes. The server validates, resizes, and stores a WebP logo. |
| `filename` | `string` | Original browser filename, for diagnostics and future compatibility. |
| `content_type` | `string` | Browser-provided content type, for diagnostics and future compatibility. |


<a id="chatto-api-v1-UploadServerLogoResponse"></a>

#### Result: UploadServerLogoResponse

Result of uploading the public server logo.

| Field | Type | Description |
| --- | --- | --- |
| `profile` | [`ServerProfile`](/reference/connectrpc-api/types/#chatto-api-v1-ServerProfile) | Updated server profile and branding. |


<a id="chatto-api-v1-ServerStateService-DeleteServerLogo"></a>

### DeleteServerLogo

Clears the public server logo. This RPC requires server.manage.

```http
POST /api/connect/chatto.api.v1.ServerStateService/DeleteServerLogo
```

<a id="chatto-api-v1-DeleteServerLogoRequest"></a>

#### Input: DeleteServerLogoRequest

Request to clear the public server logo.


<a id="chatto-api-v1-DeleteServerLogoResponse"></a>

#### Result: DeleteServerLogoResponse

Result of clearing the public server logo.

| Field | Type | Description |
| --- | --- | --- |
| `profile` | [`ServerProfile`](/reference/connectrpc-api/types/#chatto-api-v1-ServerProfile) | Updated server profile and branding. |


<a id="chatto-api-v1-ServerStateService-UploadServerBanner"></a>

### UploadServerBanner

Uploads and sets the public server banner. This RPC requires server.manage.

```http
POST /api/connect/chatto.api.v1.ServerStateService/UploadServerBanner
```

<a id="chatto-api-v1-UploadServerBannerRequest"></a>

#### Input: UploadServerBannerRequest

Request to upload and set the public server banner.

| Field | Type | Description |
| --- | --- | --- |
| `image` | `bytes` | Raw image bytes. The server validates, resizes, and stores a WebP banner. |
| `filename` | `string` | Original browser filename, for diagnostics and future compatibility. |
| `content_type` | `string` | Browser-provided content type, for diagnostics and future compatibility. |


<a id="chatto-api-v1-UploadServerBannerResponse"></a>

#### Result: UploadServerBannerResponse

Result of uploading the public server banner.

| Field | Type | Description |
| --- | --- | --- |
| `profile` | [`ServerProfile`](/reference/connectrpc-api/types/#chatto-api-v1-ServerProfile) | Updated server profile and branding. |


<a id="chatto-api-v1-ServerStateService-DeleteServerBanner"></a>

### DeleteServerBanner

Clears the public server banner. This RPC requires server.manage.

```http
POST /api/connect/chatto.api.v1.ServerStateService/DeleteServerBanner
```

<a id="chatto-api-v1-DeleteServerBannerRequest"></a>

#### Input: DeleteServerBannerRequest

Request to clear the public server banner.


<a id="chatto-api-v1-DeleteServerBannerResponse"></a>

#### Result: DeleteServerBannerResponse

Result of clearing the public server banner.

| Field | Type | Description |
| --- | --- | --- |
| `profile` | [`ServerProfile`](/reference/connectrpc-api/types/#chatto-api-v1-ServerProfile) | Updated server profile and branding. |


<a id="chatto-api-v1-ServerStateService-GetServerSecurityConfig"></a>

### GetServerSecurityConfig

Returns security-sensitive server configuration. This RPC requires
server.manage.

```http
POST /api/connect/chatto.api.v1.ServerStateService/GetServerSecurityConfig
```

<a id="chatto-api-v1-GetServerSecurityConfigRequest"></a>

#### Input: GetServerSecurityConfigRequest

Request for server security configuration.


<a id="chatto-api-v1-GetServerSecurityConfigResponse"></a>

#### Result: GetServerSecurityConfigResponse

Security-sensitive server configuration. The caller must be allowed to
manage server configuration.

| Field | Type | Description |
| --- | --- | --- |
| `blocked_usernames` | `string` | Newline-separated blocked usernames. |


<a id="chatto-api-v1-ServerStateService-UpdateBlockedUsernames"></a>

### UpdateBlockedUsernames

Updates the blocked-username list. This RPC requires server.manage.

```http
POST /api/connect/chatto.api.v1.ServerStateService/UpdateBlockedUsernames
```

<a id="chatto-api-v1-UpdateBlockedUsernamesRequest"></a>

#### Input: UpdateBlockedUsernamesRequest

Request to update the server blocked-username list.

| Field | Type | Description |
| --- | --- | --- |
| `blocked_usernames` | `string` | Newline-separated blocked usernames. |


<a id="chatto-api-v1-UpdateBlockedUsernamesResponse"></a>

#### Result: UpdateBlockedUsernamesResponse

Result of updating the server blocked-username list.

| Field | Type | Description |
| --- | --- | --- |
| `blocked_usernames` | `string` | Effective newline-separated blocked usernames after the update. |

<a id="chatto-api-v1-AdminRoomLayoutService"></a>

## AdminRoomLayoutService

Reads and mutates the server-admin channel room layout.

<a id="chatto-api-v1-AdminRoomLayoutService-ListAdminRoomLayout"></a>

### ListAdminRoomLayout

Lists the current room groups, rooms, and sidebar links for the admin
layout editor.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/ListAdminRoomLayout
```

<a id="chatto-api-v1-ListAdminRoomLayoutRequest"></a>

#### Input: ListAdminRoomLayoutRequest

Request to read the admin room layout.


<a id="chatto-api-v1-ListAdminRoomLayoutResponse"></a>

#### Result: ListAdminRoomLayoutResponse

Current admin room layout.

| Field | Type | Description |
| --- | --- | --- |
| `groups` | repeated [`AdminRoomLayoutGroup`](/reference/connectrpc-api/types/#chatto-api-v1-AdminRoomLayoutGroup) | Room groups in sidebar order. |


<a id="chatto-api-v1-AdminRoomLayoutService-CreateRoomGroup"></a>

### CreateRoomGroup

Creates a room group. The caller must be allowed to manage roles.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/CreateRoomGroup
```

<a id="chatto-api-v1-CreateRoomGroupRequest"></a>

#### Input: CreateRoomGroupRequest

Request to create a room group.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Required group display name. |
| `description` | `string` | Optional operator-facing description. |


<a id="chatto-api-v1-CreateRoomGroupResponse"></a>

#### Result: CreateRoomGroupResponse

Result of creating a room group.

| Field | Type | Description |
| --- | --- | --- |
| `group` | [`AdminRoomLayoutGroup`](/reference/connectrpc-api/types/#chatto-api-v1-AdminRoomLayoutGroup) | Created room group. |


<a id="chatto-api-v1-AdminRoomLayoutService-UpdateRoomGroup"></a>

### UpdateRoomGroup

Updates a room group's metadata. The caller must be allowed to manage
roles.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/UpdateRoomGroup
```

<a id="chatto-api-v1-UpdateRoomGroupRequest"></a>

#### Input: UpdateRoomGroupRequest

Request to update a room group.

| Field | Type | Description |
| --- | --- | --- |
| `group_id` | `string` | Required room group ID. |
| `name` | `string` | Required group display name. |
| `description` | `string` | Optional operator-facing description. |


<a id="chatto-api-v1-UpdateRoomGroupResponse"></a>

#### Result: UpdateRoomGroupResponse

Result of updating a room group.

| Field | Type | Description |
| --- | --- | --- |
| `group` | [`AdminRoomLayoutGroup`](/reference/connectrpc-api/types/#chatto-api-v1-AdminRoomLayoutGroup) | Updated room group. |


<a id="chatto-api-v1-AdminRoomLayoutService-DeleteRoomGroup"></a>

### DeleteRoomGroup

Deletes an empty room group. The caller must be allowed to manage roles.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/DeleteRoomGroup
```

<a id="chatto-api-v1-DeleteRoomGroupRequest"></a>

#### Input: DeleteRoomGroupRequest

Request to delete an empty room group.

| Field | Type | Description |
| --- | --- | --- |
| `group_id` | `string` | Required room group ID. |


<a id="chatto-api-v1-DeleteRoomGroupResponse"></a>

#### Result: DeleteRoomGroupResponse

Result of deleting a room group.

| Field | Type | Description |
| --- | --- | --- |
| `deleted` | `bool` | True when the room group was deleted. |


<a id="chatto-api-v1-AdminRoomLayoutService-ReorderRoomGroups"></a>

### ReorderRoomGroups

Replaces the global room group order. The caller must be allowed to manage
roles.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/ReorderRoomGroups
```

<a id="chatto-api-v1-ReorderRoomGroupsRequest"></a>

#### Input: ReorderRoomGroupsRequest

Request to replace the global room group order.

| Field | Type | Description |
| --- | --- | --- |
| `ordered_group_ids` | `repeated string` | Ordered room group IDs. The list must include every existing group exactly once. |


<a id="chatto-api-v1-ReorderRoomGroupsResponse"></a>

#### Result: ReorderRoomGroupsResponse

Result of replacing the global room group order.

| Field | Type | Description |
| --- | --- | --- |
| `groups` | repeated [`AdminRoomLayoutGroup`](/reference/connectrpc-api/types/#chatto-api-v1-AdminRoomLayoutGroup) | Room groups in their new order. |


<a id="chatto-api-v1-AdminRoomLayoutService-MoveRoomToGroup"></a>

### MoveRoomToGroup

Moves a channel room to another room group. The caller must be allowed to
manage rooms in both source and destination groups.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/MoveRoomToGroup
```

<a id="chatto-api-v1-MoveRoomToGroupRequest"></a>

#### Input: MoveRoomToGroupRequest

Request to move a channel room to another room group.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required channel room ID. |
| `group_id` | `string` | Required destination room group ID. |


<a id="chatto-api-v1-MoveRoomToGroupResponse"></a>

#### Result: MoveRoomToGroupResponse

Result of moving a channel room.

| Field | Type | Description |
| --- | --- | --- |
| `room` | [`Room`](/reference/connectrpc-api/types/#chatto-api-v1-Room) | Moved room after the operation. |


<a id="chatto-api-v1-AdminRoomLayoutService-ReorderSidebarItemsInGroup"></a>

### ReorderSidebarItemsInGroup

Replaces the mixed room/link sidebar order in one group. The caller must
be allowed to manage rooms in that group.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/ReorderSidebarItemsInGroup
```

<a id="chatto-api-v1-ReorderSidebarItemsInGroupRequest"></a>

#### Input: ReorderSidebarItemsInGroupRequest

Request to replace the mixed sidebar item order in one group.

| Field | Type | Description |
| --- | --- | --- |
| `group_id` | `string` | Required room group ID. |
| `items` | repeated [`AdminRoomLayoutItemInput`](/reference/connectrpc-api/types/#chatto-api-v1-AdminRoomLayoutItemInput) | Ordered room/sidebar-link item references. |


<a id="chatto-api-v1-ReorderSidebarItemsInGroupResponse"></a>

#### Result: ReorderSidebarItemsInGroupResponse

Result of replacing the mixed sidebar item order.

| Field | Type | Description |
| --- | --- | --- |
| `group` | [`AdminRoomLayoutGroup`](/reference/connectrpc-api/types/#chatto-api-v1-AdminRoomLayoutGroup) | Updated room group. |


<a id="chatto-api-v1-AdminRoomLayoutService-CreateSidebarLink"></a>

### CreateSidebarLink

Creates a sidebar link. The caller must be allowed to manage rooms in the
destination group.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/CreateSidebarLink
```

<a id="chatto-api-v1-CreateSidebarLinkRequest"></a>

#### Input: CreateSidebarLinkRequest

Request to create a sidebar link in one room group.

| Field | Type | Description |
| --- | --- | --- |
| `group_id` | `string` | Required destination room group ID. |
| `label` | `string` | Required display label. |
| `url` | `string` | Required absolute http(s) URL or server-local path. |


<a id="chatto-api-v1-CreateSidebarLinkResponse"></a>

#### Result: CreateSidebarLinkResponse

Result of creating a sidebar link.

| Field | Type | Description |
| --- | --- | --- |
| `sidebar_link` | [`SidebarLink`](/reference/connectrpc-api/types/#chatto-api-v1-SidebarLink) | Created sidebar link. |


<a id="chatto-api-v1-AdminRoomLayoutService-UpdateSidebarLink"></a>

### UpdateSidebarLink

Updates a sidebar link. The caller must be allowed to manage rooms in the
link's current group.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/UpdateSidebarLink
```

<a id="chatto-api-v1-UpdateSidebarLinkRequest"></a>

#### Input: UpdateSidebarLinkRequest

Request to update a sidebar link.

| Field | Type | Description |
| --- | --- | --- |
| `link_id` | `string` | Required sidebar link ID. |
| `label` | `string` | Required display label. |
| `url` | `string` | Required absolute http(s) URL or server-local path. |


<a id="chatto-api-v1-UpdateSidebarLinkResponse"></a>

#### Result: UpdateSidebarLinkResponse

Result of updating a sidebar link.

| Field | Type | Description |
| --- | --- | --- |
| `sidebar_link` | [`SidebarLink`](/reference/connectrpc-api/types/#chatto-api-v1-SidebarLink) | Updated sidebar link. |


<a id="chatto-api-v1-AdminRoomLayoutService-DeleteSidebarLink"></a>

### DeleteSidebarLink

Deletes a sidebar link. The caller must be allowed to manage rooms in the
link's current group.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/DeleteSidebarLink
```

<a id="chatto-api-v1-DeleteSidebarLinkRequest"></a>

#### Input: DeleteSidebarLinkRequest

Request to delete a sidebar link.

| Field | Type | Description |
| --- | --- | --- |
| `link_id` | `string` | Required sidebar link ID. |


<a id="chatto-api-v1-DeleteSidebarLinkResponse"></a>

#### Result: DeleteSidebarLinkResponse

Result of deleting a sidebar link.

| Field | Type | Description |
| --- | --- | --- |
| `deleted` | `bool` | True when the sidebar link was deleted. |


<a id="chatto-api-v1-AdminRoomLayoutService-MoveSidebarLinkToGroup"></a>

### MoveSidebarLinkToGroup

Moves a sidebar link to another group. The caller must be allowed to manage
rooms in both source and destination groups.

```http
POST /api/connect/chatto.api.v1.AdminRoomLayoutService/MoveSidebarLinkToGroup
```

<a id="chatto-api-v1-MoveSidebarLinkToGroupRequest"></a>

#### Input: MoveSidebarLinkToGroupRequest

Request to move a sidebar link to another room group.

| Field | Type | Description |
| --- | --- | --- |
| `link_id` | `string` | Required sidebar link ID. |
| `group_id` | `string` | Required destination room group ID. |


<a id="chatto-api-v1-MoveSidebarLinkToGroupResponse"></a>

#### Result: MoveSidebarLinkToGroupResponse

Result of moving a sidebar link.

| Field | Type | Description |
| --- | --- | --- |
| `sidebar_link` | [`SidebarLink`](/reference/connectrpc-api/types/#chatto-api-v1-SidebarLink) | Moved sidebar link. |

<a id="chatto-api-v1-AdminUserManagementService"></a>

## AdminUserManagementService

Server-admin user identity and cooldown commands.

<a id="chatto-api-v1-AdminUserManagementService-ListMembers"></a>

### ListMembers

Lists server members for the admin members screen.

```http
POST /api/connect/chatto.api.v1.AdminUserManagementService/ListMembers
```

<a id="chatto-api-v1-ListMembersRequest"></a>

#### Input: ListMembersRequest

Request server-admin member rows.

| Field | Type | Description |
| --- | --- | --- |
| `search` | `string` | Case-insensitive login/display-name search term. Empty disables search. |
| `page` | [`PageRequest`](/reference/connectrpc-api/types/#chatto-api-v1-PageRequest) | Page request. Defaults to 20 results when absent or limit is zero. |


<a id="chatto-api-v1-ListMembersResponse"></a>

#### Result: ListMembersResponse

Server-admin member rows plus role summaries.

| Field | Type | Description |
| --- | --- | --- |
| `users` | repeated [`AdminMember`](/reference/connectrpc-api/types/#chatto-api-v1-AdminMember) | Matching users. |
| `roles` | repeated [`AdminMemberRoleSummary`](/reference/connectrpc-api/types/#chatto-api-v1-AdminMemberRoleSummary) | Server roles for display-name lookup. |
| `page` | [`PageInfo`](/reference/connectrpc-api/types/#chatto-api-v1-PageInfo) | Page metadata. |


<a id="chatto-api-v1-AdminUserManagementService-GetMember"></a>

### GetMember

Gets one server member plus role/permission metadata for admin details.
Returns NOT_FOUND when the user does not exist.

```http
POST /api/connect/chatto.api.v1.AdminUserManagementService/GetMember
```

<a id="chatto-api-v1-GetMemberRequest"></a>

#### Input: GetMemberRequest

Request one server-admin member detail record.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Target user ID. |


<a id="chatto-api-v1-GetMemberResponse"></a>

#### Result: GetMemberResponse

Server-admin member detail payload.

| Field | Type | Description |
| --- | --- | --- |
| `member` | [`AdminMember`](/reference/connectrpc-api/types/#chatto-api-v1-AdminMember) | Member record. |
| `roles` | repeated [`AdminMemberRole`](/reference/connectrpc-api/types/#chatto-api-v1-AdminMemberRole) | Server roles for assignment UI. |
| `available_permissions` | `repeated string` | Server permissions available for per-user overrides. |
| `viewer_can_assign_roles` | `bool` | Whether the caller may assign/revoke roles. |
| `viewer_can_manage_roles` | `bool` | Whether the caller may edit role definitions. |
| `viewer_can_manage_user_permissions` | `bool` | Whether the caller may edit direct user permissions. |


<a id="chatto-api-v1-AdminUserManagementService-AssignRole"></a>

### AssignRole

Assigns a server role to a user.

```http
POST /api/connect/chatto.api.v1.AdminUserManagementService/AssignRole
```

<a id="chatto-api-v1-AssignRoleRequest"></a>

#### Input: AssignRoleRequest

Request to assign a server role to a user.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Target user ID. |
| `role_name` | `string` | Role name to assign. |


<a id="chatto-api-v1-AssignRoleResponse"></a>

#### Result: AssignRoleResponse

Result of assigning a server role.

| Field | Type | Description |
| --- | --- | --- |
| `assigned` | `bool` | True when the request completed. |


<a id="chatto-api-v1-AdminUserManagementService-RevokeRole"></a>

### RevokeRole

Revokes a server role from a user.

```http
POST /api/connect/chatto.api.v1.AdminUserManagementService/RevokeRole
```

<a id="chatto-api-v1-RevokeRoleRequest"></a>

#### Input: RevokeRoleRequest

Request to revoke a server role from a user.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Target user ID. |
| `role_name` | `string` | Role name to revoke. |


<a id="chatto-api-v1-RevokeRoleResponse"></a>

#### Result: RevokeRoleResponse

Result of revoking a server role.

| Field | Type | Description |
| --- | --- | --- |
| `revoked` | `bool` | True when the request completed. |


<a id="chatto-api-v1-AdminUserManagementService-UpdateUser"></a>

### UpdateUser

Updates a user's login and/or display name as an admin action.

```http
POST /api/connect/chatto.api.v1.AdminUserManagementService/UpdateUser
```

<a id="chatto-api-v1-UpdateUserRequest"></a>

#### Input: UpdateUserRequest

Request to update a user's identity as a server-admin action.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Target user ID. |
| `display_name` | `optional string` | New display name, when changing it. |
| `login` | `optional string` | New login identifier, when changing it. |


<a id="chatto-api-v1-UpdateUserResponse"></a>

#### Result: UpdateUserResponse

Result of an admin user update.

| Field | Type | Description |
| --- | --- | --- |
| `user` | [`UserSummary`](/reference/connectrpc-api/types/#chatto-api-v1-UserSummary) | Updated user profile. |


<a id="chatto-api-v1-AdminUserManagementService-ClearUsernameCooldown"></a>

### ClearUsernameCooldown

Clears the target user's self-service username-change cooldown.

```http
POST /api/connect/chatto.api.v1.AdminUserManagementService/ClearUsernameCooldown
```

<a id="chatto-api-v1-ClearUsernameCooldownRequest"></a>

#### Input: ClearUsernameCooldownRequest

Request to clear a user's self-service username-change cooldown.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Target user ID. |


<a id="chatto-api-v1-ClearUsernameCooldownResponse"></a>

#### Result: ClearUsernameCooldownResponse

Result of clearing a user's username-change cooldown.

| Field | Type | Description |
| --- | --- | --- |
| `cleared` | `bool` | True when the request completed. |

<a id="chatto-api-v1-RoleService"></a>

## RoleService

Provides authenticated role catalog reads and admin role CRUD.

<a id="chatto-api-v1-RoleService-ListRoles"></a>

### ListRoles

Lists the server role catalog. Requires an authenticated user.

```http
POST /api/connect/chatto.api.v1.RoleService/ListRoles
```

<a id="chatto-api-v1-ListRolesRequest"></a>

#### Input: ListRolesRequest

Request the server role catalog.


<a id="chatto-api-v1-ListRolesResponse"></a>

#### Result: ListRolesResponse

Server role catalog plus viewer role-management capabilities.

| Field | Type | Description |
| --- | --- | --- |
| `roles` | repeated [`Role`](/reference/connectrpc-api/types/#chatto-api-v1-Role) | Roles sorted by position. |
| `viewer_can_manage_roles` | `bool` | Whether the caller may create/update/delete role definitions. |
| `viewer_can_assign_roles` | `bool` | Whether the caller may assign/revoke roles and view role rosters. |


<a id="chatto-api-v1-RoleService-GetRole"></a>

### GetRole

Gets one server role plus admin detail metadata. Returns NOT_FOUND when the
role does not exist.

```http
POST /api/connect/chatto.api.v1.RoleService/GetRole
```

<a id="chatto-api-v1-GetRoleRequest"></a>

#### Input: GetRoleRequest

Request one server role.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Stable role name. |


<a id="chatto-api-v1-GetRoleResponse"></a>

#### Result: GetRoleResponse

One server role plus role-management metadata.

| Field | Type | Description |
| --- | --- | --- |
| `role` | [`Role`](/reference/connectrpc-api/types/#chatto-api-v1-Role) | Requested role. |
| `users` | repeated [`UserSummary`](/reference/connectrpc-api/types/#chatto-api-v1-UserSummary) | Explicit users with this role. Empty unless the caller may assign roles. |
| `viewer_can_manage_roles` | `bool` | Whether the caller may create/update/delete role definitions. |
| `viewer_can_assign_roles` | `bool` | Whether the caller may assign/revoke roles and view role rosters. |


<a id="chatto-api-v1-RoleService-CreateRole"></a>

### CreateRole

Creates a custom server role. Requires role.manage.

```http
POST /api/connect/chatto.api.v1.RoleService/CreateRole
```

<a id="chatto-api-v1-CreateRoleRequest"></a>

#### Input: CreateRoleRequest

Request to create a server role.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Stable role name. |
| `display_name` | `string` | Display name shown in admin and mention UIs. |
| `description` | `string` | Optional role description. |
| `pingable` | `bool` | Whether messages may notify users assigned to this role. |


<a id="chatto-api-v1-CreateRoleResponse"></a>

#### Result: CreateRoleResponse

Result of creating a server role.

| Field | Type | Description |
| --- | --- | --- |
| `role` | [`Role`](/reference/connectrpc-api/types/#chatto-api-v1-Role) | Created role. |


<a id="chatto-api-v1-RoleService-UpdateRole"></a>

### UpdateRole

Updates role metadata. Requires role.manage.

```http
POST /api/connect/chatto.api.v1.RoleService/UpdateRole
```

<a id="chatto-api-v1-UpdateRoleRequest"></a>

#### Input: UpdateRoleRequest

Request to update server role metadata.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Stable role name. |
| `display_name` | `string` | Display name shown in admin and mention UIs. |
| `description` | `string` | Optional role description. |
| `pingable` | `optional bool` | Whether messages may notify users assigned to this role, when changing it. |


<a id="chatto-api-v1-UpdateRoleResponse"></a>

#### Result: UpdateRoleResponse

Result of updating a server role.

| Field | Type | Description |
| --- | --- | --- |
| `role` | [`Role`](/reference/connectrpc-api/types/#chatto-api-v1-Role) | Updated role. |


<a id="chatto-api-v1-RoleService-DeleteRole"></a>

### DeleteRole

Deletes a custom server role. Requires role.manage.

```http
POST /api/connect/chatto.api.v1.RoleService/DeleteRole
```

<a id="chatto-api-v1-DeleteRoleRequest"></a>

#### Input: DeleteRoleRequest

Request to delete a server role.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Stable role name. |


<a id="chatto-api-v1-DeleteRoleResponse"></a>

#### Result: DeleteRoleResponse

Result of deleting a server role.

| Field | Type | Description |
| --- | --- | --- |
| `deleted` | `bool` | True when the request completed. |


<a id="chatto-api-v1-RoleService-ReorderRoles"></a>

### ReorderRoles

Replaces the custom role order. Requires role.manage.

```http
POST /api/connect/chatto.api.v1.RoleService/ReorderRoles
```

<a id="chatto-api-v1-ReorderRolesRequest"></a>

#### Input: ReorderRolesRequest

Request to replace custom server role order.

| Field | Type | Description |
| --- | --- | --- |
| `role_names` | `repeated string` | Ordered custom role names. The list must include every custom role exactly once; system roles keep fixed positions and must not be included. |


<a id="chatto-api-v1-ReorderRolesResponse"></a>

#### Result: ReorderRolesResponse

Result of replacing custom server role order.

| Field | Type | Description |
| --- | --- | --- |
| `roles` | repeated [`Role`](/reference/connectrpc-api/types/#chatto-api-v1-Role) | Full role catalog after reordering. |

<a id="chatto-api-v1-PermissionService"></a>

## PermissionService

Provides permission matrix reads and admin permission writes.

<a id="chatto-api-v1-PermissionService-GetRolePermissionTierMatrix"></a>

### GetRolePermissionTierMatrix

Gets the role-permission matrix for a single tier.

```http
POST /api/connect/chatto.api.v1.PermissionService/GetRolePermissionTierMatrix
```

<a id="chatto-api-v1-GetRolePermissionTierMatrixRequest"></a>

#### Input: GetRolePermissionTierMatrixRequest

Request a role-permission tier matrix.

| Field | Type | Description |
| --- | --- | --- |
| `scope` | [`PermissionScope`](/reference/connectrpc-api/types/#chatto-api-v1-PermissionScope) | Target scope. Omit or use SERVER for server scope. |


<a id="chatto-api-v1-GetRolePermissionTierMatrixResponse"></a>

#### Result: GetRolePermissionTierMatrixResponse

Response containing a role-permission tier matrix.

| Field | Type | Description |
| --- | --- | --- |
| `matrix` | [`TierRoles`](/reference/connectrpc-api/types/#chatto-api-v1-TierRoles) | Requested matrix. |


<a id="chatto-api-v1-PermissionService-GetRolePermissionMatrix"></a>

### GetRolePermissionMatrix

Gets one role's full permission matrix. Requires role.manage. Returns
NOT_FOUND when the role does not exist.

```http
POST /api/connect/chatto.api.v1.PermissionService/GetRolePermissionMatrix
```

<a id="chatto-api-v1-GetRolePermissionMatrixRequest"></a>

#### Input: GetRolePermissionMatrixRequest

Request one role's permission matrix.

| Field | Type | Description |
| --- | --- | --- |
| `role_name` | `string` | Stable role name. |


<a id="chatto-api-v1-GetRolePermissionMatrixResponse"></a>

#### Result: GetRolePermissionMatrixResponse

Response containing one role's permission matrix.

| Field | Type | Description |
| --- | --- | --- |
| `matrix` | [`RolePermissionMatrix`](/reference/connectrpc-api/types/#chatto-api-v1-RolePermissionMatrix) | Requested matrix. |


<a id="chatto-api-v1-PermissionService-GetUserPermissionMatrix"></a>

### GetUserPermissionMatrix

Gets one user's full permission matrix. Requires user.manage-permissions.

```http
POST /api/connect/chatto.api.v1.PermissionService/GetUserPermissionMatrix
```

<a id="chatto-api-v1-GetUserPermissionMatrixRequest"></a>

#### Input: GetUserPermissionMatrixRequest

Request one user's permission matrix.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | User ID. |


<a id="chatto-api-v1-GetUserPermissionMatrixResponse"></a>

#### Result: GetUserPermissionMatrixResponse

Response containing one user's permission matrix.

| Field | Type | Description |
| --- | --- | --- |
| `matrix` | [`UserPermissionMatrix`](/reference/connectrpc-api/types/#chatto-api-v1-UserPermissionMatrix) | Requested matrix. |


<a id="chatto-api-v1-PermissionService-ExplainPermissions"></a>

### ExplainPermissions

Explains permission resolution for another user. Requires role.manage.

```http
POST /api/connect/chatto.api.v1.PermissionService/ExplainPermissions
```

<a id="chatto-api-v1-ExplainPermissionsRequest"></a>

#### Input: ExplainPermissionsRequest

Request permission explanations for a user at server or room scope.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | User whose permissions should be explained. |
| `room_id` | `string` | Optional room scope. Omit for server-scoped permissions. |


<a id="chatto-api-v1-ExplainPermissionsResponse"></a>

#### Result: ExplainPermissionsResponse

Permission explanations for all permissions applicable at the requested scope.

| Field | Type | Description |
| --- | --- | --- |
| `explanations` | repeated [`PermissionExplanation`](/reference/connectrpc-api/types/#chatto-api-v1-PermissionExplanation) | Explanation rows sorted by permission metadata order. |


<a id="chatto-api-v1-PermissionService-SetRolePermission"></a>

### SetRolePermission

Sets one role permission decision. Requires role.manage or scoped room.manage for room scope.

```http
POST /api/connect/chatto.api.v1.PermissionService/SetRolePermission
```

<a id="chatto-api-v1-SetRolePermissionRequest"></a>

#### Input: SetRolePermissionRequest

Request to set one role permission state.

| Field | Type | Description |
| --- | --- | --- |
| `role_name` | `string` | Role name. |
| `permission` | `string` | Permission identifier. |
| `decision` | [`PermissionDecision`](/reference/connectrpc-api/types/#chatto-api-v1-PermissionDecision) | New decision. NONE clears the explicit state. |
| `scope` | [`PermissionScope`](/reference/connectrpc-api/types/#chatto-api-v1-PermissionScope) | Target scope. Omit or use SERVER for server scope. |


<a id="chatto-api-v1-SetRolePermissionResponse"></a>

#### Result: SetRolePermissionResponse

Result of setting one role permission state.

| Field | Type | Description |
| --- | --- | --- |
| `ok` | `bool` | True when the request completed. |


<a id="chatto-api-v1-PermissionService-SetUserPermission"></a>

### SetUserPermission

Sets one user permission decision. Requires user.manage-permissions.

```http
POST /api/connect/chatto.api.v1.PermissionService/SetUserPermission
```

<a id="chatto-api-v1-SetUserPermissionRequest"></a>

#### Input: SetUserPermissionRequest

Request to set one user permission state.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Target user ID. |
| `permission` | `string` | Permission identifier. |
| `decision` | [`PermissionDecision`](/reference/connectrpc-api/types/#chatto-api-v1-PermissionDecision) | New decision. NONE clears the explicit state. |
| `scope` | [`PermissionScope`](/reference/connectrpc-api/types/#chatto-api-v1-PermissionScope) | Target scope. Omit or use SERVER for server scope. |


<a id="chatto-api-v1-SetUserPermissionResponse"></a>

#### Result: SetUserPermissionResponse

Result of setting one user permission state.

| Field | Type | Description |
| --- | --- | --- |
| `ok` | `bool` | True when the request completed. |

<a id="chatto-api-v1-AdminDiagnosticsService"></a>

## AdminDiagnosticsService

Provides owner-only server diagnostics for the admin system page.

<a id="chatto-api-v1-AdminDiagnosticsService-GetSystemInfo"></a>

### GetSystemInfo

Returns broker, JetStream, and projection diagnostics.

```http
POST /api/connect/chatto.api.v1.AdminDiagnosticsService/GetSystemInfo
```

<a id="chatto-api-v1-GetSystemInfoRequest"></a>

#### Input: GetSystemInfoRequest

Request to read owner-only server diagnostics.


<a id="chatto-api-v1-GetSystemInfoResponse"></a>

#### Result: GetSystemInfoResponse

Owner-only operational diagnostics for the Chatto server.

| Field | Type | Description |
| --- | --- | --- |
| `system_info` | [`AdminSystemInfo`](/reference/connectrpc-api/types/#chatto-api-v1-AdminSystemInfo) | Broker connection, account, and JetStream state. |
| `projections` | repeated [`AdminProjectionState`](/reference/connectrpc-api/types/#chatto-api-v1-AdminProjectionState) | Event-sourced projection health and memory estimates. |

<a id="chatto-api-v1-AdminEventLogService"></a>

## AdminEventLogService

Provides server-admin audit/event-log diagnostics.

<a id="chatto-api-v1-AdminEventLogService-ListEvents"></a>

### ListEvents

Lists durable EVT entries newest-first.

```http
POST /api/connect/chatto.api.v1.AdminEventLogService/ListEvents
```

<a id="chatto-api-v1-ListEventsRequest"></a>

#### Input: ListEventsRequest

Request to list durable EVT entries newest-first.

| Field | Type | Description |
| --- | --- | --- |
| `limit` | `int32` | Maximum entries to return. The server clamps this to its diagnostic limit. |
| `before` | `optional string` | Exclusive sequence cursor. Returned entries are older than this sequence. |
| `filter` | [`AdminEventLogFilter`](/reference/connectrpc-api/types/#chatto-api-v1-AdminEventLogFilter) | Optional filter. Filtered scans inspect a bounded window per request. |


<a id="chatto-api-v1-ListEventsResponse"></a>

#### Result: ListEventsResponse

Newest-first page of durable EVT entries.

| Field | Type | Description |
| --- | --- | --- |
| `entries` | repeated [`AdminEventLogEntry`](/reference/connectrpc-api/types/#chatto-api-v1-AdminEventLogEntry) | Event-log rows. |
| `has_older` | `bool` | Whether older rows may be fetched with end_cursor. |
| `end_cursor` | `optional string` | Sequence cursor for loading older rows. |
| `total_count` | `int64` | Total EVT message count reported by JetStream. |
| `scanned_count` | `int32` | Number of stream sequences inspected while building this page. |
| `scan_limit` | `int32` | Maximum number of stream sequences inspected for this page. |
| `scan_limited` | `bool` | True when a filtered request reached scan_limit before finding a full page. |


<a id="chatto-api-v1-AdminEventLogService-ListEventTypes"></a>

### ListEventTypes

Lists filterable event type names.

```http
POST /api/connect/chatto.api.v1.AdminEventLogService/ListEventTypes
```

<a id="chatto-api-v1-ListEventTypesRequest"></a>

#### Input: ListEventTypesRequest

Request to list filterable event types.


<a id="chatto-api-v1-ListEventTypesResponse"></a>

#### Result: ListEventTypesResponse

Event types accepted by event-log filters.

| Field | Type | Description |
| --- | --- | --- |
| `event_types` | `repeated string` | Sorted event type names. |


<a id="chatto-api-v1-AdminEventLogService-GetEvent"></a>

### GetEvent

Reads one durable EVT entry by stream sequence. Returns NOT_FOUND when the
requested sequence does not exist.

```http
POST /api/connect/chatto.api.v1.AdminEventLogService/GetEvent
```

<a id="chatto-api-v1-GetEventRequest"></a>

#### Input: GetEventRequest

Request to read one durable EVT entry by stream sequence.

| Field | Type | Description |
| --- | --- | --- |
| `sequence` | `string` | Required EVT stream sequence. |


<a id="chatto-api-v1-GetEventResponse"></a>

#### Result: GetEventResponse

Result of reading one durable EVT entry.

| Field | Type | Description |
| --- | --- | --- |
| `entry` | [`AdminEventLogEntry`](/reference/connectrpc-api/types/#chatto-api-v1-AdminEventLogEntry) | Requested event-log entry. |
